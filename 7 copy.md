## 三、网卡IP配置命令（三层操作）

该命令为网卡绑定IP门牌，激活三层网络协议，没有IP则无法实现IP通信、ping、上网。

```Plain
# 给eth0网卡绑定IP和子网
sudo ip addr add 10.0.0.10/24 dev eth0
```

### 参数详解

- **ip addr add**：添加三层IP地址
- **10.0.0.10**：本机网卡的IP门牌（局域网唯一标识）
- **/24**：子网掩码，划定局域网范围 10.0.0.0 ~ 10.0.0.255，告诉内核该网段都是本地邻居
- **dev eth0**：device 设备，指定IP绑定到 eth0 这张网卡（多网卡机器必须指定）

### 配套操作命令

```Plain
# 查看网卡IP
ip -br addr eth0

# 删除IP（必须带/24子网，否则报错）
sudo ip addr del 10.0.0.10/24 dev eth0
```

核心结论：只开网卡（link up）不配置IP，会提示 Network is unreachable，无法进行任何IP通信。

## 四、ARP协议原理（局域网通信核心）

场景：三层通信靠IP，二层传输靠MAC，**ARP就是通过IP查询对方MAC地址的协议**，仅用于局域网。

### ARP完整工作流程

1. 本机需要和同网段设备通信（如10.0.0.20），已知对方IP，未知对方MAC
2. 本机通过eth0发送ARP广播，向局域网所有设备喊话：谁是10.0.0.20？请回复你的MAC地址
3. 目标设备接收广播，单播回复自身MAC地址
4. 本机记录 IP-MAC 映射关系，存入ARP缓存
5. 后续通信直接使用缓存MAC地址，无需重复广播

### ARP常用命令

```Plain
# 查看本机ARP缓存表（记录所有局域网邻居IP+MAC）
ip neigh

# 清空ARP缓存（排查异常时使用）
ip neigh flush all
```

核心限制：ARP **仅局域网生效**，跨网段、外网通信无法使用ARP，必须依靠网关。

## 五、网关原理（跨网段、外网通信核心）

局域网设备只能通过ARP互通，**所有非本地网段、外网流量，必须交给网关中转**，网关是内网和外网的桥梁。

### 网关核心作用

网关设备一般拥有两张网卡，一端接内网、一端接外网，负责转发内网设备的外网请求、回传外网响应数据。

### 配置默认网关命令

```Plain
# 配置所有外网流量，统一交给网关10.0.0.1，从eth0网卡发出
sudo ip route add default via 10.0.0.1 dev eth0

# 查看路由表
ip route
```

### 外网通信完整流程（本机10.0.0.10访问公网）

1. 本机判断目标IP不在10.0.0.0/24局域网内
2. 匹配默认路由，将数据包交给网关 10.0.0.1
3. 通过ARP获取网关的MAC地址
4. 二层封装网关MAC、三层封装目标公网IP，发送数据包
5. 网关转发数据包至外网，响应数据原路返回本机

关键知识点：访问外网时，二层目标MAC是网关MAC，三层目标IP是外网IP。

## 六、Linux 网络开机完整配置流程（必背）

```Plain
# 1. 二层开启网卡链路
sudo ip link set eth0 up

# 2. 三层配置网卡IP和子网
sudo ip addr add 10.0.0.10/24 dev eth0

# 3. 配置默认网关，实现外网访问
sudo ip route add default via 10.0.0.1 dev eth0

# 全套检查命令
ip -br link    # 检查网卡链路状态
ip -br addr    # 检查网卡IP配置
ip route       # 检查路由、网关
ip neigh       # 检查局域网ARP邻居
```

## 七、核心层级与命令对照表

| 网络层级       | 核心内容                    | 对应操作命令                     |
| :------------- | :-------------------------- | :------------------------------- |
| 1层 物理层     | 网线、电信号、物理通断      | 查看 LOWER_UP 标志（无手动命令） |
| 2层 数据链路层 | MAC地址、以太网帧、链路开关 | ip link set up/down              |
| 3层 网络层     | IP、子网、ARP、路由、网关   | ip addr、ip route、ip neigh      |

## 八、常见网络排错要点

- 无LOWER_UP：第一层物理故障（网线、端口、硬件问题）
- 链路UP但无法ping：未配置IP地址，三层未生效
- 内网通、外网不通：未配置默认网关或网关地址错误
- 局域网访问异常：ARP缓存出错，执行 ip neigh flush all 清空重试

