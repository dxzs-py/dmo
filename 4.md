适配现有Django仓库目录：apps/fastapi_service 同仓库双进程独立服务）
### 核心结论
彻底脱离 Celery、对齐 Claude Code / Trae IDE 成熟产品标准范式，采用**会话级单协程执行上下文 + Redis 事件驱动**架构；
适配现有 Django 工程：FastAPI Agent 执行代码统一存放于项目 `apps/fastapi_service` 目录，仅做代码归类，**运行时完全独立双进程**，不注册为 Django 子应用、不共用 Web 进程；
既保证仓库目录整洁统一、复用公共ORM/配置/缓存，又彻底隔离长时Agent任务与Django前端Web服务，稳定支撑30分钟以上深度研究会话，从根源消除锁竞争、死锁、审批恢复失败、状态不一致等全部问题。

Claude Code、Trae IDE、Cursor、Devin 主流Agent产品执行层均采用同类架构，无一款基于Celery短任务队列实现；本方案并非替换中间件，而是重构整套错误的调度模型，让执行架构匹配Agent「长会话、有状态、频繁中断、实时交互」的原生业务属性。

---
## 一、成熟产品底层设计三条核心铁律（当前旧架构核心偏差）
1.  **一会话一执行流**：单个深度研究会话绑定唯一协程上下文，生命周期全程不拆分，不存在多组执行/恢复任务，天然无锁、无抢锁、无批次互等死锁。
2.  **中断为原生协程暂停，不退出进程**：触发高危工具审批、子代理等待时，协程通过`asyncio.Event`主动挂起，完整保留内存执行上下文；用户审批信号直达协程毫秒唤醒，无需跨任务加载状态，无一致性损耗。
3.  **全链路事件驱动，无轮询调度延迟**：审批、终止、追加指令、工具输出、子代理状态全部基于实时事件通道双向流转，摒弃轮询、队列重试带来的卡顿与丢失风险。

原有Celery架构全部痛点（死锁、审批无结果、前端状态滞后、刷新丢失子代理层级、恢复超时失败），根源是**用短一次性任务队列承载长交互会话**；更换本架构后，所有问题的前置依赖条件直接消失。

---
## 二、完整分层架构（适配Django+独立FastAPI双进程模式）
### 仓库目录结构（你现有工程改造方案）
```
backend\Django_xm
├── data
└── Django_xm
    ├── apps
    │   ├── agent_hub / ai_engine / chat / research / users 【原有Django业务子应用】
    │   └── fastapi_service 【FastAPI Agent执行层，仅代码目录，不注册Django App】
    ├── common              # 全局公共模块（双服务共用：异步ORM、aioredis、工具函数）
    ├── settings           # 全局数据库、Redis、密钥配置，FastAPI直接import复用
    ├── utils
    ├── asgi.py / wsgi.py  # 仅启动Django Web服务，不承载Agent长任务
    ├── celery.py          # 保留，仅用于邮件、同步批量数据等短任务，移除深度研究相关任务
```

### 部署运行约束（关键，保证服务完全隔离）
1.  **双独立进程分开启动，禁止同进程混跑**
    ```bash
    # 进程1：Django Web服务（页面、用户鉴权、聊天基础接口、审批提交HTTP接口、管理后台）
    gunicorn Django_xm.asgi:application --workers 4 -k uvicornworkers --port 8000

    # 进程2：独立FastAPI Agent执行服务（长会话、协程、WebSocket/SSE、事件订阅）
    uvicorn apps.fastapi_service.main:app --host 0.0.0.0 --port 8001
    ```
2.  `apps/fastapi_service` 仅做代码存放，不写入`INSTALLED_APPS`、不挂载至Django urls，不依赖Django同步生命周期组件。
3.  数据互通：两套进程**共享同一套MySQL、Redis实例**；公共模型、配置、工具抽离至顶层`common`，双向只读导入，禁止FastAPI反向耦合Django同步中间件/请求对象。

### 整体分层架构图
```
┌───────────────────────────────────────────────────────────────────┐
│                        前端 + Django Web接入层                    │
│  Django职责：页面渲染、用户鉴权、创建会话HTTP接口、提交审批、管理后台
│  前端动作：发起深度研究、点击审批、终止任务、接收实时工具事件
└─────────────────────┬─────────────────────────────────────────────┘
                      │ 1.创建会话HTTP调用 / 2.提交审批写入DB+发Redis事件
                      │ 3.前端直连FastAPI 8001端口 SSE/WebSocket长连接
┌─────────────────────▼─────────────────────────────────────────────┐
│                FastAPI Agent 独立执行服务（apps/fastapi_service）  │
│  ┌─────────────────────────────────────────────────────────────┐  │
│  │  SessionManager 会话管理器                                  │  │
│  │  会话创建/销毁、并发限流、崩溃自愈、优雅停机、批量续跑快照  │  │
│  └─────────────┬───────────────────────────────────────────────┘  │
│                │ 生成/回收会话协程
│  ┌─────────────▼──────────┐  ┌─────────────┐  ┌─────────────┐   │
│  │ SessionExecutor 会话1  │  │ 会话执行器2 │  │ 会话执行器N │   │
│  │ 单协程全生命周期运行   │  │             │  │             │   │
│  │ LangGraph运行时        │  │             │  │             │   │
│  │ Interrupt中断控制器    │  │             │  │             │   │
│  │ Agent树形执行栈        │  │             │  │             │   │
│  │ 统一事件发射器         │  │             │  │             │   │
│  └─────────────┬──────────┘  └─────────────┘  └─────────────┘   │
│                │ 进程内本地事件通信
│  ┌─────────────▼──────────┐                                   │
│  │ 事件总线模块           │◄──── Redis Pub/Sub 跨进程外部信令 │
│  │ asyncio本地事件分发    │        审批/终止/追加指令信号    │
│  └────────────────────────┘                                   │
└───────────────────────────────────────────────────────────────────┘
                      │ 异步批量持久化写入
┌─────────────────────▼─────────────────────────────────────────────┐
│                        共享持久化层（Django/FastAPI共用）          │
│  · Checkpoint快照：Redis热存 + MySQL落地 LangGraph完整状态快照
│  · 业务事件落库：工具调用、子代理日志、审批记录（MySQL）
│  · 会话元数据表：存储会话状态、进度、用户关联信息
└───────────────────────────────────────────────────────────────────┘
```

### 各分层完整职责拆解
#### 1. Django Web接入层（原有业务不动）
- 提供页面、用户登录鉴权、会话创建接口、审批提交接口、管理后台；
- 用户提交审批时，**先写入数据库持久化审批记录，再向Redis频道广播审批事件**；
- 前端页面单独维护两套连接：8000端口Django普通HTTP请求、8001端口FastAPI WebSocket/SSE长连接拉取实时工具、子代理事件；
- 鉴权打通：Django签发Token/Session同步传递给FastAPI，复用同一套用户身份校验逻辑。

#### 2. FastAPI Agent执行服务核心：SessionExecutor 会话执行器
单个深度研究会话对应独立协程实例，全程不退出、不拆分任务，架构核心载体：
```python
class SessionExecutor:
    def __init__(self, session_id: str, user_id: str):
        self.session_id = session_id
        self.user_id = user_id
        self._interrupt_event = asyncio.Event()  # 审批等待原生信号
        self._pending_approvals = {}
        self._agent_stack = []  # 树形子代理执行栈，自动携带depth/agent_name/agent_path

    async def run(self, initial_input):
        """单协程完整生命周期执行循环，持续运行至会话结束"""
        async for event in self.graph.stream(initial_input):
            if event.type == "interrupt":
                # 触发审批中断：持久化快照、推送待审批事件、协程挂起等待
                self._pending_approvals = event.approvals
                self._emit_approval_event(event.approvals)
                await self._wait_for_approvals()
                # 全部审批完成，注入决策恢复LangGraph执行
                self.graph.resume(self._decisions)
            elif event.type == "tool_call":
                self._emit_tool_event(event)
            elif event.type == "sub_agent_start":
                self._agent_stack.append(event.agent_meta)
                self._emit_subagent_event(event)
            elif event.type == "sub_agent_end":
                self._agent_stack.pop()
                self._emit_subagent_event(event)

    async def _wait_for_approvals(self):
        # 纯协程挂起，无CPU占用，支持超时/终止信号打断
        self._interrupt_event.clear()
        await self._interrupt_event.wait()

    def on_approval_received(self, approval_id: str, decision: bool):
        # 接收Redis广播的审批信号，校验完整后唤醒协程
        self._pending_approvals[approval_id].decision = decision
        if all(a.decision is not None for a in self._pending_approvals.values()):
            self._decisions = {a.id: a.decision for a in self._pending_approvals.values()}
            self._interrupt_event.set()  # 毫秒级唤醒恢复执行
```

#### 3. SessionManager 会话调度管理器（替代原Celery调度能力）
完全自研轻量会话调度模块，专属FastAPI服务内部使用：
1. 生命周期管控：接收Django创建会话请求，初始化并启动`SessionExecutor`协程；会话正常结束/异常崩溃自动回收资源；
2. 并发限流：配置最大并行会话数，超限请求排队缓冲，防止服务资源过载；支持按用户、会话类型独立限流；
3. 崩溃自愈：协程异常崩溃后，读取最新Checkpoint快照自动重建执行上下文，用户无感知续跑；
4. 优雅停机：服务发布/重启时，主动触发所有运行会话写入快照并标记待恢复；下次启动批量扫描快照自动恢复未完成深度研究；
5. 信令路由：接收Redis Pub/Sub跨进程信号（审批、终止、追加指令），精准分发至对应会话协程。

#### 4. 双轨事件总线（进程内本地事件 + Redis跨进程信令）
1. 本地事件：同进程会话间通信使用`asyncio.Event`，延迟极低，用于会话内部工具、子代理事件分发；
2. Redis Pub/Sub：跨进程、跨节点的外部控制指令（审批、终止会话）广播通道，轻量解耦；
3. 统一事件规范：所有输出事件固定携带`session_id、depth、agent_name、agent_path、时间戳`；推送前端、异步落库使用同一套数据源，彻底解决刷新子代理标识丢失、前后端状态不一致问题。

#### 5. 持久化分层（内存优先、持久化兜底，双服务共享存储）
1. 运行上下文优先存放协程内存：正常执行读写无IO损耗，状态天然一致；
2. Checkpoint快照落盘：仅在进入审批中断、会话退出、定时快照三个节点写入Redis+MySQL，用于崩溃、服务重启续跑；
3. 事件异步批量写入数据库：工具调用、子代理、审批事件走异步批量落盘，不阻塞Agent主执行协程。

---
## 三、本架构对全部痛点的根治效果（适配30分钟以上长任务）
1. **审批死锁、多批次互等卡死**
原方案：多层锁、批次防护、任务接力逻辑堆砌，仍存在残留状态死锁；
新架构：单会话单协程，不存在多执行任务互相检测pending批次，死锁逻辑前提完全消失。

2. **审批完成后无工具输出、恢复任务超时失败**
原方案：Celery任务抢锁重试上限150s，锁TTL长达1800s，锁释放后无任务接力；
新架构：审批信号通过Redis直达运行中协程，毫秒唤醒，不存在任务调度、抢锁、重试失败问题；即使Redis消息丢失，协程定时从DB拉取审批记录兜底。

3. **恢复延迟高，用户点击审批长时间无响应**
原方案：依赖Celery队列调度、轮询检测；
新架构：事件驱动即时唤醒，无调度延迟。

4. **前端状态滞后、刷新后子代理层级丢失**
原方案：推送、落库两套独立逻辑，字段时序不一致；
新架构：单事件源统一推送+落库，全链路携带agent_path/depth，前端可直接实现分组树形渲染。

5. **SSE轮询延迟，状态不实时**
原方案：前端600s上限轮询拉取状态；
新架构：WebSocket/SSE实时推送状态变更，无需前端主动轮询。

6. **长任务资源占用过高，并发能力差**
原方案：Celery进程独占Worker，等待审批时持续占用进程资源；
新架构：协程await挂起几乎无CPU/内存开销，单进程可同时承载上百个30分钟+等待会话，并发上限提升10~20倍。

7. **发布重启中断正在运行的深度研究**
原方案：Django、Celery重启直接杀死所有长任务，进度丢失；
新架构：Django页面迭代仅重启8000端口Web服务，不影响8001端口FastAPI长会话；FastAPI重启前自动全量快照，重启后自动续跑全部未完成任务。

8. **排障链路复杂，跨多Celery任务日志分散**
原方案：一次执行链路分散在数十条Celery任务日志，排查繁琐；
新架构：单会话完整线性协程日志，按session_id全链路聚合，定位问题零成本。

---
## 四、对标Claude Code/Trae IDE的高阶原生能力
1. **运行中动态干预**：支持会话执行中途终止、暂停、追加用户提问、修改执行参数，全部通过Redis事件通道实时下发至协程，现有Celery架构很难实现；
2. **跨发布断点续跑**：服务器重启、版本发布后，自动从快照恢复30分钟、数小时长会话，用户无需重复操作，对齐商业IDE续跑体验；
3. **子代理树形分组渲染**：执行栈天然维护父子代理层级，后端事件统一输出层级字段，前端直接实现「agentPath分组卡片+组内工具树形缩进」最优展示方案；
4. **全流式输出**：Agent思考、规划、工具调用、子代理启动全过程流式实时推送，无需等待完整会话结束；
5. **审批超时自动策略**：中断控制器内置超时计时器，可配置超时自动拒绝/自动放行高危工具，无需外部定时调度任务。

---
## 六、轻量备选方案：LangGraph Server（减少自研工作量）我觉得这个更好，用框架提供的，框架不能满足再自研。
若不想从零完整自研SessionExecutor、会话调度逻辑，可直接基于LangGraph官方LangGraph Server封装业务层：
1. 原生支持interrupt中断恢复、Checkpoint持久化、流式输出、会话隔离；
2. 内置协程调度、并发管控，省去大量底层执行逻辑开发；
3. 完全脱离Celery，可同样部署在`apps/fastapi_service`目录、独立进程运行，和Django分层解耦。
**优势**：减少底层执行层重复造轮子，开发周期缩短；
**不足**：自定义子代理执行栈、事件格式、干预逻辑需要额外二次封装，极致定制化能力弱于纯自研架构。

---
## 最终总结
1. 适配现有Django工程最优落地形式：FastAPI Agent代码统一存放于`apps/fastapi_service`目录，仅做代码归类，运行时分双独立进程完全隔离，兼顾仓库整洁与分层架构优势；
2. 整套事件驱动单协程架构是对齐Claude Code、Trae IDE行业标准的彻底根治方案，从底层抹除Celery短任务队列带来的全部并发、状态、延迟缺陷；
3. 稳定支撑30分钟以上甚至数小时深度研究会话，配套快照续跑、优雅停机、消息丢失多层兜底，线上稳定性强；
4. 可选用LangGraph Server作为执行底座封装业务。