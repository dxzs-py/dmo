那我这样说可以吗？还有图片的选择你会选择什么？或者有更好的方法？：
task 立刻返回 “等待状态”，父图退出，靠外部 resume 驱动父图继续（主流 Claude‑Code / Trae‑Solo 的原生子 Agent 模型）
Claude‑Code Fork‑Subagent、Trae‑IDE Solo：父 Agent 绝不长驻等待。
调用SubAgentRuntime.spawn()派生子 Agent。
task 工具立刻返回，父 Graph 直接执行结束、做 checkpoint 持久化，父协程完全退出释放所有资源。
UI 展示子 Agent 卡片，子 Agent 遇到审批走自己独立resume_subagent。
子 Agent 全部跑完之后，由外部事件回调，重新唤醒父 Graph，把子 Agent 结果注入父图 state，父图从 checkpoint 恢复继续跑后续报告逻辑。
1、pending_interrupt_info✅ 直接保留批量结构，不压扁为单工具字段。理由：底层中间件天然支持批量审批；强行拆成单工具会做一层适配转换，增加适配器复杂度，未来遇到多工具同时触发审批的场景又要回退改数组；没有收益，属于过度设计。适配器（langgraph_adapter /deepagents_adapter）输出必须严格对齐该结构；底层框架原始 interrupt 对象禁止直接透传到上层。前端SubAgentDetailPanel直接遍历requests数组渲染多条审批项；摘要卡片只展示提示：等待你的确认，共N项待审批。Task6 SSE 定向推送 —— 本次必须完成（核心基础能力，无妥协）这是新子代理 UI、事件隔离的底层依赖，不能延后。不复用旧事件字段，新增顶层字段 subagent_thread_id?: string普通主会话事件：该字段null/undefined子代理产生的全部事件：必须携带有效的subagent_thread_id子代理内部所有 chunk：thinking、tool 调用、tool 返回、error、interrupt 事件全部带上subagent_thread_id。协议约束写入 spec：subagent_thread_id为 snake_case，不参与 camelCase 转换，作为路由原始标识符。区分两套渲染分支：