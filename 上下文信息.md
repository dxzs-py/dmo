# 上下文交接信息（项目质量整改主线）

> 用途：会话上下文丢失后恢复工作状态。新会话开局读本文件 + `任务提示词.md` 即可继续。
> 最后更新：2026-08-20（C3 第②批依赖方向债务清偿完成：三契约豁免清零；C 类已推进 8 批）

---

## 一、如何激活上下文（新会话必读）

1. **读本文件全文** + `一开始的任务提示词.md`（优化后的任务总纲，含完成状态标记）。
2. **项目权威文档**（需要细节时再查，不要一次全读）：
   - `项目质量分析报告.md`（约120KB，必须用 offset/limit 分段读，禁止整读）——所有发现项的详述、修复标记、优先级都在此。搜索关键词：`dj-01`、`✅已修复`、`推荐执行顺序`。
   - `.trae/rules/SpecificationRequirements.md`——命名规范（snake_case/camelCase 边界、sessionTransformers 唯一转换边界）。
   - `.trae/specs/<spec名>/spec.md + tasks.md`——每个阶段的详细交付记录（见下文 spec 清单）。
3. **当前进度位置**：推荐路线 6 步已全部完成（A类基建→死代码删除→B类第一波→lc-06→B类第二批→**B类第三批✅ 2026-08-20 · spec:b3-prod-config-cache**），**C类第一批轻量收敛✅ 2026-08-20 · spec:c1-lightweight-convergence**（rd-02 收口/dj-14/cq-07/dj-10/dj-07 五项），**C类第二批模型/迁移一致性✅ 2026-08-20 · spec:c2-model-migration-consistency**（dj-12 冗余索引/dj-17 信号注册/dj-21 squash 三 app 迁移 20→1·13→1·10→1/dj-22 choices 全仓 TextChoices 化含 approvals 175 处调用点），**C7 第②批分页+路由规范化✅ 2026-08-20 · spec:c7b2-pagination-route-normalization**（dj-15 ProjectPagination 统一分页+approvals 真分页删[:100]+chat/knowledge 手写收敛/dj-16 research result 删+task→tasks+chat 删除路由 RESTful 化，前端 4 调用点 100% 适配），**C2 依赖清单重写✅ 2026-08-20 · spec:c2-requirements-rewrite**（rd-01/dj-02/rd-12：requirements 219 行→56 条直接依赖，清除 spacy/jupyter 全家桶等 162 条，补 fastapi/uvicorn/python-docx 等缺漏，干净 venv 安装回归 318 测试全绿，redis 上界<8、langgraph-store-postgres 移除两处上游漂移修正，验证报告见 spec 目录），**C7 第③批数据模型收敛✅ 2026-08-20 · spec:c7b3-data-model-consolidation**（dj-18 ChatMessage JSONField 去 null+18 处双轨读点清理/dj-13 DocumentIndex 彻底删除+IndexMetadata 唯一实体+文件数派生查询+迁移拆三段修复 PG pending trigger events，C7 至此全部收口），**learning 路由收敛+knowledge 死代码清理✅ 2026-08-20 · spec:learning-route-convergence**（dj-16 learning 延伸：task/→tasks/+views_kb get_parsers 时序 bug 根因修复+IndexInfoSerializer/views.py 垫片死代码删除；后端 330/330+前端 127/127+schema.yml 重生成零旧路由），**C3 第①批跨 app 依赖方向治理✅ 2026-08-20 · spec:c3a-dependency-direction-core**（ds-02 三步的第①②步核心项+③契约固化：keyword_overlap 公共化/BaseAppError 迁 common/graph 工厂注册注入/权限类迁 core/韧性模块归位 ai_engine/信令总线迁 common/import-linter 三契约全绿），**C3 第②批依赖方向债务清偿✅ 2026-08-20 · spec:c3b-dependency-debt-cleanup**（审批三件套迁 apps/approvals/services+approval_service 4 私有公共化/realtime_events 幽灵过滤 set_user_history_filter 注册注入/persist_chat_tool_calls 归位 stream_persistence/merge_tool_calls_incremental+publish_stream_event+group_name 公共化/.importlinter 三契约 7 条豁免全部清零；后端 339/339+前端 127/127 全绿，ds-02 至此收口），**C4 后端长函数拆分✅ 2026-08-20 · spec:c4-split-long-functions**（cq-03 收口：6 个超百行函数全部 ≤80 行——chat_service 四方法 535/204/130/113→38/34/12/11 行编排+33 个私有方法+2 dataclass+rebuild_agent_fn 闭包工厂化/stream_persistence 161/112/85→62/74/70 行+10 个函数+39 个直接单测/deep_builder 143→30 行+6 私有方法；顺带删不可达死分支 28 行+死赋值 2 处；行为保持，后端 378/378（339+39 新增）+前端 127/127+lint-imports 零豁免全绿），**C5 前端 store 拆分✅ 2026-08-20 · spec:c5-split-frontend-stores**（cq-04 收口：session.js 1700 行拆为 stores/session/ 五切片领域组合——sessionList/sessionCrud/messageFlow/versionFlow/toolCallState 各自持有 state、runtimeDeps 运行时注册表解决跨切片循环调用、公共 API/store id/import 路径零变化 41 消费点零修改；handleSessionEvent.js 707→297 行纯路由——审批 handler 下沉 approvalHandler.js/子代理与研究状态下沉 subagentHandlers.js/stream_reasoning 并入 streamStateHandlers.js；新增 61 个 handler 单测；**执行中按用户指令"不要为了压缩而压缩"修正 approvalHandler 为凑 80 行的三段式硬拆（还原 162 行连续形态），toolCallState 537 行超 450 指标但职责纯粹保持不硬拆**；前端 vitest 188/188（127+61）+build+lint 三绿、后端 378/378 无波及），**C5 验收期根治✅ 2026-08-20**（finalized=false 死协议清零：用户指正后完整分析确认 finalized 为深研回写事件专属参数恒 true、聊天 SSE 结束事件从不含该字段，不可达分支 33 行删除 + 注释叙事全面修正），**C6 巨型组件拆分✅ 2026-08-20 · spec:c6-split-giant-components**（cq-05 四个千行级收口：ToolSelector 1114→574 行容器+三 tab 子组件 LangchainToolList/McpServerList/SkillList（props/emits 契约逐字不变、调用点零修改、跨 tab 共享样式入 popover 命名空间非 scoped 块）；DeepResearchView 1377→1175 行+useDeepResearchPolling/useDeepResearchKnowledge（**实例安全：零模块级可变状态+onUnmounted 自动清理定时器**）+ContinueResearchDialog 子组件+死样式 135 行删除；WorkflowView 1303（实测）→1036 行+useWorkflowPolling/useWorkflowExecution（实例安全+onUnmounted 断开 SSE）+api/workflow.js 死方法 6 个删除；**ChatMessage 按用户"不为拆分而拆分"原则变更锁死零改动**（1188 行中样式占 558 行/script 仅 300 行且已委托 8 子组件/高频流式渲染路径，git 确认零 diff）；Composable 实例安全为用户补充强制约束——无全局单例/全新 ref/onUnmounted 自动清理；前端 188/188+build+lint 三绿、后端 378/378 无波及）——**C 类主线（C1-C8）至此全部收口**。**cq-06 收口✅ 2026-08-20 · spec:converge-validation-zod**（实测"7 个工具函数违规导出"已被前序批次顺带解决，types/index.js 现为纯枚举+JSDoc 零函数导出并固化约束注释；本批收敛 validateMessage 为 MessageSchema.safeParse 包装（zod 单一出口）+ ChatRequestSchema role 字面量改引用 MessageRole 枚举源；前端 188/188+build+lint 三绿），**cq-08/cq-10 核验补标✅ 2026-08-20 · spec:verify-cq08-cq10-resolved**（零代码核验批：两项均实测确认已被 b1 批次完整解决——cq-08 parseProtocolEvent 已建于 sse.js:173 且 readSSEStream 双点内置 + 消费点全收敛 + 8 单测；cq-10 axios 调试序列化已限 DEV——报告漏标已补，标记沿用 b1 完成日 2026-08-19），**终局复审✅ 2026-08-20 · spec:reassess-post-refactor-quality**（以重构前快照 `D:\programming\langchain\langchain_xm_\langchain_xm` 为只读基线的全面复审，报告增补第 11 章权威视图：**①功能完整性——后端零回归（路由/事件契约逐行一致零漂移/六链路语义等价/squash 完整性通过），前端仅 1 处真实回归：dj-01 残余 R1（user.js:59 读 userData.is_staff 但 axios 已 toCamelCase 转 isStaff → isAdmin 恒 false → 管理员缓存清理按钮不可见）+ 后端 pattern 组合绕过（cache_manager/views.py:199 if pattern 优先于 scope=query 分支），两处 <10 行建议立即立项**；②✅ 标记真实性——80 项终局盘点：47 已解决/6 部分/3 用户保留/24 未解决，抽样 20+ 项 18 项行号级吻合，漂移修正：dj-01 降级部分解决（已改）、ds-15/cq-09 正向补标、rd-07/lc-15 部分失效；③五维度重评——新发现 P1 两项（dj-01R + dj-30 五 app 零测试含 users/core 安全路径）+ P2 八项（cq-14 adapter 764 行巨函数/lc-02 prompt 双轨/dj-31 MIDDLEWARE 重复/vue-01 console.log 9 处/vue-02 超 500 行组件实为 18 个/fd-01 延迟导入 61%）+ P3 五项，评分 Django 7→7.5/功能设计 7→7.5/冗余 6.5→7/Vue3 专项新增 8 分；④实证健康——script setup 100%/命名零违例/print 零残留/N+1 防护到位/sync.js 装配链零差异）。剩余项：**P1 立即项 dj-01R（isAdmin 键名 + pattern 绕过）与 dj-30（users/core 测试）**；P2 清单见报告 11.4；用户保留 lc-01/lc-03/cq-13；部署前需 staging 演练 squash 迁移。

## 二、环境与命令（务必照抄，勿改）

- **后端 Python**：conda 环境路径 `D:\Anaconda_envs\envs\langchain_xm`。
  - ⚠️ 终端里 `conda activate` 会报 "Run 'conda init'"，**直接用完整路径调用**：
  ```
  cd backend/Django_xm
  D:\Anaconda_envs\envs\langchain_xm\python.exe manage.py test Django_xm.apps --settings=Django_xm.settings.test --verbosity=1
  D:\Anaconda_envs\envs\langchain_xm\python.exe manage.py check --settings=Django_xm.settings.test
  ```
- **前端**：`frontend/langchain-vue`（注意目录层级，不是 frontend/src），Node v20.19.5。
  ```
  cd frontend/langchain-vue
  npx vitest run        # 127 用例
  npm run build
  npm run lint          # oxlint + eslint，均需 0 错误 0 警告
  ```
- **数据库**：Docker 容器 PostgreSQL。测试走 `settings/test.py`（自动建 `test_<DB_NAME>` 临时库，继承 base.py 的 PG 配置，LocMemCache + InMemoryChannelLayer，Celery EAGER）。**禁止用 sqlite/内存库测试**（用户明确要求 PG 贴合实际）。
- **基线数字**：后端 378/378 测试通过（339 基线 + C4 批新增 39 个 stream_persistence 单测；命令需含 `Django_xm.apps Django_xm.common Django_xm.services.fastapi_service` 三个模块，否则漏计）；前端 **188/188**（127 基线 + C5 批新增 61 个 sync handler 单测）；build 成功；lint 零告警；**依赖契约 `lint-imports` 三契约全绿零豁免**（backend/Django_xm 目录下执行，配置 `.importlinter`：common↛apps、ai_engine↛agent_hub、apps↛services 三契约均无 ignore_imports，新增违规直接红）。任何改动后必须回到这个基线。

## 三、项目背景（一句话）

LangChain 智能体平台：Django 5.2 + DRF（后端 `backend/Django_xm`，apps 模块化）+ Vue3 + Element Plus + Tailwind（前端 `frontend/langchain-vue`）；Celery + Redis 双通道实时同步；核心模块：chat（含 agent/深度研究模式）、deep research、learning workflow、knowledge/RAG、tools（含 MCP）、approvals（工具审批）。

前期通过 ontology 评估生成《项目质量分析报告》（总分 7.1/10，80 条发现：P0×1、P1×16、P2×34、P3×29），之后按 A/B/C 三类分级逐批整改。

## 四、用户红线约束（每次动手前默念）

1. **拒绝任何补丁修复/兜底策略，必须根因彻底解决**。
2. **严格依据 `.trae/rules/SpecificationRequirements.md`**，不合规代码直接改，不搞别名映射。
3. **死代码坚决删除**（用户原话：僵尸表也直接删，代码部分也是）。
4. **允许大范围重构**，前后端调用点 100% 适配，**不兼容旧代码**。
5. **测试用 PostgreSQL**。
6. **拿不稳的先讨论**，不瞎猜。
7. **每个阶段新建 spec**（`.trae/specs/<名称>/spec.md` + `tasks.md`），完成后提醒用户并说明做法。
8. **lc-01 P0（shell_exec 白名单前缀绕过）用户明确保留不动**（有审批提示，用户自行确认）。
9. 实时同步是统一底层能力：有问题则所有模块都有问题，修一次全部生效，禁止按模块打补丁。

## 五、已完成工作（按时间线，全部有 spec 存档）

### 1. assess-project-quality（质量评估，只读）
80 条发现 + 评分 7.1/10，产出报告即后续整改依据。

### 2. normalize-tools-rest-fix-chat-api（三项修复）
- **dj-05 缓存翻页 bug**：`chat/views_chat.py` 缓存命中分支 page=1 硬编码删除，复用 DB 分支的 `Paginator.get_page`。
- **dj-04 N+1 查询**：`chat/serializers.py` 新增 `build_research_task_map` 批量查询，12 处实例化点改造，50 条消息 100 次→1 次查询。
- **dj-06 tools REST 资源化**：18 条资源路由重构 urls.py + 前端 `api/tools.js`，删除 19 个旧动词 View 类，组件层零改动。

### 3. analyze-context-manager-dead-modules + remove-dead-code-batch（死代码清除）
- 删除 context_manager 死模块：lazy_loader / rule_engine / context_lifecycle / hierarchical_memory.py（399行，唯一消费方）。
- 删除僵尸表：ContextRule / AutoMemory / PromptCache 三模型 + Admin 注册 + 迁移 0003（Docker PG 上已执行并确认）。
- 删除 `core/__init__.py` 的 `__getattr__` 转发层、mcp_integration.py 整文件（lc-05，136行）、PromptCacheSerializer、前端 validation.js 6 个死 schema、format.js 死函数、realtimeEvents.js 死 EventSource 枚举、多处死配置（cross_session_max_context_length 等）。
- rd-03：`core/config.py` 旧 get_logger 实现体删除，57 个调用文件 import 全迁 `logging_utils`，未留转发。
- rd-04（删除部分）：usage_tracker.py 本地 MODEL_LIMITS 表删除，`get_max_tokens` 改调 `TokenEstimator.get_model_limit`。
- 净删除约 900+ 行。

### 4. add-regression-test-infra（A类打包）
- cq-01 前端 vitest 接入（`vitest.config.js` + `utils/__tests__/`）。
- cq-02 后端补关键链路测试（tools REST、serializer 分页、filesystem、interrupt/resume 等）。
- dj-02 requirements.txt 补 channels / channels-redis / daphne。
- cq-09 三级 README + `.env.example`。

### 5. b1-low-risk-convergence（B类第一波）
- `api/axios.js` 生产环境移除双重 JSON.stringify 调试代码。
- `types/index.js` 删 7 个死工具函数，validateMessage 迁至 `utils/validation.js`。
- `utils/sse.js` 新增 `parseProtocolEvent` 统一 SSE 事件转换（删五处手动转换；type 字段保留 snake_case 协议路由标识符——规范要求）。
- `utils/apiErrorHandler.js` 重写 `extractSSEError`（修 body 二次读取缺陷）。
- 三处自写节流防抖替换为 `@vueuse/core`，删 `useThrottle.js`。
- `handleSessionEvent.js` / `handleTaskEvent.js` 用 `Set.has` 替换 13 个 case 字符串（TOOL_CALL_EVENT_TYPES / APPROVAL_EVENT_TYPES）。

### 6. lc06-strict-rag-convergence（收益最高项）
- `knowledge/services/strict_rag_chain.py` 三在役入口收敛至共享内核（sync/async 双版本内核是必要结构差异），759→613 行。
- 删除死符号：create_strict_rag_chain / aquery_strict_rag / STRICT_RAG_SYSTEM_PROMPT / _get_chat_model 及 4 个失效 import。
- 新增 17 个等价性测试（test_strict_rag_convergence.py）。

### 7. b2-exception-hardening（B类第二批，刚完成）✅
- **dj-03 异常泄漏清理（17 文件 53 处）**：原则「业务语义异常保留（ValueError/FileNotFoundError/WorkflowAlreadyFinishedError/DoesNotExist 等校验文案），系统异常隐藏（通用文案 + logger.exception）」。覆盖 HTTP error_response、SSE error 事件（views_rag / strict_rag_chain / learning_stream / research streams）、WS payload（chat / learning consumers）、裸 Response 规范化（views_rag.py:269）。tools/views_mcp.py 扩散的 7 处 e!s 全清。
- **dj-01 CacheClearView 越权根因修复**：
  - 键错位根因：前端传 `scope` 后端读 `type` → 后端统一读 `scope`（对齐 CacheInvalidateView），非法 scope 400。
  - `get_permissions()` 动态分派：`query`→IsAuthenticated（仅清 `rag_query:user_{id}_*`），`all/model/pattern`→IsAdminUser。
  - 登录响应（UserInfoSerializer / MyTokenObtainPairSerializer）补 `is_staff`；前端 user store 增 `isAdmin` computed；KnowledgeBaseView「清空全部缓存」按钮 `v-if="userStore.isAdmin"`。
  - 新增 `cache_manager/tests/test_views_clear.py` 10 用例（权限矩阵+通用文案）。
- **dj-20 dev.py Redis 启动探测删除**：删 ping + REDIS_AVAILABLE（零残留），CHANNEL_LAYERS 固定 RedisChannelLayer，**连 InMemoryChannelLayer 静默降级分支一并删**（该降级致多进程 WS 互不可见），靠 channels_redis 连接池惰性建连+自动重连。
- 终验：后端 220/220（PG 测试库）、check 零问题、前端 111/111、build 成功、lint 零告警；质量报告 dj-01/dj-03/dj-20 及风险恶化点①②④已标 ✅。

### 8. c1-lightweight-convergence（C类第一批，2026-08-20）✅
- **rd-02 收口**：anti_oscillation.py 直接删除（全仓零引用 + 无痛点驱动，推翻早期「值得集成」结论——那是功能考虑非引用激活），rd-02 至此全部关闭。
- **dj-14**：6 处 get_absolute_url 全删（DocumentIndex/ChatSession 等，参数名错误 + 无消费方，按拍板不修复保留）。
- **cq-07 双异常体系统一**：新建 `core/exceptions.py` `BaseAppError` 基类，LCAgentException / AgentHubError 双体系继承；knowledge 新异常同步继承；classify_exception 两处 ImportError 降级补 logger.debug；双捕获点盘点为零存在。
- **dj-10 知识库校验规范化**：新建 `knowledge/serializers.py`（Create/Update/Search 三 Serializer 标准 is_valid）；服务层改抛 `KnowledgeBaseAlreadyExistsError` 类型化异常，视图按异常类型分支 409，`pop("existing")` 魔法消费消除（改 `result.get("existing")` 透传）。
- **dj-07 知识库路由收敛**：/indices/ 12 条旧路由 + upload 动词路由全删，收敛为 /knowledge-bases/ 单一资源体系；前端 api/knowledge.js 100% 适配，旧路径零残留；新增旧路径 404 断言。
- 终验：后端 297/297 + check 0 issues；前端 vitest 127/127 + build + lint 零告警；质量报告 5 处标 ✅。

### 9. c2-model-migration-consistency（C类第二批 / C7 第①批，2026-08-20）✅
- **dj-12 冗余索引**：删 IndexMetadata.idx_name（unique 已建索引）、UserEvent created_at 单列 + 3 个最左前缀字段索引、DailyAggregation date + (user,-date) 索引；迁移 knowledge.0004 + analytics.0004（3 RemoveIndex + 4 AlterField）已应用；base_models 基类索引经评估非重复项保留。
- **dj-17 信号注册根因修复**：core/signals.py 删 try/except + get_model 吞错块，celery_task_record_post_save 改字符串 sender；CoreConfig.ready() 显式 import core.signals 激活（消除靠其他 app import 副作用激活的隐性依赖）。
- **dj-21 squashmigrations**：chat 20→1、approvals 13→1、learning 10→1（删 43 旧文件，replaces 保留）；内联 6 个 RunPython 函数修复 Django 生成的数字模块非法引用；**额外根因修复**：approvals 5 个 RenameIndex 与 PG deferred CREATE INDEX 时序冲突，折叠进 CreateModel 终态索引名（新旧库终态等价）；存量库 migrate "No migrations to apply"（无需重置）。
- **dj-22 choices 全仓 TextChoices 化**：attachments 3 处（CleanupAction/AlertLevel/AlertStatus）+ tools 3 处（ToolStatus/Transport/Mode）+ **approvals 3 处（Approval.Source/State、ApprovalOutboxEntry.State，含全仓 18 文件 175 处调用点 100% 机械替换**，覆盖审批状态机主链路）；零迁移零数据影响。
- 终验：后端 297/297（测试库全新建库走 squashed 全链路）+ check + makemigrations --check 三绿。

### 10. c7b2-pagination-route-normalization（C7 第②批，2026-08-20）✅
- **dj-15 分页统一**：新建 `common/pagination.py` ProjectPagination（items/total/page/page_size/total_pages 统一结构）+ paginate_to_dict 辅助；全局 DEFAULT_PAGINATION_CLASS 改指它（配置形同虚设根因消除）；ApprovalListView 迁 generics.ListAPIView + django-filter 四字段 + 真分页删 [:100] 硬截断（含 filter_backends=[DjangoFilterBackend] 显式声明——全局 backends 不含它，不声明则过滤静默失效）；chat DB+缓存两分支与 knowledge 两列表收敛到同一分页器（手写实现零残留）；前端 getApprovalHistory 死函数删除（camelCase query 违规 + 零消费）。
- **dj-16 路由规范化**：research 删 result/ 重复端点、task/ → tasks/ 复数化（name tasks_*）；chat messages/<id>/delete/ → messages/<id>/（与 PATCH update 同径合并为 ChatMessageDeleteView(ChatMessageUpdateView) 单视图承载 patch+delete，对齐 tools dj-06 惯例）、pair/delete/ → pair/<int:user_message_id>/（DELETE body 改路径参数）；name 统一 chat_messages_*；前端 4 处调用点 100% 适配 + 旧路径 404 防回归断言。
- **回归中发现并根因修复**：paginate_to_dict 首版用 max(int(page),1) 预归一化破坏 Django get_page 原生语义（page<1 应回最后一页，dj-05 测试锁定），删除预归一化直接透传 get_page（单一权威）。
- **新发现**：learning/urls.py 存在同款 path("task/...") 单数资源路由（前端 workflow.js deleteTask 调 /learning/task/），同病待后续批次。
- 终验：后端 318/318（基线 297 + 新增 21）+ check + makemigrations --check 三绿；前端 vitest 127/127 + build + lint 全绿。

### 11. c2-requirements-rewrite（C2 依赖清单重写，2026-08-20）✅
- **rd-01 + dj-02 + rd-12 联动**：requirements.txt 由 219 行（freeze 混直接依赖）重写为 **56 条直接依赖**，判定标准四类证据（模块顶层 import / 配置引用 / 命名空间隐蔽 import / 已装降级路径）。
- **清除约 162 条**：spacy 全家桶 16、jupyter/ipython 全家桶 22、开发工具（pipreqs/flower/eventlet/simpleui/tenacity）、langchain-anthropic 零 import、langchain-milvus 当前未启用、全部传递依赖固化不保留（由 pip 自动解析）。
- **补缺漏声明**：fastapi/uvicorn/python-docx/duckduckgo-search/groq/chromadb/langchain-ollama/langchain-unstructured 等。
- **新环境（Python 3.11.15 干净 venv）安装回归**：pip check 无冲突 + check 0 issues + **318 测试全绿** + fastapi/run_fastapi 导入 + 五向量后端注册冒烟通过；验证后 venv 已删。
- **三处版本修正**：langgraph-store-postgres 移除（Python 3.11 无兼容版本，命名空间由 langgraph-checkpoint-postgres ≥3.0 同包提供）；redis 上界 <8（redis-py 8 默认 RESP3 HELLO 命令与 Redis 5 不兼容）；deepagents 下限校准 >=0.7.5（原 0.1.0 允许装到无 0.7.x API 的早期版本，用户复核发现）。
- 验证报告（56 条逐条证据 + 162 条清除映射 + 验证输出）：`.trae/specs/c2-requirements-rewrite/verification-report.md`。

### 12. c7b3-data-model-consolidation（C7 第③批数据模型收敛，2026-08-20）✅
- **dj-18 ChatMessage JSONField 去 null**：7 个 JSONField + ChatSession.selected_knowledge_bases 去 `null=True`（default=dict/list 生效）；chat/0002 迁移 NULL 归一 RunPython 先于 AlterField；18 处双轨读点 `or []`/`or {}` 死防御清除（views_chat/realtime_events/writeback/approval_service/stream_persistence/research views/session_executor；`reasoning or None` 显示转换保留）；新增 4 测试。
- **dj-13 双索引模型合并（彻底删除 DocumentIndex）**：IndexMetadata 继承 BaseModel 成唯一实体（软删墓碑 + objects 默认过滤 + all_objects）；Document.index FK 改指 IndexMetadata（非空，related_name=documents）；文件数改 Document 表派生查询（废弃 document_count 存储计数器与计数信号，消除漂移）；显示语义修正 document_count=文件数/chunk_count=块数（前端 KnowledgeBaseView「文档数」+ RagView 7 处「分段」全量适配）；create 三分支（软删恢复/活跃 existing/向量残留异常）+ IndexManager.delete_index 不硬删元数据；signals/admin/serializers/analytics/cross_app/learning 调用点 100% 适配 + chat_service 传 user_id 而非 User 实例的静默降级根因修复；新增 test_kb_validation 11 项 + test_kb_lifecycle 4 项。
- **迁移拆三段（关键根因修复）**：knowledge/0005(纯 DDL)/0006(纯 DML 回填)/0007(FK 切换+删模型)。根因：AddField(db_index=True) 会把 CREATE INDEX 放入 schema_editor 的 deferred_sql（迁移事务结束时执行），与同事务 RunPython DML 触发的 DEFERRABLE FK 延迟检查事件冲突 → PG "pending trigger events"。测试库因空数据无 DML 而漏测，存量库必炸；DDL 与 DML 分迁移（各自独立事务）彻底解决。
- 终验：后端 326/326 + check + makemigrations --check 三绿；前端 vitest 127/127；**存量库 migrate 冒烟通过**（0005/0006/0007 全 OK + 数据完整性验证：9 索引实体合并/29 文档全链接/DocumentIndex 表已删/临时列零残留）。

### 13. learning-route-convergence（learning 路由收敛 + knowledge 死代码清理，2026-08-20）✅
- **dj-16 learning 延伸收口**：`learning/urls.py` 的 `task/<thread_id>/` → `tasks/<thread_id>/`、name `delete` → `tasks_delete`（对齐 research 已建立模式）；`WorkflowDeleteView` → `WorkflowTaskDeleteView`；删除 `# 新增API` 注释残留；前端 `workflow.js` deleteTask 1 处调用点适配，全仓 `/learning/task/` 消费零残留；新增 `learning/tests/test_route_normalization.py` 4 项防回归（旧路由 404 / 新 name 可 reverse / 旧 name NoReverseMatch / tasks 列表防误删）；schema.yml 重新生成，旧路由零残留。
- **顺带根因修复（views_kb.py get_parsers 时序 bug）**：`get_parsers` 覆写依赖 `self.request.method`，但 DRF `initialize_request` 先调 `get_parsers()` 后赋值 `self.request`（mock request / drf-spectacular 场景必崩）——改类级 `parser_classes = [MultiPartParser, FormParser, JSONParser]`（GET 无请求体不受影响）。此前 schema.yml 因此 bug 无法重新生成。
- **knowledge 死代码清理**：`IndexInfoSerializer`（全仓零消费方）删除；`knowledge/views.py` 重导出垫片删除（urls.py 改直接导入 `views_kb`/`views_rag`；`views_utils` 工具函数消费方 views_rag/kb_service/learning retrieval_node 均直接导入，不经垫片）。
- 终验：后端 330/330（326 基线 + 4 新增）+ check + makemigrations --check 三绿；前端 vitest 127/127 + build ✓。
- **环境异常记录**：实施中遭遇编辑器缓存与磁盘内容脱节（Edit 报告成功但磁盘未变、Read 显示新内容但 Python 读旧代码），views_kb.py 修复经 Python 脚本直接写盘 + 命令行复核持久性解决；后续所有关键编辑建议用 Select-String 验证磁盘真实状态。

### 14. c3b-dependency-debt-cleanup（C3 第②批依赖方向债务清偿，2026-08-20）✅
- **审批三件套迁移**：`common/approval_lifecycle.py`、`common/approval_gateway.py`、`common/approval_batch.py` 整体迁移 `apps/approvals/services/`（common 下三文件删除，模块语义与单例不变）；approval_service 4 个私有函数公共化（`clean_extra_temp_keys`/`persist_and_broadcast`/`publish_tool_call_timeout_event`/`release_lock`，同步删除 `_clean_extra_temp_keys_async`/`_release_lock_async` 死代码）；9 处调用方 import + 2 个测试文件 mock 路径（test_approval_state_machine/test_approval_batch_resume patch.object 公共名、test_subagent_wait mock 路径）全量适配，全仓旧路径零残留。
- **幽灵会话过滤治理（依赖反转）**：`_filter_ghost_session_created` 自 common/realtime_events.py 迁 `apps/chat/services/session_history.py` 公共化（依赖 ChatSession 模型，归位 chat 业务层）；common 侧新增 `set_user_history_filter(fn)` 注册点，chat app `ready()` 注入；`_group_name`→`group_name` 公共化（consumers 4 处同步）；新增 test_session_history.py 9 用例（幽灵过滤/DB 覆盖/DB 异常原样/注册接线/频道隔离/未注册独立可用）。
- **persist_chat_tool_calls 归位**：自 services/fastapi_service/chat_executor_core.py 迁 `apps/chat/services/stream_persistence.py` 公共化（DB 持久化逻辑归业务层）；chat_executor_core 3 处调用 + chat_service `_flush_subagent_snapshot` 1 处改同 app 导入，**apps→services 反向依赖违规清零**。
- **跨模块私有成员公共化**：`_merge_tool_calls_incremental`→`merge_tool_calls_incremental`（stream_persistence 定义 + writeback 2 处延迟导入 + 测试引用）、`_publish_stream_event`→`publish_stream_event`（sse_generator 定义 + chat_executor_core import/调用），旧名全仓零残留（含注释）。
- **契约豁免清零**：`.importlinter` 删除 common↛apps 6 条 + apps↛services 1 条共 7 条 ignore_imports 及债务注释；`lint-imports` 输出 **3 kept, 0 broken**（零豁免，新增违规直接红）。
- 终验：后端 **339/339**（330 基线 + 9 新增）+ check 0 issues + makemigrations --check No changes + lint-imports 3 kept 0 broken；前端 vitest 127/127。**ds-02 至此收口**（lc-09/ds-11/ds-15 跨 app 循环依赖属长期演进，契约已固化防止恶化）。

## 六、未完成任务（原拍板方案；已完成项见第五节 8-14，剩余项如下）

### C类专项（剩余待执行）
- ~~**rd-01**：requirements **以实际 import 为准重写 + 新环境（干净 venv）安装回归验证**~~ **[✅已完成 2026-08-20 · spec:c2-requirements-rewrite，含 dj-02/rd-12 联动，见第五节 11]**
- ~~**ds-02**：**三步全做**——① `_merge_tool_calls_incremental`/`_keyword_overlap` 等跨 app 私有成员公共化；② 依赖方向规则重构（langgraph_adapter 反向调用改回调注入等）；③ import-linter CI 契约固化。~~ **[✅已完成 2026-08-20 · 第①批 spec:c3a-dependency-direction-core + 第②批 spec:c3b-dependency-debt-cleanup（豁免清零），见第五节 14；ds-02 收口]**
- **cq-03**：后端长函数**逐链路拆**（chat_service 4 个超百行函数、_persist_to_db_sync 163 行；每步独立可回归）。
- **cq-04**：session.js（1731行）**按领域拆多 store** + handleSessionEvent.js（670行）**case 下沉 handler 模块**。
- **cq-05**：巨型组件**优先拆 4 个千行级**（DeepResearchView 1377 / WorkflowView 1164 / ToolSelector 1114 / ChatMessage 1055；轮询抽 usePolling、对话框抽子组件）。
- ~~**dj-12 系列**：**按影响拆 2-3 个 spec**——①无行为变化（冗余索引/信号注册/TextChoices/squash）~~ **[✅第①批 2026-08-20 · spec:c2-model-migration-consistency]** ~~②API 变化需前端适配（分页 dj-15/路由命名 dj-16）~~ **[✅第②批 2026-08-20 · spec:c7b2-pagination-route-normalization + learning 同病延伸已收口 · spec:learning-route-convergence，见第五节 13]** ~~③数据迁移 + 大重构（JSONField null dj-18、双索引模型合并 dj-13）~~ **[✅第③批已完成 2026-08-20 · spec:c7b3-data-model-consolidation，见第五节 12；C7 至此全部收口]**
- ~~**rd-02 剩余**：anti_oscillation.py **直接删除**（全仓零引用死代码，无痛点驱动接线）~~ **[✅已执行 2026-08-20 · spec:c1-lightweight-convergence]**
- ~~**dj-14**：get_absolute_url **删除**（参数名错误 + 无消费方）~~ **[✅已执行 2026-08-20 · spec:c1-lightweight-convergence]**
- ~~**新增纳入（原未入清单的 P2 项，用户已勾选）**：dj-07 知识库路由双轨收敛（/indices/ 与 /knowledge-bases/ 收敛单一资源体系，前端适配）、dj-10 知识库校验规范化（标准 is_valid + 带码异常）、cq-07 双异常体系统一（LCAgentException/AgentHubError 共同基类 + ImportError 降级加日志）。~~ **[✅三项均已完成 2026-08-20 · spec:c1-lightweight-convergence]** cq-13 未选、暂不做。
- lc-01 已排除（用户决定保留不动）。

### 数据库重置约定（2026-08-19 用户确认）
- 开发阶段数据可丢，**整库可删除重做**；dj-18/dj-21 等迁移按"可重置"最简方案写。
- 重建种子数据只需：用户 `daixing` / 密码 `daixing241212` + 大模型相关配置（厂商、模型、工具）；对话、任务等业务数据可抛弃。

### 已决策保留项（用户已点头，勿再动）
- `tasks/*.py` 的 `mark_failure(error_message=str(e))` → 任务状态 API 透出异常原文——**用户决策（2026-08-19）：保留不动**。项目处于开发阶段未到发布阶段，排障信息价值大于泄漏风险；发布阶段前可再评估。
- `core/services/db_monitor.py:118/:140`、`tools/mcp/__init__.py:691` 健康检查类 str(e)（db_monitor 两处普通用户可见、mcp 一处无调用方）——**用户决策（2026-08-19）：全部保留不动**，开发阶段排障优先；发布阶段前再统一收紧。

> 以上两类均不属于 dj-03 整改范围，后续做 str(e) 扫描时不要误改。

## 七、关键文件索引

| 用途 | 路径 |
|------|------|
| 质量报告（权威整改台账） | `项目质量分析报告.md`（120KB，分段读） |
| 本次任务总纲 | `一开始的任务提示词.md`（已优化，含完成标记） |
| 命名规范 | `.trae/rules/SpecificationRequirements.md` |
| 全部 spec 存档 | `.trae/specs/<名称>/spec.md + tasks.md` |
| 后端入口 | `backend/Django_xm/manage.py` |
| 前端项目 | `frontend/langchain-vue/` |
| 测试设置 | `backend/Django_xm/Django_xm/settings/test.py` |
| 问题记录（用户测试用） | `问题.txt`（用户手工测试时记录） |

## 八、工作节奏提醒

- 用户会用多浏览器实测（实时同步、审批流、工具调用），测试发现的问题记 `问题.txt`，**先测完再统一改**。
- 每完成一个 spec：更新质量报告对应条目为 `[✅已修复 日期 · spec:名称]` 格式 → 跑全量回归（后端+前端+build+lint）→ 向用户汇报「做了什么+怎么做的+验证证据」。
- **遇到待决策项（含所有「建议」类结论）：必须先向用户讲清楚「是什么问题 / 在哪 / 泄漏或影响什么 / 暴露面（谁能看到）/ 选项与各自影响」，再让用户拍板；用户点头后才能标「已决策」；不得替用户默默归类**（2026-08-19 用户明确要求，并确认此提问格式：问题表格 + 暴露面 + 理由 + 选项）。
- 改动未 commit（我同意commit）。
