### 核心结论
如果彻底脱离 Celery、追求对齐 Claude Code / Trae IDE 的成熟范式，**行业标准的最优解是「会话级单执行上下文 + 事件驱动架构」**——本质是回归 Agent 任务「单流、有状态、频繁中断、实时交互」的原生属性，从根源上抹除任务队列带来的所有并发复杂度。

Claude Code、Trae IDE、Cursor、Devin 等主流产品的 Agent 执行层，全部遵循这套设计范式，没有任何一款是基于 Celery 这类短任务队列实现的。它不是“换一个中间件”，而是换掉了整个错误的调度模型，让架构和业务天然匹配。

---

## 一、先对齐：成熟产品的底层设计范式
Claude Code、Trae IDE 这类产品的 Agent 执行层，核心是三条铁律，也是你们当前架构最核心的偏差：
1.  **一会话一执行流**：一个深度研究会话，全程对应唯一的执行上下文（协程/线程），从启动到结束不拆分。不存在“执行任务”“恢复任务”的概念，自然没有锁、抢锁、接力、死锁。
2.  **中断是原生暂停，不是任务退出**：遇到审批、子代理等待时，执行流主动挂起（`await`），内存上下文完整保留；用户操作后直接唤醒，零延迟恢复，没有状态加载、跨任务传递的开销与一致性问题。
3.  **事件驱动全链路**：用户操作（审批/终止/追加指令）、工具输出、子代理状态，全部通过实时事件通道双向流动，没有轮询、没有队列调度延迟。

你们之前遇到的所有问题——死锁、恢复失败、状态不一致、刷新丢失子代理标识、前端状态滞后——本质都是「用短任务队列的模型，硬套长驻有状态的 Agent 场景」带来的次生复杂度。换对模型后，这些问题的产生前提直接消失。

---

## 二、完整架构设计（彻底脱离 Celery）
### 整体架构分层
```
┌───────────────────────────────────────────────────────────────────┐
│                        接入层（Gateway）                          │
│  负责鉴权、会话路由、SSE/WebSocket 长连接维护、消息透传            │
└─────────────────────┬─────────────────────────────────────────────┘
                      │  实时事件流
┌─────────────────────▼─────────────────────────────────────────────┐
│                      Agent 执行服务（核心）                       │
│  ┌─────────────────────────────────────────────────────────────┐  │
│  │  会话管理器 SessionManager                                  │  │
│  │  会话生命周期管理、并发限流、异常重启、优雅停机、资源调度    │  │
│  └─────────────┬───────────────────────────────────────────────┘  │
│                │  创建/销毁
│  ┌─────────────▼──────────┐  ┌─────────────┐  ┌─────────────┐   │
│  │ 会话执行器 Session1     │  │ 会话执行器 2 │  │ 会话执行器 N │   │
│  │  · 单协程全生命周期     │  │             │  │             │   │
│  │  · LangGraph 运行时     │  │             │  │             │   │
│  │  · 中断控制器           │  │             │  │             │   │
│  │  · 子代理执行栈         │  │             │  │             │   │
│  │  · 事件发射器           │  │             │  │             │   │
│  └─────────────┬──────────┘  └─────────────┘  └─────────────┘   │
│                │  内部事件订阅/发布                               │
│  ┌─────────────▼──────────┐                                   │
│  │  内部事件总线           │◄────── Redis Pub/Sub 外部信令     │
│  │  (asyncio 本地事件)     │        （审批/终止/指令）         │
│  └────────────────────────┘                                   │
└───────────────────────────────────────────────────────────────────┘
                      │  异步持久化
┌─────────────────────▼─────────────────────────────────────────────┐
│                        持久化层                                   │
│  · Checkpoint 存储：LangGraph 状态快照（Redis/DB）               │
│  · 消息/事件落库：聊天记录、工具调用、审批记录（MySQL/PG）       │
│  · 会话元数据：状态、进度、配置（DB）                             │
└───────────────────────────────────────────────────────────────────┘
```

### 各层核心职责拆解
#### 1. 接入层：网关 + 长连接
- 维持前端的 SSE / WebSocket 长连接，负责鉴权、会话路由、心跳保活。
- 不执行业务逻辑，只做消息透传：前端指令转发给执行服务，执行服务的事件推送给前端。
- **价值**：把连接管理和业务执行解耦，执行服务重启不影响前端连接，用户无感知。

#### 2. 核心执行层：会话执行器（最关键）
一个深度研究会话对应一个 `SessionExecutor` 实例，全程单协程运行，是整个架构的灵魂。
**核心内部结构**：
- **LangGraph 运行时**：Agent 逻辑的实际载体，全程在同一个协程内运行，不退出、不拆分。
- **中断控制器**：基于 `asyncio.Event` 实现原生等待，替代原来的 Celery 任务接力。
  - 遇到审批中断时，调用 `event.wait()` 挂起协程，内存上下文完整保留；
  - 收到审批信令后，校验决策完整性，调用 `event.set()` 唤醒，立刻继续执行。
- **子代理执行栈**：维护树形的 Agent 调用层级，天然携带 `depth / agent_name / agent_path`，所有事件自动附带层级字段。
- **事件发射器**：所有状态变更、工具输出、审批通知，统一从这里发出，推送和落库共用同一个事件源。

**伪代码示意（核心逻辑）**：
```python
class SessionExecutor:
    def __init__(self, session_id: str):
        self.session_id = session_id
        self._interrupt_event = asyncio.Event()
        self._pending_approvals = {}
        self._agent_stack = []  # 子代理层级栈

    async def run(self, initial_input):
        """单协程全生命周期运行，全程不退出"""
        async for event in self.graph.stream(initial_input):
            if event.type == "interrupt":
                # 进入审批等待：原生协程挂起，零资源占用
                self._pending_approvals = event.approvals
                self._emit_approval_event(event.approvals)
                await self._wait_for_approvals()  # 原生 await 暂停
                # 唤醒后注入决策，继续执行
                self.graph.resume(self._decisions)
            elif event.type == "tool_call":
                self._emit_tool_event(event)
            # ... 其他事件处理

    async def _wait_for_approvals(self):
        """等待审批事件唤醒，支持超时、终止"""
        self._interrupt_event.clear()
        await self._interrupt_event.wait()

    def on_approval_received(self, approval_id: str, decision: bool):
        """审批信令到达，直接唤醒（事件驱动）"""
        self._pending_approvals[approval_id].decision = decision
        if all(a.decision is not None for a in self._pending_approvals.values()):
            self._decisions = {a.id: a.decision for a in self._pending_approvals.values()}
            self._interrupt_event.set()  # 毫秒级唤醒
```

#### 3. 会话管理器：替代 Celery 的调度能力
自己实现轻量的会话调度，只做核心几件事，逻辑极简：
- **生命周期管理**：接收启动请求，创建 `SessionExecutor` 并启动协程；正常结束/异常终止时回收资源。
- **并发管控**：配置最大并发数，超出的任务排队等待，避免服务过载；支持按用户、按任务类型限流。
- **异常自愈**：协程意外崩溃时，自动从最新 Checkpoint 拉起，恢复执行，用户无感知。
- **优雅停机**：服务发布/重启前，持久化所有运行中会话的 Checkpoint，标记为「待恢复」；启动后自动批量恢复。
- **信令路由**：接收 Redis 发来的外部指令（审批/终止/追加），路由到对应会话执行器。

#### 4. 事件层：双轨事件总线
- **内部事件**：同进程内的会话用 `asyncio.Event` / 本地事件总线通信，延迟最低。
- **外部信令**：跨进程/跨节点的指令（审批、终止）通过 Redis Pub/Sub 传递，轻量、实时、解耦。
- **统一事件格式**：所有事件自带会话ID、子代理层级、时间戳，推送前端和异步落库用同一份数据，彻底解决「刷新后标识丢失」「前后端状态不一致」。

#### 5. 持久化层：内存为主，持久化为辅
- **运行态全在内存**：正常执行时状态全部在协程内存中，读写零开销，一致性天然保证。
- **Checkpoint 异步落盘**：仅在进入中断、协程退出前、定时快照三个时机，把 LangGraph 状态持久化到 Redis/数据库，用于崩溃恢复。
- **事件异步落库**：所有前端展示的事件，通过异步队列批量写入数据库，不阻塞主执行流。

---

## 三、针对你所有痛点的根治效果
| 之前的痛点 | Celery 方案的解法 | 新架构的根治方式 |
| :--- | :--- | :--- |
| 审批死锁、互等 | 加锁、加防护、加接力，越修越复杂 | 单执行流，没有多任务抢锁，死锁从逻辑上不可能存在 |
| 审批后无结果、恢复失败 | 增加重试、接力队列、兜底巡检 | 事件直接唤醒协程，没有队列调度、抢锁失败，100% 可恢复 |
| 恢复延迟高 | 缩短轮询间隔、增加接力触发 | 原生事件唤醒，毫秒级响应，用户点击后立刻看到执行 |
| 状态不一致、刷新丢标识 | 补字段、补推送、补同步逻辑 | 单事件源，推送和落库同一份数据，天然一致，永不丢失 |
| 子代理无法分组展示 | 前端硬凑层级、后端补字段 | 执行栈天然树形结构，全链路携带层级字段，前端直接渲染 |
| 任务状态不实时 | SSE 轮询、前端定时刷新 | 状态变更实时推送，零延迟，无需轮询 |
| 资源效率低 | 加 Worker、扩队列 | 挂起协程几乎零资源占用，同等资源并发量提升 10~20 倍 |
| 排障困难 | 跨多个 Celery 任务串日志 | 单会话单执行流，日志线性连贯，链路追踪零成本 |

---

## 四、对齐 Claude Code / Trae IDE 的高级特性
这套架构不仅解决当前问题，还能低成本实现成熟产品的体验：
1.  **中途干预能力**：支持终止、暂停、追加用户指令、修改配置，全部通过事件通道送达，执行器即时响应。
2.  **断点续跑**：服务发布、服务器重启后，自动从 Checkpoint 恢复，用户完全无感知，和 Claude Code 的续跑体验一致。
3.  **子代理树形展示**：天然的执行栈层级，前端可以直接渲染成折叠树、步骤流，对齐 Trae IDE 的子任务可视化。
4.  **流式思考过程**：Agent 的思考、规划、工具调用全程流式输出，前端实时渲染，不用等整段完成。
5.  **审批超时自动处理**：中断控制器内置超时逻辑，支持超时自动拒绝、自动通过，无需外部调度。

---

## 五、分阶段落地路径（不在乎工作量，但求稳）
因为是核心架构替换，建议分四阶段落地，全程可灰度、可回滚，不影响线上业务：

### 阶段一：核心执行层开发（2~3周）
- 实现 `SessionExecutor` 核心：单协程运行 LangGraph、原生中断等待、事件发射
- 实现基础的 `SessionManager`：会话启动、销毁、异常重启
- 接入 Redis Pub/Sub 信令：审批唤醒、终止指令
- 实现 Checkpoint 持久化与恢复
- **目标**：跑通完整的深度研究主流程，功能对齐旧版，内部测试验证。

### 阶段二：网关与体验补齐（1~2周）
- 接入网关层，统一 SSE / WebSocket 事件推送
- 完善事件规范，子代理层级全链路落地
- 实现优雅停机、批量恢复、并发限流
- 补齐监控、告警、全链路日志
- **目标**：体验全面超越旧版，状态实时、子代理分组可用。

### 阶段三：灰度与全量切换（1周）
- 网关层做流量灰度，按用户/比例切流到新架构
- 并行运行一段时间，对比稳定性、性能、体验
- 验证稳定后全量切换，下线旧 Celery 深度研究任务
- **目标**：平滑上线，用户无感知。

### 阶段四：高级能力迭代（持续）
- 中途追加指令、暂停/续跑
- 审批超时、自动处理策略
- 多 Agent 并行调度、任务优先级
- 跨版本断点续跑

---

## 六、备选轻量方案：LangGraph Server
如果不想从零自研执行层，**LangGraph 官方的 LangGraph Server** 是最贴合技术栈的现成方案，本质就是官方实现的上述架构：
- 原生支持 interrupt 中断/恢复、Checkpoint 持久化、流式输出
- 内置线程/协程隔离、会话管理、并发控制
- 提供标准 API，审批、终止、追加指令都有对应接口
- 完全脱离 Celery，本身就是长驻执行服务

**优势**：不用自己造执行层轮子，官方维护，稳定性有保障，开发量大幅减少。
**不足**：定制化能力不如自研，子代理层级、事件格式、特殊业务逻辑需要二次扩展。

如果追求“又好又省工作量”，LangGraph Server 是极佳选择；如果追求极致定制化、长期完全自主可控，就自研上述架构。

---

### 最终总结
如果目标是彻底做对、对齐行业顶级产品、长期没有技术债务，**自研「会话级单执行流 + 事件驱动」架构是最优解**——它不是修复某个 bug，而是直接换掉了产生 bug 的底层模型，所有当前和潜在的并发、状态、延迟问题都会一次性消失。

如果想减少重复造轮子的工作量，核心执行层直接用 LangGraph Server，外围做业务封装，也是非常成熟合理的选择，同样能彻底脱离 Celery，解决所有问题。