## 一、业界真实做法：没有 “绝对禁止”，只有 “分级管控 \+ 边界兜底”

### 1\. Claude Code：四层权限管线 \+ Bubble 冒泡 \+ 沙箱兜底

Claude Code 的权限系统不是简单的 “放行 / 禁止” 二元开关，而是一条四阶段的判定管线，子 Agent 统一采用 `bubble` 冒泡模式：

1. **输入验证层**：校验工具参数合法性，过滤明显非法输入；

2. **规则匹配层**：通过 `allow/deny` 规则匹配工具名 \+ 参数，支持按子 Agent 角色配置 `disallowedTools`（比如 Explore 子 Agent 天生禁用文件编辑工具），但这是**角色边界约束，不是全局绝对禁止**，用户可通过配置修改；

3. **上下文评估层**：自动分类器评估操作风险，自动放行低风险操作，高风险操作进入人工确认；

4. **交互式确认层**：主终端统一弹出审批，标注风险等级，用户最终决策。

最核心的防绕过机制是**内置沙箱**：所有 Bash 命令和文件操作默认在沙箱内执行，OS 级限制文件访问范围（仅当前项目目录）、网络权限和系统调用。无论 Agent 用什么花式命令绕过正则匹配，最终执行都被锁在沙箱边界内，无法破坏系统或访问敏感文件。

### 2\. Trae IDE：沙箱隔离 \+ 命令分级 \+ 风险提示

Trae 的安全体系和 Claude Code 高度一致，核心原则是「不禁止，但必受控」：

- 没有绝对禁止的命令，即使是 `rm -rf` 这类高危操作，也不会直接拒绝，而是**高亮风险提示 \+ 默认放入沙箱运行**，用户确认后可执行；

- 沙箱是核心防线：自动限制文件读写范围，越权操作会被系统拦截，用户可选择 “本次放行” 或 “加入白名单”；

- 风险分级：low/medium/high/critical 四级，等级越高弹窗提示越强，自动审批的门槛越高，用户可自定义各等级的默认策略。

## 二、最优架构方案：主网关统一审批 \+ 三级风险分级 \+ 沙箱执行兜底

完全对齐 Claude Code / Trae 的生产级设计，去掉无意义的 “绝对禁止级”，换成「分级审批提示 \+ 沙箱边界兜底」的分层体系。既保留 Agent 完整的工具调用能力，又从机制上解决绕过风险。

### 核心设计原则

- 没有绝对不能用的工具，只有**不同风险等级的审批强度**；

- 子 Agent 不做独立审批，所有权限请求统一冒泡到主 Agent 网关，体验一致、状态一致；

- 正则规则只做风险分级参考，**沙箱才是终极安全边界**，防绕过靠系统隔离，不靠关键词匹配。

---

### 第一层：工具风险三级分级（无绝对禁止）

所有工具按风险程度划分为三级，对应不同的审批策略和执行环境，子 Agent 继承主 Agent 的权限上限，不得超出。

|风险等级|定义|典型工具 / 操作|子 Agent 默认处理|执行环境|
|---|---|---|---|---|
|**安全级（低风险）**|纯只读、无副作用、不会修改任何数据，最坏情况只是浪费算力|网络搜索、文件读取、目录列表、知识库检索、代码静态分析、信息查询类 API|✅ 自动放行，直接执行，不打扰主 Agent，仅最终结果转发展示|普通环境|
|**受控级（中风险）**|有修改行为，但影响范围可控、可回滚，业务中高频使用|文件写入 / 编辑、普通文件删除、依赖安装、代码格式化、项目内配置修改|⏸️ 挂起执行，冒泡到主 Agent 审批网关；普通样式弹窗，用户批准后执行|普通环境|
|**高危级（高风险）**|破坏性强、不可逆、涉及系统级权限、可能影响项目外环境|递归删除、系统命令执行、Shell 脚本运行、跨目录文件操作、外部系统写接口、环境变量修改|⏸️ 强制挂起，冒泡到主 Agent 审批网关；**红名高亮 \+ 风险详情 \+ 操作后果提示**；用户批准后，**强制放入沙箱执行**|沙箱隔离环境|

> 关键说明：没有任何工具被永久禁止。即使是最高危的操作，只要用户确认，就可以在沙箱内执行；规则只负责 “分级提醒”，不替用户做 “能不能做” 的决定。
> 
> 

---

### 第二层：主 Agent 统一审批网关（核心机制，完全对齐 Bubble 模式）

这是整个架构的核心，和 Claude Code 的 `bubble` 模式、Trae 的主终端审批完全一致，同时完美避开你们项目遇到的嵌套中断底层问题。

#### 完整执行链路

1. **子 Agent 侧：前置钩子 \+ 风险判定**

    - 复用现有 `subagent_patch` 的 `_on_tool_event` 回调，扩展 `pre_tool_execute` 前置钩子；

    - 工具执行前，先调用统一的风险分级器，判定当前工具调用的风险等级；

    - 安全级：直接执行，不打扰主链路；

    - 受控级 / 高危级：挂起当前协程，构造审批请求（含工具名、参数、风险等级、子 Agent 名称、操作影响说明），通过内部通道发送到主 Agent 审批网关。

2. **主 Agent 侧：统一审批网关**

    - 新增 `ApprovalGateway` 单例，作为全系统唯一审批入口，主 Agent 自身调用、所有子 Agent 冒泡请求全部走这里；

    - 网关 100% 复用现有 `ApprovalMiddleware` 的业务能力：创建审批记录、持久化状态、推送 WebSocket 事件、对接前端审批组件、处理用户批准 / 拒绝操作；

    - 高危级请求自动附加风险标签，前端渲染红名高亮、风险后果提示，和 Trae / Claude Code 的体验一致；

    - 审批结果统一回传给子 Agent 的挂起协程。

3. **结果回传 \+ 执行调度**

    - 批准：受控级直接执行；高危级自动调度到沙箱环境执行，执行完成后返回结果；

    - 拒绝：返回拒绝原因，子 Agent 接收后自行调整策略，不会强制终止整个任务。

#### 为什么这是最优

- **彻底避开框架坑**：审批 100% 发生在主 Agent 层，子 Agent 不注入审批中间件，完全不依赖嵌套中断机制，不会出现 “中断被吞、工具偷跑” 的问题；

- **权限不溢出**：子 Agent 权限永远不超过主 Agent，所有决策都在用户可见的主终端完成，不会出现子 Agent 偷偷执行高危操作的情况，对齐 `bubble` 模式的核心设计思想；

- **一套代码全复用**：审批状态机、持久化、实时同步、前端组件全部复用现有主链路成果，之前解决的 “刷新状态不一致”“快照同步” 等问题，子 Agent 天然继承。

---

### 第三层：沙箱执行环境（终极兜底，解决 “绕过” 问题）

这是解决 “硬拦截能被绕过” 的核心 ——**不靠关键词拦，靠系统边界锁**。无论 Agent 用什么方式变通执行命令，最终的系统调用都被沙箱限制，破坏力可控。

#### 沙箱核心能力（对齐 Claude Code / Trae 标准）

1. **文件系统隔离**：仅开放项目目录、临时目录、依赖缓存目录的读写权限，禁止访问系统目录、用户家目录、其他项目目录；所有文件修改可快照回滚；

2. **命令权限降权**：以低权限用户身份执行命令，禁止 sudo、禁止修改系统配置、禁止挂载磁盘；

3. **网络白名单**：可配置允许访问的域名，禁止内网扫描、禁止访问敏感内网服务；

4. **资源限制**：限制 CPU、内存、执行时长，防止恶意进程耗尽系统资源。

#### 落地方式

- 开发环境可直接用 Docker 容器做沙箱，执行高危命令时自动启动一次性容器，执行完成后销毁，环境干净无残留；

- 沙箱对 Agent 透明：Agent 只需要调用工具，不需要关心是不是在沙箱里，由网关统一调度；

- 支持用户选择：用户审批时可以选择 “沙箱执行” 或 “本机执行”，默认高危操作强制沙箱。

---

### 第四层：增强层：前置风险分类器 \+ 全链路审计

参考 Claude Code Auto 模式的设计，进一步平衡安全和效率：

1. **前置风险分类器**：用轻量模型 / 规则引擎，在人工审批前自动评估操作风险，明显安全的操作自动放行，明显异常的操作强化提示，减少人工审批次数；

2. **全链路审计**：所有子 Agent 的工具调用、审批记录、沙箱执行日志，全量关联主会话 ID 存储，支持事后完整溯源；

3. **异常行为熔断**：短时间内大量高危操作请求、频繁尝试越权操作，自动触发熔断，暂停子 Agent 执行并告警。

---

## 三、和你们项目现有资产的对应关系

这套方案不是从零搭建，绝大部分能力可以复用现有成果：

1. **复用 subagent\_patch**：已有工具事件转发能力，扩展前置钩子即可，不用重写事件链路；

2. **复用 ApprovalMiddleware**：核心审批逻辑、数据库模型、状态机、实时同步全部复用，只新增网关入口；

3. **复用统一后端底层**：`enrich_tool_calls_with_approvals` 天然支持，前端审批组件零改造，只需要增加风险等级的样式渲染；

4. **沙箱可渐进落地**：可以先上线分级审批 \+ 冒泡机制，沙箱作为后续增强项逐步落地，不影响主流程。

---

### 最终总结

最好的安全架构，从来不是 “禁止越多越安全”，而是**让合适的操作顺畅执行，让危险的操作被充分提示，让所有操作的边界可控**。

- 去掉无意义的硬禁止，保留 Agent 的完整生产力，对齐业界主流设计；

- 用主网关冒泡审批解决状态一致性和用户体验问题，避开框架底层坑；

- 用沙箱做终极边界兜底，从系统层面解决 “绕过” 问题，而不是靠永远追不上的正则规则。

需要我把这套方案拆解成对应具体代码文件的落地任务清单，或者补充沙箱选型的技术细节吗？

> 目前项目主要在windows环境中，linux系统的也可以考虑怎么区分。
