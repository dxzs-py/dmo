# SubAgentRuntime 技术规格（纯血版｜无兼容、无补丁、无过渡包装）
> 原则：不做薄包装、不做降级兼容、不保留旧入口；直接删除旧通路；只输出最优架构；存量业务必须全部迁移到新抽象层；旧会话、旧调用链路不再保障可用。
> 参考 Trae‑IDE Solo / Claude‑Code Fork‑Subagent；子Agent是完整对等Agent实例；interrupt归属子Agent自身；父Agent绝不阻塞；彻底消除两套子Agent实现。

## 1 背景与问题
系统现存两套完全独立的子Agent实现：
1. 工具通路：`agent_create` / `agent_run` → `AgentFactory.create`
2. DeepAgents内部原生spawn子Agent

两套实现：生命周期、checkpointer装配、interrupt/human‑in‑loop恢复逻辑不一致；`astream`流结束直接判定任务完成，interrupt暂停时协程直接退出无法resume；UI需要两套模型适配；业务代码散落DeepAgents原生API，升级修改成本极高。

本次重构**彻底清除历史实现，不保留任何兼容补丁、过渡包装、降级分支**。旧工具、旧内部spawn调用全部删除，不做向后兼容。

## 2 核心目标（硬性约束）
1. **唯一入口**：全系统所有子Agent，无论来自工具调用、DeepAgents编排、LangGraph编排，**仅允许通过`SubAgentRuntime`创建、运行、恢复、销毁**，不存在第二条通路。
2. **框架解耦**：上层业务代码禁止直接导入DeepAgents原生spawn API；通过适配器隔离底层编排实现；底层可切换LangGraph / DeepAgents，上层完全无感知。
3. **行为契约强统一**：创建、执行、中断检测、状态、resume恢复、元数据100%标准化；根治`astream结束=任务完成`的历史bug。
4. **产品体验最优**：子Agent拥有独立`thread_id`、独立checkpoint；子Agent内部HITL审批交互**只渲染在子Agent卡片内部**，绝不冒泡到主会话；支持多层嵌套、同级多子Agent并行。
5. **资源安全强制约束**：子Agent触发interrupt，执行协程必须立刻退出，释放asyncio、数据库连接；**禁止协程驻留、sleep、轮询等待人工输入**。
6. **基础设施完全复用**：直接复用Phase1统一异步checkpointer；Phase2 Redis热缓存开关完整复用，不重复造持久层。

> 非目标：
> - 不兼容历史会话、旧的agent_create/agent_run调用；
> - 不提供降级渲染分支；
> - 不在抽象层内实现任何Agent业务逻辑、Prompt、Graph编排。

## 3 关键强决策（Breaking‑Change）
### D‑1：彻底删除旧子Agent通路
1. 直接删除工具定义：`agent_create`、`agent_run`完全移除，不保留、不做薄包装。
2. 删除DeepAgents框架内部原生spawn子Agent的调用点；所有派生子Agent逻辑全部改为调用`SubAgentRuntime.spawn`；框架内部不再维护独立子Agent生命周期。
3. 删除旧代码路径中独立的子Agent创建、执行、状态管理代码；全部逻辑收敛到subagent_runtime模块。

> 影响：依赖旧工具、旧spawn接口的存量业务必须全部迁移；未迁移的旧代码直接编译/导入报错，没有静默降级。

### D‑2：子Agent与主Agent能力对等，仅创建来源不同
- 每一个子Agent分配独立`thread_id`；统一装配Phase1异步checkpointer；独立checkpoint存储。
- 独立元数据表存储`SubAgentInstance`，包含`parent_thread_id`；父子关系由运行时维护，**绝不依赖框架内部私有字段**；用于UI嵌套渲染、会话清理、权限隔离。

### D‑3：Interrupt / HITL 强契约（对齐Trae Solo）
1. 子Agent内部触发interrupt（shell_exec审批等），**绝对不冒泡到父Agent graph**；中断状态归属子Agent自身thread。
2. 父Agent不会被阻塞，可继续调度其他任务、其他子Agent，原生支持并行。
3. 硬性规则：**`astream迭代结束 ≠ 任务完成`**。流消费完毕，必须读取graph真实state做判断：
    - `state.next`非空：子Agent进入`INTERRUPTED_PENDING_USER_INPUT`；checkpoint持久化；协程退出释放全部资源；等待外部resume；
    - `state.next`为空：任务正常完成。
4. 恢复子Agent只能通过外部接口，向指定子Agent`thread_id`投递resume payload重新拉起执行；父Agent逻辑不参与子Agent恢复流程。
5. UI强制约束：子Agent审批控件**仅渲染在子Agent卡片内部**；主会话只渲染子Agent摘要信息，不渲染子Agent内部工具、审批按钮；**不做任何降级兼容分支**。

### D‑4：父Agent不会自动等待子Agent结果
`SubAgentRuntime`不提供阻塞等待API。
- 如果业务需要等待子Agent输出，由上层调用方（父Agent工具）自行轮询`get_instance`状态；完全对齐Trae Solo行为。

### D‑5：全局统一子Agent状态枚举（所有适配器必须映射到此集合）
- `RUNNING`：正在执行
- `COMPLETED`：正常执行结束
- `FAILED`：执行异常失败
- `INTERRUPTED_PENDING_USER_INPUT`：HITL中断，等待外部输入/审批

> `SubAgentInstance`携带标准化`pending_interrupt_info`，作为UI渲染审批控件唯一数据源。适配器输出必须对齐该模型，框架私有字段禁止透出到上层。

### D‑6：适配器模式隔离底层实现
- `BaseRuntimeAdapter`定义完整契约：spawn / resume / get_state / terminate；
- 实现两套适配器：`langgraph_adapter`、`deepagents_adapter`；
- 上层业务只导入`SubAgentRuntime`，禁止直接import适配器实现、禁止直接调用底层框架spawn接口。

### D‑7：防护约束（内置，不可关闭）
1. 子Agent最大嵌套深度硬限制，防止无限递归派生子Agent；
2. 子Agent单实例最大轮次上限，防止token无限消耗；
3. 子Agent终止时同步标记实例状态，可清理对应checkpoint记录。

## 4 模块划分（干净版本，无遗留兼容代码）
```
apps/ai_engine/subagent_runtime/
├─ runtime.py          # SubAgentRuntime 唯一对外入口
├─ models.py           # SubAgentInstance、状态枚举、元数据定义（UI唯一数据源）
├─ adapters/
│   ├─ base.py         # BaseRuntimeAdapter 抽象契约
│   ├─ langgraph_adapter.py
│   └─ deepagents_adapter.py
└─ exceptions.py       # 子代理运行时异常

# 删除：
# 1. apps/agent_hub/tools/agent_management.py 内 agent_create / agent_run 工具定义全部移除
# 2. DeepAgents内部原生spawn子Agent相关执行逻辑删除
```

### 模块职责边界（严格遵守，禁止业务逻辑侵入运行时）
1. **models.py**
    - `SubAgentInstance`：`thread_id`、`parent_thread_id`、status、`pending_interrupt_info`、metadata、created_at。
    - 只保存实例元数据；不存储graph、prompt、业务配置；业务配置由上层传入。
2. **BaseRuntimeAdapter**
    定义底层运行时必须实现的全部契约；适配器负责对接底层框架，处理graph执行、astream消费、state读取、框架私有字段到统一模型的映射。
3. **SubAgentRuntime**
    - 对外暴露spawn/resume/get_instance/terminate；
    - 元数据持久化、参数校验、异常处理；
    - 根据配置选择对应适配器；
    - 不编写任何Agent业务逻辑，不构造graph、不写prompt。

> 上层所有业务（DeepAgents编排、LangGraph graph内部），创建子Agent只能：
> ```python
> from apps.ai_engine.subagent_runtime import SubAgentRuntime
> ```

## 5 核心业务流程
### 5.1 spawn 创建子Agent
1. 上层调用`SubAgentRuntime.spawn(parent_thread_id, agent_config)`；
2. Runtime分配独立`thread_id`，写入父子元数据；校验嵌套深度防护；
3. 委派适配器spawn，装配Phase1统一异步checkpointer；
4. 适配器启动graph，消费astream流；流结束读取graph真实state判定状态；
5. 更新`SubAgentInstance`元数据返回；
6. 若状态为`INTERRUPTED_PENDING_USER_INPUT`：协程完全退出释放资源；checkpoint持久化；等待外部resume。
> 调用方spawn调用立刻返回，绝不阻塞等待子Agent执行结束。

### 5.2 resume 恢复中断子Agent
1. 前端子Agent卡片完成审批，调用后端`resume_subagent`HTTP接口，传入`subagent_thread_id`、resume_payload；鉴权校验；
2. Runtime读取子Agent元数据；校验状态必须为`INTERRUPTED_PENDING_USER_INPUT`；
3. 委派适配器，基于已有thread_id与checkpoint投递payload，重新拉起astream执行；
4. 执行结束再次读取graph state，更新实例状态；
5. SSE将子Agent事件流定向推送至对应子Agent卡片；不混入主会话流。

### 5.3 get_instance 查询实例
上层、父Agent工具查询标准化`SubAgentInstance`；父Agent如需等待结果，业务层自行轮询。

### 5.4 terminate 终止子Agent
终止执行，清理checkpoint，更新实例状态。

## 6 API契约
### 6.1 Python内部API（SubAgentRuntime对外）
- `spawn(parent_thread_id, agent_config) → SubAgentInstance`
- `resume(subagent_thread_id, resume_payload) → SubAgentInstance`
- `get_instance(subagent_thread_id) → SubAgentInstance | None`
- `terminate(subagent_thread_id) → None`

### 6.2 HTTP接口
新增唯一接口：`resume_subagent`
- 参数：`subagent_thread_id`，resume_payload；鉴权；
- 仅操作指定子Agent thread；不触碰父Agent会话；
原有主会话resume接口保留，二者完全隔离。

## 7 UI/UX硬性规范（无降级分支）
1. 主会话流只渲染子Agent摘要卡片：名称、状态；不展开子Agent内部思考、工具调用、审批按钮。
2. 用户展开独立子Agent卡片：卡片内完整渲染子Agent全部思考、工具调用、消息流。
3. 状态为`INTERRUPTED_PENDING_USER_INPUT`时，审批控件**仅渲染在子Agent卡片内部**。
4. 同级多子Agent各自独立卡片，互不干扰，支持并行。
5. 支持多层嵌套子Agent，每一层拥有独立卡片。

## 8 风险与硬性约束（文档固化）
1. **存量业务迁移风险**
    > 旧工具、旧spawn通路直接删除；未迁移代码直接报错，系统不会静默兼容。所有业务必须全部切换到`SubAgentRuntime`之后才能使用子Agent能力。
2. **适配器映射成本**
    DeepAgents与LangGraph内部state、interrupt数据结构不同，字段映射收敛在adapter内部；上层输出严格统一`SubAgentInstance`模型。
3. **无限嵌套防护**
    Runtime内置最大嵌套深度，超出直接抛出异常终止spawn，不可关闭。
4. **子Agent删除与缓存**
    继承Phase2 Redis缓存约束：PG删除thread，Redis靠TTL自动过期；thread_id不复用。
5. **资源安全红线**
    代码评审强制检查：禁止任何位置出现子Agent协程sleep、轮询等待用户输入；interrupt后协程必须退出释放资源。

## 9 前置依赖
1. Phase1完成：全链路统一异步checkpointer；`AgentFactory.create`标准化注入checkpointer；异常防御逻辑完成。
2. Phase2可选开启：Redis热缓存，开关可控。
3. SSE事件体系支持事件定向推送到指定子Agent卡片，区分主会话流与子Agent流。

## 10 落地实施步骤（一步到位，无过渡版本）
### Step‑1 搭建subagent_runtime模块
1. 定义`SubAgentInstance`数据模型、状态枚举、`BaseRuntimeAdapter`契约；
2. 实现`langgraph_adapter`、`deepagents_adapter`；
3. 实现`SubAgentRuntime`外壳，spawn/get_instance/terminate基础能力；
4. 实现嵌套深度、轮次上限防护。

### Step‑2 删除全部旧子Agent通路
1. 删除`agent_create`、`agent_run`工具定义；删除其全部实现代码；
2. 删除DeepAgents框架内部原生spawn子Agent的实现与调用点；
3. 全项目代码扫描：删除所有直接调用DeepAgents原生spawn的业务代码；全部替换为`SubAgentRuntime.spawn`。
> 本步骤完成后，系统不存在第二条子Agent创建通路。

### Step‑3 补齐Interrupt & Resume完整能力
1. 在适配器层实现流结束后读取graph真实state，区分完成 / interrupt暂停；
2. 实现Runtime.resume；新增HTTP接口`resume_subagent`；
3. 单元测试覆盖：interrupt暂停、resume断点续跑、协程资源释放、嵌套深度超限、轮次超限、失败场景。

### Step‑4 后端‑前端联调
1. SSE完成子Agent事件定向推送；
2. UI实现子Agent独立卡片；审批控件只在子Agent卡片内渲染；**不做任何降级兼容分支**。

### Step‑5 全链路集成测试
验收全部测试用例，全部业务通路迁移完成；上线。

## 11 验收测试用例（必须全部通过）
1. 子Agent触发shell_exec审批interrupt，协程正常退出释放资源；状态置为`INTERRUPTED_PENDING_USER_INPUT`；父Agent继续执行不阻塞。
2. 用户在子Agent卡片审批，调用`resume_subagent`，子Agent从checkpoint断点续跑。
3. 多层嵌套子Agent，每层独立thread_id、独立状态、独立卡片。
4. 多个同级子Agent并行运行互不干扰。
5. 子Agent触发嵌套超限，spawn直接抛出异常拒绝创建。
6. 子Agent轮次到达上限，标记FAILED终止。
7. checkpointer复用Phase1/Phase2；Redis开关正常开关；Redis异常自动降级PG。
8. 子Agent执行失败状态正确上报，不会出现“astream结束误判完成返回无效结果”历史bug。
9. 全项目代码中，不存在绕过`SubAgentRuntime`直接创建子Agent的代码路径。

## 12 淘汰方案对比
|方案|说明|结论|
|---|---|---|
|本方案：纯血SubAgentRuntime，直接删除旧通路，零兼容补丁|唯一入口；适配器隔离底层；interrupt归属子Agent卡片；父Agent不阻塞；资源安全；对齐Trae/Claude‑Code|✅采纳，最优架构|
|薄包装过渡方案|保留旧工具做内部转发；存在历史包袱，代码会持续积累技术债务|❌拒绝，不做兼容补丁|
|interrupt冒泡至主会话|复用现有审批组件，但破坏产品模型，无法并行，UI割裂|❌拒绝，无降级分支|
|子Agent协程挂起轮询等待审批|实现简单；协程、连接池泄漏风险|❌禁止，反模式|

> 整体评估：该方案彻底清除历史技术债务，架构干净，产品体验对齐业界标杆；代价是强制所有存量业务迁移，旧会话、旧调用链路不再兼容（现在只是开发，没用的直接不要）；适合追求长期质量、愿意一次性完成重构的场景。


## 1、主会话聊天视图（未展开任何卡片，用户第一眼看到）

```Plain Text
                                                     （紧靠左边）用户：帮我做一份市场调研，同时生成PPT大纲

AI主会话：
xxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxx
╭─────────────────────────────────────────────────────────────────────────╮
│  🛠️ 【工具调用】                                                          │
│  ├─ 工具：web_search 网页检索                                            │
│  ├── 入参：{"query":"2026消费行业市场规模分析","page_num":2}                   │         
│  ╰── 输出结果：检索到4份行业报告，覆盖市场规模、竞争格局基础数据。……              │
│   执行状态：已完成｜耗时：1420ms                                           │
╰────────────────────────────────────────────────────────────────────────╯（具体细节和之前卡片类似或者优化一下，工具审批什么的都是在一个工具调用卡片中）

xxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxx
╭─────────────────────────────────────────────────────────────────────────╮
🛠️ 【工具调用】                                                                   
│  ├─ 工具：spawn 派生子Agent                                               │
│  ├── 入参：{"agent_name":"市场调研任务","task":"市场调研任务"}        │
│  	执行状态：执行中｜耗时：480ms                                           │
╰────────────────────────────────────────────────────────────────────────╯
> 正在并行启动两项子任务，请等待任务执行完成，可点击卡片查看任务详情。

╭─────────────────────────────────────╮
│ 🧩 子Agent：市场调研任务             │
│ 🔵 ⏳ 状态：执行中                   │
╰─────────────────────────────────────╯

╭─────────────────────────────────────╮
│ 🧩 子Agent：PPT大纲生成              │
│ ⚪ ⏸️ 状态：等待调度  （执行中 最好可以并行|
╰─────────────────────────────────────╯


```

### 当市场调研任务进入【等待你的确认】

```Plain Text
                                                     （紧靠左边）用户：帮我做一份市场调研，同时生成PPT大纲

AI主会话：
xxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxx
╭─────────────────────────────────────────────────────────────────────────╮
│  🛠️ 【工具调用】                                                          │
│  ├─ 工具：web_search 网页检索                                            │
│  ├── 入参：{"query":"2026消费行业市场规模分析","page_num":2}                   |
│  ╰── 输出结果：检索到4份行业报告，覆盖市场规模、竞争格局基础数据。……                 │
│  执行状态：已完成｜耗时：10480ms                                            │
╰────────────────────────────────────────────────────────────────────────╯（具体细节和之前卡片类似或者优化一下，工具审批什么的都是在这对应的一个工具调用卡片中）

xxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxx

╭─────────────────────────────────────────────────────────────────────────╮
🛠️ 【工具调用】                                                                   
│  ├─ 工具：spawn 派生子Agent                                               │
│  ├── 入参：{"agent_name":"市场调研任务","task":"市场调研任务"}              |
│  	执行状态：执行中｜耗时：480ms                                           │
╰────────────────────────────────────────────────────────────────────────╯
╭─────────────────────────────────────────────────────────────────────────╮
🛠️ 【工具调用】                                                                   
│  ├─ 工具：spawn 派生子Agent                                               │
│  ├── 入参：{"agent_name":"PPT大纲生成","task":"PPT大纲生成"}              |
│  	执行状态：执行中｜耗时：480ms                                           │
╰────────────────────────────────────────────────────────────────────────╯
> 正在并行启动两项子任务，请等待任务执行完成，可点击卡片查看任务详情。

╭─────────────────────────────────────╮
│ 🧩 子Agent：市场调研任务             │
│ 🟠 ⚠️ 状态：等待你的确认             │
│ 💡 提示：点击卡片进入处理确认操作    │
╰─────────────────────────────────────╯

╭─────────────────────────────────────╮
│ 🧩 子Agent：PPT大纲生成              │
│ 🔵 ⏳ 状态：执行中                    |
╰─────────────────────────────────────╯
xxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxx

```



---

## 2、用户点击「市场调研任务」卡片 → 卡片内部展开完整详情面板

> 卡片在原地扩展开，**这里才出现全部思考、结构化工具调用、消息流、审批控件；嵌套子 Agent 仍然为摘要卡片**

```Plain Text
╭─────────────────────────────────────────────────────────────────────────────────────╮
│ 🧩 子Agent：市场调研任务                                                              |
│ 🟠 ⚠️ 状态：等待你的确认                                                                │
│                                                                │
│ > 需要获取行业最新公开数据，计划调用网页爬虫工具；该操作会访问外部网络，需要获得用户授权之后才可继续执行。                                                                                    │
│                                                                                      │
│ ╭───────────────────────────────────────────────────────────────────────╮
│ │  🛠️ 【工具调用】                                                        │
│ │  ├─ 工具：web_search 网页检索                                          │
│ │  ├── 入参：{"query":"2026消费行业市场规模分析","page_num":2}                 │
│ │  ╰── 输出结果：检索到4份行业报告，覆盖市场规模、竞争格局基础数据。……          │
│ │  执行状态：已完成｜耗时：1420ms   
│ ╰────────────────────────────────────────────────────────────────────────╯
|  xxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxx
│ ╭───────────────────────────────────────────────────────────────────────╮
│ │  🛠️ 【工具调用】        
│ │  ├─ 工具：spawn 派生子Agent                                              │
│ │  ├── 入参：{"agent_name":"抓取竞品数据","task":"收集头部竞品公开经营信息"}     │           
│ │  ├──🔐【待审批操作】                                                         │
│ │  ├───请求授权：允许调用爬虫工具访问外部网页抓取资料                                 │
│ │  ╰───[同意]      [拒绝]      [补充指令给Agent]    
│ | 
│ │   执行状态：执行中｜耗时：10480ms                                         │
│ ╰────────────────────────────────────────────────────────────────────────╯
╰──────────────────────────────────────────────────────────────────────────────────────╯
```

审批后：

```Plain Text
╭─────────────────────────────────────────────────────────────────────────────────────╮
│ 🧩 子Agent：市场调研任务                                                              │
│ 🔵 ⏳ 状态：执行中                                                                 │
│                                                                                     │
│ 📝 【Agent内部思考】                                                                  │
│ > 需要获取行业最新公开数据，计划调用网页爬虫工具；该操作会访问外部网络，需要获得用户授权之后才可继续执行。                                                                                    │
│                                                                                      │
│ ╭───────────────────────────────────────────────────────────────────────╮
│ │  🛠️ 【工具调用】                                                        │
│ │  ├─ 工具：web_search 网页检索                                          │
│ │  ├── 入参：{"query":"2026消费行业市场规模分析","page_num":2}                 │
│ │  ╰── 输出结果：检索到4份行业报告，覆盖市场规模、竞争格局基础数据。……          │
│ │  执行状态：已完成｜耗时：1420ms                                           │
│ ╰────────────────────────────────────────────────────────────────────────╯
|  xxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxx
│ ╭─────────────────────────────────────────────────────────────────────────╮
│ 🛠️ 【工具调用】                                                                   
│ │  ├─ 工具：spawn 派生子Agent                                               │
│ │  ├── 入参：{"agent_name":"PPT大纲生成","task":"PPT大纲生成"}              |
│ │  	执行状态：执行中｜耗时：480ms                                           │
│ ╰────────────────────────────────────────────────────────────────────────╯
│ > 正在并行启动一项待任务执行完成，可点击卡片查看任务详情。                       
│ ╭─────────────────────────────────────────────────────╮                            │
│ │ 🧩 嵌套子Agent：抓取竞品数据                         │                               │
│ │ 🔵 ⏳ 状态：执行中                                   │                             │
│ ╰─────────────────────────────────────────────────────╯                             │
╰──────────────────────────────────────────────────────────────────────────────────────╯
```



内层「抓取竞品数据」依旧是摘要卡片；点击内层卡片，才会展开它自己的思考、工具调用、消息流。

---

## 3、任务全部执行完成后的主会话摘要卡片（带简短结果预览，依然没有内部细节）

```Plain Text
用户：帮我做一份市场调研，同时生成PPT大纲

AI主会话：
xxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxx


╭─────────────────────────────────────────────────────────────────────────╮
│  🛠️ 【工具调用】                                                          │
│  ├─ 工具：web_search 网页检索                                            │
│  ├── 入参：{"query":"2026消费行业市场规模分析","page_num":2}                   │         
│  ╰── 返回摘要：检索到4份行业报告，覆盖市场规模、竞争格局基础数据。                  │
│   执行状态：已完成｜耗时：1420ms                                           │
╰────────────────────────────────────────────────────────────────────────╯
xxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxx
╭─────────────────────────────────────────────────────────────────────────╮
🛠️ 【工具调用】                                                                   
│  ├─ 工具：spawn 派生子Agent                                               │
│  ├── 入参：{"agent_name":"市场调研任务","task":"市场调研任务"}              |
│  	执行状态：已完成｜耗时：480ms                                           │
╰────────────────────────────────────────────────────────────────────────╯
╭─────────────────────────────────────────────────────────────────────────╮
🛠️ 【工具调用】                                                                   
│  ├─ 工具：spawn 派生子Agent                                               │
│  ├── 入参：{"agent_name":"PPT大纲生成","task":"PPT大纲生成"}              |
│  	执行状态：已完成｜耗时：23480ms                                           │
╰────────────────────────────────────────────────────────────────────────╯

> 两项子任务已全部执行完毕，点击卡片查看完整执行详情。
╭─────────────────────────────────────╮
│ 🧩 子Agent：市场调研任务             │
│ 🟢 ✅ 状态：已完成                  │
│ 📋结果预览：收集4份行业报告+竞品信息 │
╰─────────────────────────────────────╯

╭─────────────────────────────────────╮
│ 🧩 子Agent：PPT大纲生成              │
│ 🟢 ✅ 状态：已完成                  │
│ 📋结果预览：输出12页PPT完整大纲     │
╰─────────────────────────────────────╯
```

## 4、已完成状态点开后的展开面板示例

```Plain Text
╭─────────────────────────────────────────────────────────────────────────────────────╮
│ 🧩 子Agent：市场调研任务                                                               │
│ 🟢 ✅ 状态：已完成                                                                    │
│                                                                                      │
│ 📝 【Agent内部思考】                                                                  │
│ > 需要获取行业最新公开数据，计划调用网页爬虫工具；该操作会访问外部网络，需要获得用户授权之后才可继续执行。                                                                                    │
│                                                                                      │
│ ╭───────────────────────────────────────────────────────────────────────╮
│ │  🛠️ 【工具调用】                                                        │
│ │  ├─ 工具：web_search 网页检索                                          │
│ │  ├── 入参：{"query":"2026消费行业市场规模分析","page_num":2}                 │
│ │  ╰── 输出结果：检索到4份行业报告，覆盖市场规模、竞争格局基础数据。……          │
│ │  执行状态：已完成｜耗时：1420ms                                           │
│ ╰────────────────────────────────────────────────────────────────────────╯
|  xxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxx
│ ╭─────────────────────────────────────────────────────────────────────────╮
│ 🛠️ 【工具调用】                                                                   
│ │  ├─ 工具：spawn 派生子Agent                                               │
│ │  ├── 入参：{"agent_name":"PPT大纲生成","task":"PPT大纲生成"}              |
│ │  	执行状态：已完成｜耗时：23480ms                                           │
│ ╰────────────────────────────────────────────────────────────────────────╯
│ > 正在并行启动一项待任务执行完成，可点击卡片查看任务详情。                       
│ ╭─────────────────────────────────────────────────────╮         │
│ │ 🧩 嵌套子Agent：抓取竞品数据                         │         │
│ │ 🟢 ✅ 状态：已完成                                  │         │
│ ╰─────────────────────────────────────────────────────╯         │
│ 📜【子Agent完整消息流】                                                              │
│ xxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxx
╰──────────────────────────────────────────────────────────────────────────────────────╯
```

