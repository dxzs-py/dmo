# 项目质量分析报告

- **项目**：langchain_xm（AI Agent 平台：Django + LangChain + Vue3 + RAG）
- **评估日期**：2026-08-19
- **状态更新**：2026-08-19 晚间复核——5 组发现已处置（详见各章标记与第 10 章）
- **评估方式**：5 个专项维度静态代码评审（只读），全部发现已登记至 ontology 图谱（`memory/ontology/graph.jsonl`）

---

## 1. 评估概览

### 1.1 目的

对项目后端（Django + LangChain 智能体平台）与前端（Vue3 单页应用）进行系统化质量评估，识别安全风险、正确性缺陷、规范偏离与可维护性问题，并给出可执行的改进路线。

### 1.2 范围

- **后端**：`backend\Django_xm\Django_xm\apps\` 下 11 个 Django app（core、ai_engine、agent_hub、chat、knowledge、approvals、attachments、cache_manager、context_manager、analytics、learning），另有 research、tools app（见各发现 locations）。技术栈：Django 5.x + DRF + Channels + Celery + LangGraph。
- **前端**：`frontend\langchain-vue\`，Vue3 `<script setup>` + Vite + Pinia + Element Plus + Tailwind，63 个 .vue 组件，src/ 下 api/components/composables/config/router/stores/types/utils/views。
- **LangChain 栈**：langchain>=1.2.13、langgraph>=1.1.3、deepagents>=0.1.0，官方 `create_agent`/`create_deep_agent` + `init_chat_model` + PostgresSaver checkpointer。

### 1.3 方法

5 个专项维度独立评审：Django 框架设计规范符合性、LangChain 集成与最佳实践、代码质量综合评估、功能设计合理性、代码冗余与质量问题。评审手段包括：静态阅读核心链路代码、跨 app import 统计、全仓 Grep 验证（零引用/重复定义/规范偏离）、配置与路由核对。每条发现均附代码位置与证据，登记至 ontology 图谱。

### 1.4 优先级定义

| 优先级 | 定义 | 处置要求 |
|--------|------|----------|
| **P0** | 安全/正确性问题，存在可验证的攻击路径或数据错误 | 立即处理 |
| **P1** | 高风险：安全边界缺口、正确性 bug、工程基础缺失 | 尽快处理（建议本迭代内） |
| **P2** | 规范偏离、性能隐患、可维护性问题 | 中期改进（1-2 个迭代） |
| **P3** | 风格一致性、长期优化项 | 长期优化（按机会安排） |

---

## 2. 评分摘要

| 维度 | 评分 | 发现数 | P0 | P1 | P2 | P3 | 状态（2026-08-19 二次复核） | 一句话结论 |
|------|------|--------|----|----|----|----|--------------------------|------------|
| 一、Django 框架设计规范符合性 | 7/10 | 22 | 0 | 6 | 9 | 7 | 17 条已关闭（dj-04/05/06、dj-02、dj-08/09、dj-12/17/21/22、dj-15/16、dj-18/13、dj-07/10/14）/其余待办 | 架构骨架优秀，安全细节与 API 语义一致性需专项整改 |
| 二、LangChain 集成与最佳实践 | 8/10 | 18 | 1 | 2 | 9 | 6 | 2 条已关闭（lc-02 含勘误、lc-05）/1 条不解决（lc-03）/其余待办 | 官方模式对齐度高、工程质量好，但安全边界存在可验证的缺口 |
| 三、代码质量综合评估 | 7/10 | 13 | 0 | 4 | 5 | 4 | 3 条已关闭（cq-01/02/09）/其余待办 | 风格与工程化水平较高，测试基础设施缺失与复杂度热点是主要短板 |
| 四、功能设计合理性 | 7/10 | 15 | 0 | 2 | 6 | 7 | 全部待办 | 分层架构与一致性设计亮点突出，双向依赖与生产配置缺口拉低上限 |
| 五、代码冗余与质量问题 | 6.5/10 | 12 | 0 | 2 | 5 | 5 | 3 条已关闭（rd-03、rd-04 全量收敛；rd-05 删除部分，Set 接线待办）+ 僵尸表 ContextRule/AutoMemory/PromptCache 代码与数据库已彻底删除（2026-08-19，spec: remove-dead-code-batch）/rd-02 全部收口（anti_oscillation.py 已删，2026-08-20，spec: c1-lightweight-convergence）/rd-01 + rd-12 已完成（2026-08-20，spec: c2-requirements-rewrite） | 复用健康度中上，依赖清单失序与死代码/双实现需集中清理 |
| **总分（五维平均）** | **7.1/10** | **80** | **1** | **16** | **34** | **29** | **A 类 4 项完成（spec: add-regression-test-infra）+ 死代码删除 spec 完成（remove-dead-code-batch：rd-03 彻底统一、rd-04 全量收敛、rd-05 删除部分、僵尸表三模型代码+数据库删除、hierarchical_memory 死模块删除）+ 26 项关闭（dj-04/05/06、dj-02、lc-02、lc-05、cq-01/02/09、rd-02 全部收口、dj-14、cq-07、dj-10、dj-07；后 5 项见 spec:c1-lightweight-convergence 2026-08-20；dj-12/17/21/22 见 spec:c2-model-migration-consistency 2026-08-20；dj-15/16 见 spec:c7b2-pagination-route-normalization 2026-08-20；rd-01/rd-12/dj-02 见 spec:c2-requirements-rewrite 2026-08-20；dj-18/dj-13 见 spec:c7b3-data-model-consolidation 2026-08-20，C7 全部收口）/1 项搁置（lc-03）** | — |

### 整体结论

项目整体质量处于**良好水平（7.1/10）**，工程化成熟度明显高于一般同类项目，但存在一个必须立即处置的安全 P0 与一批制约上限的结构性问题。

**亮点**：

1. **官方生态对齐**：LangChain 栈全面采用 1.x 官方新 API（create_agent/create_deep_agent/AgentMiddleware/interrupt/init_chat_model/PostgresSaver），全后端无废弃 API 残留；分层容错链（SDK 重试→执行器退避重试→模型 fallback→工具降级→无工具直答）落实完整。
2. **审批发件箱与跨浏览器同步设计**：审批体系（状态机 + Redis/DB 双写 + ApprovalOutboxEntry 事务性发件箱 + 消息幂等 + 超时注入 ToolMessage 不终结任务）完整支撑多浏览器一致性；前端以 seq 单调序 + orderedQueue + replay 分块回放解决乱序/去重/断线恢复。
3. **分层容错与规范执行**：Django 侧 settings 环境拆分、密钥全走环境变量、统一异常处理/限流/JWT、BaseModel/AuditModel 抽象基类；前端 63 个组件全部 `<script setup>`、命名边界（sessionTransformers 唯一转换点 + getQueryParam 隔离）执行到位。

**短板**：

1. **审批门控缺口与 shell 白名单绕过**（lc-01/lc-02/lc-03）：shell_exec 白名单前缀匹配可被 `python manage.py shell -c` 等方式绕过；子代理未注入审批中间件（**lc-02 已于 2026-08-19 修复并勘误：原判断系死路径误报，子代理统一经 BaseAgentBuilder 构建、本就有审批**）；无策略工具（含全部 MCP 动态工具）默认免审批放行（**lc-03 用户决定不解决，保持现状**）。
2. **测试基础设施缺失**（cq-01/cq-02）：前端 8 个测试文件因缺 vitest 依赖无法运行；后端仅 11 个测试文件，context_manager/knowledge/learning 等关键路径零测试。
3. **依赖清单失序**（dj-02/rd-01/rd-12）：requirements.txt 既固化 40+ 条零引用/传递依赖（spacy 全家桶等），又缺失 channels 系运行时核心依赖，新环境无法按清单复原。
4. **双向依赖**（ds-02/ds-11/ds-15）：app 级循环依赖普遍，靠函数内延迟导入掩盖，且跨 app 私有成员导入穿透门面，无 CI 防护。

---

## 3. 维度一：Django 框架设计规范符合性（7/10，22 条）

### 3.1 小结

项目 Django 层整体架构水准较高：settings 按环境拆分（base/dev/prod/test）规范，密钥全部走环境变量与 Pydantic 配置中心；DRF 全局配置完善（JWT 认证、全局 IsAuthenticated、统一异常处理器、统一限流、drf-spectacular 文档）；模型层有统一 BaseModel/AuditModel 抽象基类（软删除 + 审计字段 + 双管理器），复合索引设计有针对性（approvals 按 user/state/created_at 建索引）；视图-服务分层清晰（视图薄、服务厚）；中间件按职责拆包且安全头完备（HSTS/CSP/nosniff/会话固定防护）；信号处理规范（AppConfig.ready() 延迟注册 + transaction.on_commit）；审批模块有 outbox 最终一致性设计与消息幂等（Redis SETNX）。扣分项集中在：1 个权限越权风险（任意登录用户可清空全局缓存）、requirements 缺失 channels 系核心依赖、多处错误响应泄漏内部异常细节、消息序列化 N+1 与缓存分支翻页失效两个正确性问题、tools 等 app 大面积动词式 URL + POST 承载删除操作违背 RESTful 规范。

### 3.2 P1 发现详述

#### dj-01【P1】CacheClearView 权限过宽：任意登录用户可清空全局缓存并按任意 pattern 删 key

**[🔶部分解决 2026-08-19 · spec:b2-exception-hardening · 2026-08-20 复审降级]** 根因修复（键错位 + 越权两层）：① 后端参数键从 `type` 改读 `scope`（与 CacheInvalidateView 语义对齐，根因修复「前端传 scope / 后端读 type 导致请求固定落入全量清除分支」的键错位）；② `get_permissions()` 按 scope 动态分派——`query` → IsAuthenticated（仅清 `rag_query:user_{id}_*` 自己的缓存），`all/model/pattern` → IsAdminUser；③ 非法 scope 返回 400；④ 登录响应（UserInfoSerializer / MyTokenObtainPairSerializer）补 `is_staff` 字段，前端 user store 增 `isAdmin` computed，KnowledgeBaseView「清空全部缓存」danger 按钮加 `v-if="userStore.isAdmin"`。新增 cache_manager/tests/test_views_clear.py 10 用例覆盖权限矩阵（普通用户 query 可用 / all/model/pattern 403，管理员全 scope 可用）与通用文案断言。**复审发现两处残余（见第 11 章 R1/dj-01R）**：① 前端 `stores/user.js:59` 读 `userData.is_staff`，但 axios 响应拦截器 toCamelCase 已将键转为 `isStaff` → `isAdmin` 恒 false，管理员也看不到按钮（旧版按钮无条件显示，构成功能回归）；② `cache_manager/views.py:199` `if pattern:` 分支优先于 `:202 elif scope == "query"`，普通用户 POST `{scope:"query", pattern:"*"}` 时 get_permissions 仅返回 IsAuthenticated，post 内直接执行 delete_pattern（无白名单）→ 组合绕过。两处修复均 <10 行，建议立即立项。

**问题描述（修复前）**：CacheClearView 仅要求 IsAuthenticated，但无 pattern 参数时会遍历所有 CACHE_PREFIX_LABELS 前缀删除全部 Redis 缓存并重置统计，影响全部用户的 RAG 查询/模型缓存；且 pattern 参数直接透传 CacheService.delete_pattern 无白名单，登录用户可传 `'*'` 删光任意 key。同文件中能力更弱的 CacheInvalidateView 与 CacheResetStatsView 反而都是 IsAdminUser，权限设计自相矛盾。

**代码位置**：
- backend/Django_xm/Django_xm/apps/cache_manager/views.py:180
- backend/Django_xm/Django_xm/apps/cache_manager/views.py:191
- backend/Django_xm/Django_xm/apps/cache_manager/views.py:199

**改进建议**：将 CacheClearView 的全量清除分支与 pattern 分支权限收紧为 IsAdminUser（或自定义 IsAdmin）；用户自清理仅保留 `cache_type='query'` 分支（按 user_id 前缀隔离）；pattern 参数增加前缀白名单校验。

**证据**：

```python
class CacheClearView(APIView):
    permission_classes = [IsAuthenticated]
    ...
            if pattern:
                CacheService.delete_pattern(pattern)
            else:
                # 遍历 CACHE_PREFIX_LABELS 全量删除
```

#### dj-02【P1】requirements.txt 缺失运行时核心依赖 channels / channels-redis / daphne

**问题描述**：INSTALLED_APPS 含 'daphne' 与 'channels'，dev.py 的 CHANNEL_LAYERS 使用 channels_redis.core.RedisChannelLayer，asgi.py 导入 channels.routing，但 requirements.txt 中没有任何 channels/channels-redis/daphne 条目（仅 requirements-dev.txt 有 types-channels 类型存根）。新环境按依赖清单安装后项目无法启动，依赖清单失真。

**代码位置**：
- backend/Django_xm/Django_xm/settings/base.py:53
- backend/Django_xm/Django_xm/settings/dev.py:40
- backend/Django_xm/Django_xm/asgi.py:10
- backend/Django_xm/requirements.txt:1

**改进建议**：在 requirements.txt 补充 channels>=4.x、channels-redis>=4.x、daphne>=4.x；建议改用 pip-compile / uv lock 生成锁定文件，避免 200+ 条 >= 浮动版本混入传递依赖（aiohappyeyeballs、blis 等明显是传递依赖被固化）。

**证据**：

```python
INSTALLED_APPS = [
    "daphne",  # ASGI WebSocket 服务器
    ...
    "channels",
]
```

**[✅已修复 2026-08-20 · spec:c2-requirements-rewrite]** channels>=4.3.2 / channels-redis>=4.3.0 / daphne>=4.2.2 已随 C2 全量重写收录进 requirements.txt（Web 框架与 ASGI 组），并在干净 venv（Python 3.11.15）安装回归实证：`manage.py check` 0 issues + 318 测试全绿 + daphne 作为 ASGI 服务器接管 runserver 正常。

#### dj-03【P1】多处错误响应直接返回内部异常细节（str(e)）给客户端

**[✅已修复 2026-08-19 · spec:b2-exception-hardening]** 全量清理 17 文件 53 处（原报告 5 处 + 复核发现的 tools/views_mcp.py 扩散 7 处 + views 层全量补漏）。原则：「业务语义异常保留（ValueError/FileNotFoundError/WorkflowAlreadyFinishedError/DoesNotExist 等预期内、面向用户的校验文案），系统异常隐藏（通用文案 + logger.exception 落日志）」。覆盖范围：HTTP 响应（error_response message/data）、SSE error 事件（views_rag/strict_rag_chain/learning_stream/research streams）、WebSocket payload（chat/learning consumers）、裸 Response 规范化（views_rag.py:269）。新增 knowledge/tests/test_views_error_messages.py 2 用例断言通用文案。范围外保留项（用户已决策 2026-08-19 均保留不动，发布阶段前再统一收紧）：tasks/*.py mark_failure(error_message=str(e)) → 任务状态 API 透出（开发阶段排障优先）；db_monitor/mcp 健康检查类 str(e)（db_monitor 两处普通用户可见、mcp 一处无调用方，诊断价值优先）。

**问题描述（修复前）**：多个视图在 except 分支把 str(e) 拼入 message/data 返回：ChatView 的 500 响应 data 含 error=str(e)；knowledge/views_kb.py 的创建/列表/详情接口 message=str(e)；cache_manager/views.py 三处同样。异常字符串可能包含数据库表结构、文件路径、上游 API 报文等敏感信息，与 common/exceptions.py「避免泄露内部错误信息」的设计目标冲突。

**代码位置**：
- backend/Django_xm/Django_xm/apps/chat/views_chat.py:446
- backend/Django_xm/Django_xm/apps/knowledge/views_kb.py:77
- backend/Django_xm/Django_xm/apps/knowledge/views_kb.py:106
- backend/Django_xm/Django_xm/apps/cache_manager/views.py:83
- backend/Django_xm/Django_xm/apps/tools/views_mcp.py:253

**改进建议**：统一约定：5xx 响应只返回通用文案（同 exceptions.py 500 分支），异常细节仅写入 logger.exception；对外可返回 error_id 便于日志关联排障。

**证据**：

```python
return error_response(
    code=ErrorCode.SERVER_ERROR, message=str(e),
    http_status=status.HTTP_500_INTERNAL_SERVER_ERROR
)
```

#### dj-04【P1】ChatMessageSerializer 每条消息触发 2 次跨表查询（N+1）

**[✅已修复 2026-08-19 · spec:normalize-tools-rest-fix-chat-api]** 新增模块级函数 `build_research_task_map` 一次 all_objects 批量查询构建 `{task_id: task}` 映射，经 serializer context 传播（嵌套自动继承），两个 method field 改读映射且映射缺失时 KeyError 快速失败，全仓 12 处实例化点已全部改造。

**问题描述**：get_research_task_status 与 get_research_task_deleted 各自独立执行 ResearchTask.all_objects.filter(...).first()。ChatSessionDetailSerializer 嵌套 ChatMessageSerializer(many=True)，会话详情接口若含 100 条消息则额外产生 200 次查询；ChatMessageBatchCreateView 等批量场景同样放大。注释说明必须用 all_objects（绕过软删除过滤）导致无法用 prefetch_related，但未做任何结果缓存或批量聚合。

**代码位置**（修复后现状）：
- backend/Django_xm/Django_xm/apps/chat/serializers.py:198（build_research_task_map 定义）
- backend/Django_xm/Django_xm/apps/chat/serializers.py:301（get_research_task_status 改读映射）
- backend/Django_xm/Django_xm/apps/chat/serializers.py:314（get_research_task_deleted 改读映射）
- backend/Django_xm/Django_xm/apps/chat/serializers.py:407（ChatSessionDetailSerializer 契约：context 自动传播）

**改进建议**：在视图中一次性批量查询本会话涉及的所有 ResearchTask（task_id__in=list）构建 {task_id: task} 映射，通过 serializer context 传入；或在序列化前聚合为 dict 缓存到 instance 上（如 instance._research_task），两个 method field 复用同一结果。

**证据**：

```python
def get_research_task_status(self, obj) -> str | None:
    if not obj.research_task_id:
        return None
    task = ResearchTask.all_objects.filter(task_id=obj.research_task_id).first()
    return task.status if task else None
```

#### dj-05【P1】ChatSessionListView 缓存分支 page 硬编码为 1，缓存命中时翻页失效

**[✅已修复 2026-08-19 · spec:normalize-tools-rest-fix-chat-api]** 缓存命中分支删除 page=1 硬编码，改为与 DB 分支复用同一 `Paginator(sessions_data, page_size).get_page(request.query_params.get("page", 1))`，非法值回退第 1 页、越界回退最后一页，分页语义与 DB 分支完全对齐。

**问题描述**：会话列表缓存命中分支中 page = 1 硬编码，未读取 page 查询参数（只解析了 page_size）。用户请求第 2 页及以后时，只要缓存命中就永远返回第一页数据，而 DB 分支的 paginate_queryset 正确处理 page。属于正确性 bug：前端翻页在缓存有效期内显示错误数据。

**代码位置**（修复后现状）：
- backend/Django_xm/Django_xm/apps/chat/views_chat.py:869（缓存命中分支 if sessions_data）
- backend/Django_xm/Django_xm/apps/chat/views_chat.py:874（Paginator + get_page 真实页码）

**改进建议**：缓存分支同样从 request.query_params 解析 page（复用 _parse_page_params 风格），对 sessions_data 切片时使用真实页码；并为该路径补充带 page 参数的单元测试。

**证据**：

```python
if sessions_data:
    sessions_data.sort(key=lambda x: x.get("updated_at", ""), reverse=True)
    total = len(sessions_data)
    page = 1  # 硬编码，忽略 ?page= 参数
```

#### dj-06【P1】tools app 大面积动词式 URL 且用 POST 承载删除/更新操作

**[✅已修复 2026-08-19 · spec:normalize-tools-rest-fix-chat-api · BREAKING 无兼容层]** urls.py 全量重写为资源路由（GET/POST 集合端点、PUT/DELETE `/{name}/`、PATCH `/{name}/status/`、POST `/{name}/test/`，discover 保留）；views_mcp.py 删除 19 个旧动词式 View 类，新增 4 组资源视图（McpServer/CustomTool/Skill/SkillPackage）+ MethodThrottleMixin（读写分级限流）；前端 api/tools.js 方法全量对齐资源路由；新增 tools/tests/test_views_rest.py 覆盖资源语义。**BREAKING：未保留旧路径兼容层，前后端同步切换。**

**问题描述**：tools/urls.py 约 10 个端点采用动词式路径（mcp/servers/add/、mcp/servers/update/、mcp/servers/delete/、mcp/servers/toggle/、custom/delete/、custom/toggle/、skills/create/、skills/delete/ 等），且 McpServerDeleteView、CustomToolDeleteView、SkillDeleteView、SkillPackageDeleteView 均以 POST 方法执行删除。违背团队规范中 RESTful（GET/POST/PUT/DELETE 语义化）要求，是全项目 RESTful 一致性最差的模块。chat（messages/<id>/delete/）、knowledge（indices/<kb_id>/delete/）、attachments（admin/cleanup/）也存在同类动词式后缀，但方法语义多数正确（chat 删除用 DELETE）。

**代码位置**（修复后现状）：
- backend/Django_xm/Django_xm/apps/tools/urls.py:7-36（资源路由全量，MCP 只读/call-log 与 discover 保留）
- backend/Django_xm/Django_xm/apps/tools/views_mcp.py:294（MethodThrottleMixin）
- backend/Django_xm/Django_xm/apps/tools/views_mcp.py:319（McpServerView 集合视图；资源视图组另见 171/360/393、512/616/671、696/733/766、804/893/926）

**改进建议**：资源化改造：mcp/servers/（POST 创建）、mcp/servers/{name}/（PATCH 更新、DELETE 删除）、mcp/servers/{name}/enabled/（PATCH 切换）；toggle 类状态切换映射为 PATCH + status 字段。存量路径可保留一个兼容期并标注 deprecated。

**证据**：

```python
path("mcp/servers/delete/", views_mcp.McpServerDeleteView.as_view(), name="mcp_servers_delete"),
# McpServerDeleteView:
    def post(self, request):  # 删除操作使用 POST
```

### 3.3 P2/P3 发现明细

| ID | 优先级 | 问题 | 位置 | 建议 |
|----|--------|------|------|------|
| dj-07 | P2 | 知识库路由双轨制混乱：indices/create 与 indices/create-empty 同视图、/indices/ 与 /knowledge-bases/ 两套等价端点并存，索引管理路由命名与视图职责错位 | backend/Django_xm/Django_xm/apps/knowledge/urls.py:24<br>backend/Django_xm/Django_xm/apps/knowledge/urls.py:25<br>backend/Django_xm/Django_xm/apps/knowledge/urls.py:43 | 收敛为单一 /knowledge-bases/ 资源体系；create-empty 改为 POST 请求体显式字段或独立子资源端点；清理旧路径并标注迁移计划<br>**[✅已修复 2026-08-20 · spec:c1-lightweight-convergence]** 收敛为 /knowledge-bases/ 单一资源体系（detail/documents/documents/{filename}/search 子资源）；删除 /indices/ 全部旧端点与 upload 动词路由；前端 api/knowledge.js 全量适配，旧路由零残留 |
| dj-08 | P2 | SystemConfig 进程内缓存多 worker 不一致，异步上下文缓存未命中时静默返回默认值（仅 debug 日志），异步任务可能用错模型 | backend/Django_xm/Django_xm/apps/ai_engine/models.py:73<br>backend/Django_xm/Django_xm/apps/ai_engine/models.py:76<br>backend/Django_xm/Django_xm/apps/ai_engine/models.py:87 | 缓存改用带 TTL 的 Redis，或写时经 Redis pub/sub 广播失效；异步未命中至少提升为 warning 并考虑 sync_to_async 读真实值<br>**[✅已修复 2026-08-19 · spec:b3-prod-config-cache]** get/set 全量迁 default cache（Redis，key `system_config:{key}`，TTL 3600s），删 `_config_cache` dict 与 `invalidate_system_config_cache`；异步未命中返回 default + warning；warmup 改遍历 DB 幂等写 Redis，四处调用点不变 |
| dj-09 | P2 | 会话 post_save 全量失效用户会话列表缓存，与列表缓存加速目标自相矛盾，命中率趋近于零且徒增 Redis 写放大 | backend/Django_xm/Django_xm/apps/chat/signals.py:26<br>backend/Django_xm/Django_xm/apps/chat/signals.py:29<br>backend/Django_xm/Django_xm/apps/chat/views_chat.py:856 | 失效粒度降为单会话条目（短 TTL 自然过期）；仅标题等列表可见字段变更才失效列表缓存<br>**[✅已修复 2026-08-19 · spec:b3-prod-config-cache]** post_save 仅失效当前会话单条 key，post_delete 失效单条 + 列表索引 remove，patch 重命名显式失效单条 + 列表；列表索引 TTL 24h→120s；**根因修复：chat/apps.py ready() 注册 signals（此前从未激活，失效逻辑为死代码）**；新增 11 测试 |
| dj-10 | P2 | 知识库视图绕过 Serializer 手动校验，并以异常消息字符串「已存在」做业务分支，文案调整即失效 | backend/Django_xm/Django_xm/apps/knowledge/views_kb.py:81<br>backend/Django_xm/Django_xm/apps/knowledge/views_kb.py:95<br>backend/Django_xm/Django_xm/apps/knowledge/views_kb.py:130 | 定义 Create/UpdateSerializer 走标准 is_valid；服务层抛带 code 的自定义异常，按异常类型分支<br>**[✅已修复 2026-08-20 · spec:c1-lightweight-convergence]** 新建 knowledge/serializers.py（Create/Update/Search 三 Serializer 标准 is_valid，错误定位到字段）；服务层改抛 KnowledgeBaseAlreadyExistsError 类型化异常，视图按异常类型分支，不再解析文案字符串 |
| dj-11 | P2 | threading.local 存储请求上下文在 ASGI/线程池场景失效，analytics 事件可能归因到错误 IP/UA | backend/Django_xm/Django_xm/apps/core/base_models.py:12<br>backend/Django_xm/Django_xm/apps/core/middleware/request.py:18<br>backend/Django_xm/Django_xm/apps/chat/views_chat.py:242 | 迁移到 contextvars.ContextVar（asgiref 3.x 原生 async 安全）；或写路径显式传递 request/user |
| dj-12 | P2 | 多处冗余索引：唯一列/已建索引字段上重复建索引（IndexMetadata.idx_name、UserEvent/DailyAggregation 的 created_at/date 单列索引） | backend/Django_xm/Django_xm/apps/knowledge/models.py:87<br>backend/Django_xm/Django_xm/apps/knowledge/models.py:115<br>backend/Django_xm/Django_xm/apps/analytics/models.py:81<br>backend/Django_xm/Django_xm/apps/analytics/models.py:123<br>backend/Django_xm/Django_xm/apps/core/base_models.py:39 | 通过新迁移 RemoveIndex 清理重复索引；复合索引已覆盖的单列索引一并评估删除<br>**[✅已修复 2026-08-20 · spec:c2-model-migration-consistency]** 删 IndexMetadata.idx_name（unique 已建索引）；UserEvent 删 created_at 单列索引 + user/event_type/event_category 字段级索引（复合索引最左前缀）；DailyAggregation 删 date 与 (user,-date) 索引（字段级/unique_together 已覆盖）；迁移 knowledge.0004 + analytics.0004（3 RemoveIndex + 4 AlterField）已应用；base_models 基类索引经评估非重复项保留 |
| dj-13 | P2 | DocumentIndex 与 IndexMetadata 两个索引模型职责重叠（文档数双计数、状态机单侧维护），路由双轨也源于此 | backend/Django_xm/Django_xm/apps/knowledge/models.py:19<br>backend/Django_xm/Django_xm/apps/knowledge/models.py:77<br>backend/Django_xm/Django_xm/apps/knowledge/models.py:103 | 以 IndexMetadata 为唯一实体（已有状态机与扩展 JSON），DocumentIndex 改 FK 指向，计数保留单一权威来源<br>**[✅已修复 2026-08-20 · spec:c7b3-data-model-consolidation]** DocumentIndex 彻底删除（模型/admin/serializer/信号全仓零残留），IndexMetadata 继承 BaseModel 成唯一实体（软删墓碑 + objects 默认过滤 + all_objects）；Document.index FK 改指 IndexMetadata（非空）；文件数改 Document 表派生查询（废弃 document_count 存储计数器，消除增量维护+信号重算漂移）；显示语义修正 document_count=文件数/chunk_count=块数（前端 KnowledgeBaseView/RagView 全量适配）；迁移拆三段 0005(DDL)/0006(DML 回填)/0007(FK 切换)——根因：AddField(db_index=True) 的 deferred CREATE INDEX 与 RunPython DML 同事务触发 PG pending trigger events，测试库因空数据无 DML 而漏测；存量库 migrate 冒烟 + 数据完整性验证通过（9 实体合并/29 文档全链接/临时列零残留）；signals/analytics/cross_app/learning 调用点 100% 适配 + chat_service 传 user_id 而非 User 实例的静默降级根因修复；新增生命周期测试 4 项 |
| dj-14 | P2 | DocumentIndex.get_absolute_url 反查参数与路由不匹配（name vs kb_id），调用即抛 NoReverseMatch；ChatSession 等同类方法在纯 API 项目语义存疑 | backend/Django_xm/Django_xm/apps/knowledge/models.py:46<br>backend/Django_xm/Django_xm/apps/knowledge/urls.py:27<br>backend/Django_xm/Django_xm/apps/chat/models.py:49 | 统一 kwargs 与路由捕获参数名；确认无模板消费场景则删除死代码<br>**[✅已修复 2026-08-20 · spec:c1-lightweight-convergence]** 按拍板方案直接删除（参数名错误 + 无消费方，不修复保留）：6 处 get_absolute_url 全删（DocumentIndex/ChatSession 等），全仓零残留 |
| dj-15 | P2 | APIView 手写分页/截断，DRF 全局分页与过滤配置形同虚设；ApprovalListView [:100] 硬截断超量静默丢失 | backend/Django_xm/Django_xm/apps/approvals/views.py:270<br>backend/Django_xm/Django_xm/apps/chat/views_chat.py:344<br>backend/Django_xm/Django_xm/settings/base.py:213<br>backend/Django_xm/Django_xm/settings/base.py:218 | 列表端点迁移 generics.ListAPIView + 自定义 ProjectPagination（统一 items/total/page 结构）；ApprovalListView 改真实分页；筛选接入 django-filter<br>**[✅已修复 2026-08-20 · spec:c7b2-pagination-route-normalization]** 新建 common/pagination.py ProjectPagination（items/total/page/page_size/total_pages 统一结构）+ paginate_to_dict 辅助；全局 DEFAULT_PAGINATION_CLASS 改指它（配置形同虚设根因消除）；ApprovalListView 迁 generics.ListAPIView + ApprovalFilter 四字段 + 真分页删 [:100]（含 filter_backends 显式声明防过滤静默失效）；chat DB+缓存两分支与 knowledge 两列表收敛到同一分页器（手写实现零残留）；前端 getApprovalHistory 死函数删除（camelCase query 违规 + 零消费）；新增 12 测试 |
| dj-16 | P3 | research 重复端点（result/ 与 results/ 同视图）与 chat 路由命名前缀不一致、research 单复数资源名不一致 | backend/Django_xm/Django_xm/apps/research/urls.py:36<br>backend/Django_xm/Django_xm/apps/research/urls.py:37<br>backend/Django_xm/Django_xm/apps/chat/urls.py:23<br>backend/Django_xm/Django_xm/apps/research/urls.py:32 | 保留其一并标注 deprecated；chat 路由统一 chat_messages_* 前缀；删除路由对齐 tasks/ 复数形式<br>**[✅已修复 2026-08-20 · spec:c7b2-pagination-route-normalization]** 删 result/ 重复端点（零消费直删不留 deprecated）；task/ → tasks/ 资源复数化（delete + retry-subagent，name 同步 tasks_*）；chat messages/<id>/delete/ → messages/<id>/（与 PATCH update 同径合并为单视图承载 patch+delete，对齐 tools dj-06 惯例）；pair/delete/ → pair/<int:user_message_id>/（DELETE body 改路径参数）；name 统一 chat_messages_* 前缀；前端 4 处调用点 100% 适配 + 旧路径 404 防回归断言；新发现：learning/urls.py 存在同款 path("task/...") 单数资源（同病待后续批次）<br>**[✅learning 延伸收口 2026-08-20 · spec:learning-route-convergence]** learning/urls.py task/ → tasks/ + name delete → tasks_delete + WorkflowDeleteView → WorkflowTaskDeleteView；前端 workflow.js 1 处调用点适配；新增 learning/tests/test_route_normalization.py 4 项防回归；schema.yml 重新生成零旧路由；顺带根因修复 views_kb.py get_parsers 覆写在 DRF initialize_request 时序下 self.request 恒 None 的崩溃 bug（改类级 parser_classes）；清理 knowledge 死代码（IndexInfoSerializer 零消费方删除 + views.py 重导出垫片删除、urls.py 直接导入子模块） |
| dj-17 | P3 | core/signals.py 模块级 try/except 注册信号（失败静默跳过），与 analytics 的 ready() 模式不一致，迁移期可能拿不到模型 | backend/Django_xm/Django_xm/apps/core/signals.py:49<br>backend/Django_xm/Django_xm/apps/core/signals.py:68<br>backend/Django_xm/Django_xm/apps/analytics/signals.py:202 | 统一到 AppConfig.ready() 注册，用字符串 sender 替代 get_model，删除吞错 try/except<br>**[✅已修复 2026-08-20 · spec:c2-model-migration-consistency]** celery_task_record_post_save 改字符串 sender "core.CeleryTaskRecord"，删 try/except + get_model 吞错块；CoreConfig.ready() 显式 import core.signals 激活（根因：此前靠其他 app import 副作用激活的隐性依赖一并消除） |
| dj-18 | P3 | ChatMessage 宽表承载 10 个 JSONField 且 null=True 与 default 并存，NULL 判断与空容器判断双轨 | backend/Django_xm/Django_xm/apps/chat/models.py:77<br>backend/Django_xm/Django_xm/apps/chat/models.py:88<br>backend/Django_xm/Django_xm/apps/chat/models.py:96 | 短期统一去掉 null=True（迁移时 NULL 归一为空容器）；长期将 versions/suggestions 拆子表或文档存储<br>**[✅已修复 2026-08-20 · spec:c7b3-data-model-consolidation]** ChatMessage 7 个 JSONField 与 ChatSession.selected_knowledge_bases 去 null=True（default=dict/list 生效）；chat/0002 迁移 NULL 归一 RunPython 先于 AlterField（存量 NULL → 空容器）；18 处双轨读点 `or []`/`or {}` 死防御清除（views_chat/realtime_events/writeback/approval_service/stream_persistence/research views/session_executor，`reasoning or None` 显示转换保留）；新增 4 测试（字段默认值 + payload 无 null） |
| dj-19 | P3 | ContextRule 的 organization 作用域绑定单一 user 且级联删除，全局共享语义与数据生命周期矛盾 | backend/Django_xm/Django_xm/apps/context_manager/models.py:33<br>backend/Django_xm/Django_xm/apps/context_manager/models.py:39<br>backend/Django_xm/Django_xm/apps/context_manager/models.py:44 | user 改 null=True + SET_NULL（或 PROTECT），organization 行 user 置 NULL；查询按 scope=organization OR user 组合 |
| dj-20 | P3 | dev.py 在 settings 导入阶段执行 Redis ping（模块导入副作用），Redis 不可达时启动固定延迟 3 秒 | backend/Django_xm/Django_xm/settings/dev.py:17<br>backend/Django_xm/Django_xm/settings/dev.py:20 | **✅ 已完成（2026-08-19，spec:b2-exception-hardening）**：删除启动 ping 探测与 REDIS_AVAILABLE 变量（全仓零残留），CHANNEL_LAYERS 固定 RedisChannelLayer（删除 InMemoryChannelLayer 静默降级分支——该降级会导致多进程 WebSocket 互不可见），依赖 channels_redis 内建连接池惰性建连 + 自动重连（socket_keepalive/health_check_interval/retry_on_timeout 已配置） |
| dj-21 | P3 | 迁移碎片化：chat 20 个迁移字段反复增删（is_streaming 先删后加、plan/chain_of_thought 删后加回），approvals 13 个、learning 10 个 | backend/Django_xm/Django_xm/apps/chat/migrations/0016_chatmessage_chain_of_thought_chatmessage_plan.py:1<br>backend/Django_xm/Django_xm/apps/chat/migrations/0017_add_is_streaming.py:1<br>backend/Django_xm/Django_xm/apps/chat/migrations/0014_remove_chatmessage_is_streaming_and_more.py:1 | 大版本前执行 squashmigrations（chat/approvals/learning 优先）；日常约束一个 feature 一个迁移<br>**[✅已修复 2026-08-20 · spec:c2-model-migration-consistency]** 三 app 各收敛为单 0001_squashed（20→1/13→1/10→1，删 43 旧文件）；RunPython 函数内联（修 Django 生成的数字模块非法引用）；修复 approvals 5 个 RenameIndex 与 PG deferred CREATE INDEX 时序冲突（折叠进 CreateModel 终态索引名）；存量库 replaces 识别无需重置，全新测试库走 squashed 全链路 87 测试 OK |
| dj-22 | P3 | 附件模块 choices 风格不统一：裸元组列表与 TextChoices 混用（ACTION_CHOICES/LEVEL_CHOICES/STATUS_CHOICES） | backend/Django_xm/Django_xm/apps/attachments/models.py:100<br>backend/Django_xm/Django_xm/apps/attachments/models.py:134<br>backend/Django_xm/Django_xm/apps/attachments/models.py:15 | 重构为 models.TextChoices 子类（与同文件 AttachmentStatus 对齐），迁移仅 AlterField 无需回填<br>**[✅已修复 2026-08-20 · spec:c2-model-migration-consistency]** 全仓收敛而非仅附件：attachments 3 处（CleanupAction/AlertLevel/AlertStatus）+ tools 3 处（ToolStatus/Transport/Mode）+ approvals 3 处（Approval.Source/State、ApprovalOutboxEntry.State，含全仓 18 文件 175 处调用点 100% 机械替换）；TextChoices 与原元组 deconstruct 等价，零迁移零数据影响，全仓 `_CHOICES = [` 零残留 |

---

## 4. 维度二：LangChain 集成与最佳实践（8/10，18 条）

### 4.1 小结

项目对 LangChain/LangGraph 新版生态的采用程度高：requirements 锁定 langchain>=1.2.13/langgraph>=1.1.3/deepagents>=0.1.0，全后端未发现 LLMChain/initialize_agent/ConversationChain 等废弃 API（chat_service.py 中的 AgentExecutor 为项目自研韧性执行器，非 langchain 经典类），Agent 构建统一走官方 create_agent/create_deep_agent + AgentMiddleware + interrupt，模型初始化统一 init_chat_model，checkpointer 采用官方 PostgresSaver/AsyncSqliteSaver 并自研 Redis 读缓存装饰器，分层容错（SDK 重试→执行器退避重试→模型 fallback→工具降级→无工具直答）落实完整。主要短板集中在审批门控完整性（子代理构建器未注入审批中间件、无策略工具含 MCP 动态工具默认放行）与 shell_exec 白名单的前缀匹配绕过（python manage.py shell -c / find -delete 可自动放行），以及若干死代码、四份重复的 RAG 查询实现和约 205KB 的自研记忆子系统带来的维护负担。综合评分 8/10：官方模式对齐度高、工程质量好，但安全边界存在可验证的缺口。

### 4.2 P0 发现详述

#### lc-01【P0】shell_exec 白名单前缀匹配可被绕过，实现任意代码执行/文件删除自动放行

**问题描述**：白名单判定采用「首词/多词前缀匹配 + 无重定向管道即放行」的宽松逻辑：「python manage.py」是白名单前缀，而 Django 的 manage.py shell 支持 -c 参数，因此「python manage.py shell -c "import os; os.system(...)"」这类任意代码执行命令会被判定为 SAFE 级自动通过（不含 > | 等操作符）；同理 Unix 白名单命令「find」携带 -delete/-exec 参数、「curl」携带 -o 写文件或访问内网地址（SSRF）均不在 BLOCKED_PATTERNS 覆盖范围内，全部绕过审批直接执行。白名单的语义（安全命令免审批）被前缀匹配破坏。**这是全项目唯一的 P0 发现，为可验证的攻击路径。**

**代码位置**：
- backend/Django_xm/Django_xm/apps/tools/langchain/shell.py:42
- backend/Django_xm/Django_xm/apps/tools/langchain/shell.py:252
- backend/Django_xm/Django_xm/apps/tools/langchain/shell.py:275
- backend/Django_xm/Django_xm/apps/tools/langchain/shell.py:90

**改进建议**：白名单从「前缀匹配」收紧为「命令 + 子命令枚举」：python 类白名单仅允许 manage.py 的只读子命令集合（help/showmigrations/check 等）并显式拒绝 shell/test/dbshell；find/curl/ping 从白名单移除或对其参数做二次校验（拒绝 -delete/-exec/-o/--output/-d 等）；BLOCKED_PATTERNS 补充 find -delete/find -exec、curl -o 等模式。可引入 shlex 分词后逐 token 校验，而非正则前缀。

**证据**：

```python
DEFAULT_WHITELIST_COMMANDS 含 "python manage.py"
_is_command_whitelisted: cmd_stripped.startswith(wl_cmd + " ") → return not _has_redirect_or_pipe(cmd_stripped)
BLOCKED_PATTERNS 无 find/curl 相关条目
```

### 4.3 P1 发现详述

#### lc-02【P1】审批门控不完整：SubAgentBuilder 构建的三类子代理未注入 ApprovalMiddleware

**[✅已修复 2026-08-19 · spec:unify-context-injection · 含勘误]** 审批注入下沉至 `agent_hub/middleware.py build_middleware` 统一收敛点，幂等保底注入 ApprovalMiddleware + ContextManagerMiddleware（任何 builder 无法遗漏）；config.py 三个死类型表项补 context_management。**勘误：原「子代理绕过审批」实为死路径——子代理统一以 AgentType.BASE 构建经 BaseAgentBuilder 本就有审批，前端审批正常系在役行为，本条原描述的安全缺口不成立，收敛点改造属防御性加固**；附带 deep_dynamic_context_enabled 开关（默认 False）与 StateGraph 评估结论（learning/plan_execute 暂不接入）。

**问题描述**：ensure_approval_middleware 仅在 base_builder.py 与 deep_builder.py 中调用（且注释明确「审批机制是核心安全能力，应对所有有工具的 agent 强制启用」），而 subagent_builder.py 构建的 WEB_RESEARCHER/DOC_ANALYST/REPORT_WRAPPER 子代理只调用 build_middleware（CapabilityRegistry 路径），capabilities 模块中无任何 ApprovalMiddleware 构建逻辑。经 spawn_sub_agent 派生的子代理若携带写操作工具，其工具调用将完全绕过审批拦截。

**代码位置**（修复后现状）：
- backend/Django_xm/Django_xm/apps/agent_hub/middleware.py:8（build_middleware 收敛点，83-97 保底注入）
- backend/Django_xm/Django_xm/apps/agent_hub/config.py:29（AGENT 类型中间件表项，33-35 为原三个死类型补 context_management）
- backend/Django_xm/Django_xm/apps/agent_hub/builders/subagent_builder.py:26（经收敛点 build_middleware 构建）
- backend/Django_xm/Django_xm/apps/agent_hub/builders/base_builder.py:36（收敛点之上的显式双保险注入）

**改进建议**：在 subagent_builder._build_internal 中与 base_builder 对齐调用 ensure_approval_middleware；更彻底的做法是把审批注入下沉到 AgentFactory.create 或 build_middleware 统一入口，使任何 builder 无法遗漏，而不是依赖各 builder 自觉调用。

**证据**：

```
grep ensure_approval_middleware 全仓仅 base_builder.py:39 与 deep_builder.py:225 两处调用；
subagent_builder.py 全文无 approval 相关代码
```

#### lc-03【P1】审批策略 fail-open：未注册策略的工具（含全部 MCP 动态工具）默认免审批放行

**[⏸用户决定不解决 2026-08-19]** 保持现状，不再列入整改计划。

**问题描述**：ApprovalMiddleware.aafter_model 中 `policy = self.policies.get(tool_name)` 为 None 时直接 continue（不审批、不审计），_default_policies 是按 tool_name 静态注册的白名单。MCP server 提供的工具名是动态的（如 sequentialthinking、resolve-library-id），永远不在注册表内，因此所有 MCP 工具调用绕过审批门控；未来新增的内置工具同样默认无审批。安全边界应为默认受控（fail-closed），而非默认放行。

**代码位置**：
- backend/Django_xm/Django_xm/apps/agent_hub/approval/middleware.py:742
- backend/Django_xm/Django_xm/apps/agent_hub/approval/middleware.py:776
- backend/Django_xm/Django_xm/apps/agent_hub/approval/middleware.py:69

**改进建议**：反转默认策略：未知工具名（含 MCP 工具，可通过 metadata.is_mcp_tool 识别）默认评 CONTROLLED 进入审批批次，再为确属只读的工具显式注册 SAFE 策略；MCP server 接入时可按 server 维度配置风险等级。至少为 MCP 工具提供基于工具名前缀/元数据的策略匹配器。

**证据**：

```
middleware.py:743-776：policy is None → 注册 ctx + publish_pending → continue # 无策略，不审批
```

### 4.4 P2/P3 发现明细

| ID | 优先级 | 问题 | 位置 | 建议 |
|----|--------|------|------|------|
| lc-04 | P2 | 审批中间件注入失败时 fail-open：仅告警后继续创建无审批 agent，与 deep_builder 的 fail-fast 断言标准不一致 | backend/Django_xm/Django_xm/apps/agent_hub/builders/base_builder.py:36<br>backend/Django_xm/Django_xm/apps/agent_hub/builders/base_builder.py:74 | 有工具的 agent 注入失败应 fail-fast（抛 AgentCreationError）或降级为无工具 agent；base_builder 对齐 deep_builder 直接调用 |
| lc-05 | P2 | 死代码且双重错误：create_langgraph_with_mcp_checkpointer 导入不存在的模块（checkpoint_factory 实为 checkpointer_factory）并传错误参数，调用即抛 ImportError/TypeError | backend/Django_xm/Django_xm/apps/agent_hub/mcp_integration.py:113<br>backend/Django_xm/Django_xm/apps/agent_hub/mcp_integration.py:120 | 删除该函数及 __all__ 导出；若保留则修正导入与参数（thread_id 经 config.configurable 传递） |
| ~~lc-06~~ | P2 | ~~create_strict_rag_chain 死代码；实际查询走 query/aquery/astream/stream 四个手写函数，逻辑 90% 重复约 500 行~~ | ~~backend/Django_xm/Django_xm/apps/knowledge/services/strict_rag_chain.py~~ | **✅ 已完成（2026-08-19，spec: lc06-strict-rag-convergence）**：4 个死符号（aquery_strict_rag / create_strict_rag_chain / STRICT_RAG_SYSTEM_PROMPT / _get_chat_model）+ 4 个失效 import 删除，services/__init__.py 死 re-export 同步清理；三在役入口抽 `_maybe_rewrite_query` / `_retrieve_with_fallback` / `_handle_empty_result` sync+async 双版本共享内核（759 → 613 行，净减 146 行）；新增 17 个行为等价性测试逐分支锁定（含 sync/async 降级传参差异、事件序列、HyDE 语义），全量 210/210 通过 |
| lc-07 | P2 | Prompt 外置化不完整：TOOL_USAGE_INSTRUCTIONS 约 90 行硬编码且内部两章节规则完整重复；各 RAG 提示词分散硬编码 | backend/Django_xm/Django_xm/apps/ai_engine/prompts/system_prompts.py:90<br>backend/Django_xm/Django_xm/apps/ai_engine/prompts/system_prompts.py:144<br>backend/Django_xm/Django_xm/apps/ai_engine/prompts/system_prompts.py:162<br>backend/Django_xm/Django_xm/apps/agent_hub/prompts.py:3 | 合并重复章节；工具指令与各 RAG 提示词统一迁入 prompts.yaml，经加载器统一读取 |
| lc-08 | P2 | get_system_prompt 用 str.format 渲染 YAML 模板，花括号字面量（JSON 示例等）会引发 KeyError 或误替换 | backend/Django_xm/Django_xm/apps/ai_engine/prompts/system_prompts.py:51<br>backend/Django_xm/Django_xm/apps/ai_engine/prompts/system_prompts.py:55 | 改用 PromptTemplate/ChatPromptTemplate.from_template + partial_variables 注入；或约定唯一占位符并加载时校验 |
| lc-09 | P2 | agent_hub 与 research 自认的循环依赖，靠延迟导入规避；AgentFactory 对 deep_builder 的 ImportError 仅告警，DEEP_RESEARCH 类型静默不可用 | backend/Django_xm/Django_xm/apps/agent_hub/builders/deep_builder.py:26<br>backend/Django_xm/Django_xm/apps/agent_hub/builders/deep_builder.py:36<br>backend/Django_xm/Django_xm/apps/agent_hub/factory.py:43 | 构建参数与韧性回调协议抽象到 agent_hub 侧接口，适配器移入独立模块（或 cross_app 门面），消除 ImportError 容忍分支 |
| lc-10 | P2 | 自研记忆子系统规模过大（17 模块约 205KB），与官方 SummarizationMiddleware/trim_messages/Store 能力重叠度高，测试覆盖与维护成本高 | backend/Django_xm/Django_xm/apps/context_manager/services/compression.py<br>backend/Django_xm/Django_xm/apps/context_manager/services/manager.py<br>backend/Django_xm/Django_xm/apps/context_manager/services/hierarchical_memory.py<br>backend/Django_xm/Django_xm/apps/context_manager/services/progressive_compressor.py | 能力映射审计：消息裁剪/摘要迁移官方实现，自研收缩到差异化能力；提供官方/自研双实现开关逐步收敛 |
| lc-11 | P2 | RedisCachedCheckpointer.adelete_thread 不清 Redis，TTL 窗口（默认 1800 秒）内已删会话 checkpoint 仍可读，删除语义不一致 | backend/Django_xm/Django_xm/apps/ai_engine/services/redis_cached_checkpointer.py:121<br>backend/Django_xm/Django_xm/apps/ai_engine/services/checkpointer_factory.py:588 | 按 thread_id 前缀扫描删除 Redis key，或维护 thread→checkpoint_id 索引 set 逐个删除；失败记审计日志 |
| lc-12 | P2 | plan_execute 工作流内 plan 节点用 with_structured_output，reflect 节点却手写 JSON 解析（花括号截取 + 双重回退），解析失败静默返回 {} | backend/Django_xm/Django_xm/apps/ai_engine/workflows/plan_execute.py:18<br>backend/Django_xm/Django_xm/apps/ai_engine/workflows/plan_execute.py:40<br>backend/Django_xm/Django_xm/apps/ai_engine/workflows/plan_execute.py:145 | 为 reflect 定义 Pydantic 模型并复用 get_structured_model_with_fallback，删除 _parse_json_response |
| lc-13 | P3 | LLM 语义缓存使用 langchain_community.RedisSemanticCache（同步、维护模式），非官方推荐 langchain-redis | backend/Django_xm/Django_xm/apps/ai_engine/services/llm_cache.py:113<br>backend/Django_xm/Django_xm/apps/ai_engine/services/llm_cache.py:122<br>backend/Django_xm/Django_xm/apps/ai_engine/services/llm_factory.py:706 | 引入 langchain-redis 切换 RedisCache/RedisSemanticCache（异步复用连接池）；短期补容量上限与失效策略说明 |
| lc-14 | P3 | langchain_classic retrievers 依赖 5 处（ContextualCompressionRetriever/MultiQueryRetriever/ParentDocumentRetriever/InMemoryStore），属官方过渡路径 | backend/Django_xm/Django_xm/apps/knowledge/services/rag_retrieval.py:44<br>backend/Django_xm/Django_xm/apps/knowledge/services/retrieval_service.py:157<br>backend/Django_xm/Django_xm/apps/knowledge/services/retrieval_service.py:371<br>backend/Django_xm/Django_xm/apps/knowledge/services/retrieval_service.py:377 | 跟踪官方迁移及时切换；ParentDocumentRetriever 可评估 InMemoryVectorStore + 自维护 docstore 轻量替代 |
| lc-15 | P3 | 跨模块导入私有函数：approval/policies.py 依赖 shell.py 的 _is_command_blocked/_is_command_whitelisted；strict_rag_chain 依赖 retrieval_service 私有降级函数 | backend/Django_xm/Django_xm/apps/agent_hub/approval/policies.py:156<br>backend/Django_xm/Django_xm/apps/knowledge/services/strict_rag_chain.py:290<br>backend/Django_xm/Django_xm/apps/knowledge/services/strict_rag_chain.py:410 | 黑白名单判定提升为公开函数（is_command_safe 等），检索降级三件套提升为公开 API 并补单测锁定契约 |
| lc-16 | P3 | 对第三方类运行时 monkey-patch（ChatGroq bind_tools 字段补丁），与库版本强耦合，上游修复后补丁可能反噬 | backend/Django_xm/Django_xm/apps/ai_engine/services/llm_factory.py:583<br>backend/Django_xm/Django_xm/apps/ai_engine/services/llm_factory.py:632 | 补丁前检测官方是否已具备可用 bind_tools；环境变量开关包裹便于一键禁用；跟踪上游修复后移除 |
| lc-17 | P3 | 子代理默认提示词为英文硬编码（web_researcher/doc_analyst/report_writer），与全站中文语境不一致且未纳入外置体系 | backend/Django_xm/Django_xm/apps/agent_hub/builders/subagent_builder.py:52 | 三个子代理提示词中文化并迁入 prompts.yaml，与深研子代理统一管理 |
| lc-18 | P3 | 审批超时以 ToolMessage 反馈让 agent 自行继续，缺少同一工具反复「审批→超时→重试」的累计限制，存在长时占用窗口 | backend/Django_xm/Django_xm/apps/agent_hub/approval/timeout_handler.py:38<br>backend/Django_xm/Django_xm/apps/agent_hub/approval/timeout_handler.py:46<br>backend/Django_xm/Django_xm/apps/agent_hub/approval/middleware.py:972 | 每 thread 维护超时决策计数：同一工具第 2 次超时注入终止性反馈，或接入 tool_usage_guard 触发任务终结 |

---

## 5. 维度三：代码质量综合评估（7/10，13 条）

### 5.1 小结

后端核心代码风格与工程化水平较高：抽查的 chat/agent_hub/approvals/ai_engine 核心文件 snake_case 一致、类型注解与 docstring 覆盖率高、统一 logging（print 仅迁移脚本 1 处、无裸 except）、异常分类体系（classify_exception）与审批状态机设计清晰，drf-spectacular 已配置且视图层广泛使用 extend_schema。前端 63 个 .vue 全部 `<script setup>`、URL query 参数全部经 getQueryParam 隔离访问、错误码映射仅在 apiErrorHandler.js 单点定义、sessionTransformers.js 使用 Object.prototype.toString.call 严格类型判断，命名边界规范执行良好。主要短板：测试基础设施缺失（前端 8 个测试文件无 vitest 依赖与 test 脚本、无法运行；后端仅 11 个测试文件，context_manager 15 个服务模块、knowledge、learning 等关键路径零测试）；存在多处复杂度热点（session.js 1731 行、createHandleSessionEvent 约 663 行闭包、chat_service 4 个超百行函数、15 个超 500 行组件）；types/index.js 违规导出 7 个工具函数且与 zod 校验双轨并存；项目文档几乎缺失（根/后端/前端 README 均不存在）。

### 5.2 P1 发现详述

#### cq-01【P1】前端测试文件存在但无法运行：缺 vitest 依赖与 test 脚本

**问题描述**：utils/__tests__/ 下有 8 个测试文件（sessionTransformers/toolCallStateMachine/orderedQueue/subagentAggregation/messageOperations/inlineContent 等，含 vite-alias-loader.mjs、logger.stub.mjs 辅助文件），但 package.json 无 test 脚本、devDependencies 无 vitest、项目根无 vitest.config.*。测试写了却不在构建/验证链路中，极易腐烂为僵尸测试。

**代码位置**：
- frontend/langchain-vue/package.json:6-14
- frontend/langchain-vue/src/utils/__tests__/

**改进建议**：安装 vitest（及 @vue/test-utils 如需组件测试），添加 vitest.config.js 与 `"test": "vitest run"` 脚本，并接入 lint/CI 流程。

**证据**：

```
scripts 仅 dev/build/preview/lint/format；devDependencies 无任何测试框架。
```

#### cq-02【P1】后端测试覆盖严重不均：context_manager 等 5 个 app 零测试，审批状态机仅 1 个场景

**问题描述**：全后端仅 11 个测试文件（approvals 1、chat 1、agent_hub 1、ai_engine 1、research 4、tools 1、fastapi_service 2），无 conftest.py。context_manager 的 15 个服务模块（压缩、知识图谱、断路器、token 预算等）、knowledge 全部检索/向量库服务、learning/nodes 学习流、analytics 零测试。approvals 仅覆盖 batch_resume 批次完整性场景，request/resume/timeout/complete/reject 全状态流转、SSE 流持久化链路（stream_persistence 仅 _persist 部分）均无系统性测试。

**代码位置**：
- backend/Django_xm/Django_xm/apps/context_manager/services/（无 tests 目录）
- backend/Django_xm/Django_xm/apps/knowledge/services/（无测试）
- backend/Django_xm/Django_xm/apps/learning/nodes/（无测试）
- backend/Django_xm/Django_xm/apps/approvals/tests/test_approval_batch_resume.py:1-15

**改进建议**：优先补齐：approval 状态机全流转参数化测试、stream_persistence 持久化链路测试、context_manager 压缩/断路器单测（纯逻辑易测）、retrieval_service 降级链路测试；建立 conftest.py 公共 fixture。

**证据**：

```
Glob test_*.py 全量仅 11 个文件；context_manager 无 tests 目录。
```

#### cq-03【P1】复杂度热点：后端超长函数集中于 chat 分发与流持久化链路 **[✅已修复 2026-08-20 · spec:c4-split-long-functions]** 6 个超百行函数全部拆分达标（≤80 行）：chat_service.py 四方法（_process_normal_stream_chat 535→38 行编排 + 17 个阶段方法 + 2 个 dataclass、_dispatch_by_mode 204→34、process_chat_request 130→12、_create_agent_for_mode 113→11）；stream_persistence.py（_persist_to_db_sync 161→62、persist_stream_result 112→74、_evolve_tool_call 85→70，拆出 10 个职责单一函数）+ 新增 39 个直接单元测试（此前零覆盖）；deep_builder.py（_build_internal 143→30 行编排 + 6 个私有方法）。行为保持（事件序列/DB 写入/异常语义零变化），闭包捕获显式化（rebuild_agent_fn 工厂化仅捕获 holder、子代理回调收敛为实例方法）。顺带删除不可达死分支 28 行（elif selected_knowledge_base 恒不可达证明）+ _ce_metadata 死赋值 2 处。回归：后端 378/378（339+39 新增）+ check + makemigrations --check + lint-imports 三契约零豁免 + 前端 vitest 127/127 全绿

**问题描述**：chat_service.py 4 个函数超 100 行（_dispatch_by_mode 约 208 行、_process_normal_stream_chat 约 200 行、process_chat_request 约 130 行、_create_agent_for_mode 约 113 行）；stream_persistence.py 的 _persist_to_db_sync 约 163 行；deep_builder.py 的 _build_internal 约 144 行。均属最高变更频率的核心路径，长函数叠加审批中断/子代理挂起等分支，回归风险高。

**代码位置**：
- backend/Django_xm/Django_xm/apps/chat/services/chat_service.py:193,404,612,729
- backend/Django_xm/Django_xm/apps/chat/services/stream_persistence.py:268,431
- backend/Django_xm/Django_xm/apps/agent_hub/builders/deep_builder.py:181

**改进建议**：按模式分派（normal/deep/rag）拆为独立方法或策略类；_persist_to_db_sync 按字段组（正文/工具调用/审批/token）拆分；配合 cq-02 补测试后再重构。

#### cq-04【P1】前端超大文件：session store 1731 行、事件处理闭包 663 行 **[✅已修复 2026-08-20 · spec:c5-split-frontend-stores]** session.js（实测 1700 行）拆为 stores/session/ 领域切片组合（index 组装 + sessionList 394/sessionCrud 247/messageFlow 408/versionFlow 256/toolCallState 537/helpers 35，五切片各自持有 state 与 actions，跨切片循环调用经 runtimeDeps 运行时注册表解决，公共 API/store id/import 路径完全不变，41 处消费点零修改）；handleSessionEvent.js（实测 707 行）收敛为 297 行纯路由（审批 handler 220 行下沉 approvalHandler.js、子代理/研究状态事件下沉 subagentHandlers.js、stream_reasoning 并入 streamStateHandlers.js），`subagent_thread_id` 协议标识符还原逻辑原样保留；新增 approvalHandler 38 + subagentHandlers 23 共 61 个直接单测（此前零覆盖）；顺带删除 subagent_status_change 恒真兜底析取（payload.subagentThreadId 已由主文件注入点统一还原）。执行中按用户指令"不要为了压缩而压缩"修正：approvalHandler 初版为凑单函数 80 行把主流程硬拆三段辅助（徒增参数传递层），已内联还原原版连续形态（162 行）；toolCallState 537 行超 450 指标但职责纯粹（工具调用状态域全部真实成员，行数主要来自 docstring），保持单一职责完整性不硬拆。回归：前端 vitest 188/188（127+61）+ build + lint 三绿；后端 378/378 无波及

**问题描述**：stores/session.js 达 1731 行（虽已抽 messageOperations 到 utils，仍承载会话/消息/版本/流状态多职责）；stores/sync/handleSessionEvent.js 的 createHandleSessionEvent 为约 663 行巨型工厂闭包，内部嵌套全部 session 事件分支；chat.js 558 行、sync.js 532 行。事件类型继续增长时维护成本线性上升。

**代码位置**：
- frontend/langchain-vue/src/stores/session.js:1-1731
- frontend/langchain-vue/src/stores/sync/handleSessionEvent.js:49-712
- frontend/langchain-vue/src/stores/chat.js:1-558

**改进建议**：session.js 按领域继续拆（消息版本流、会话元数据、流状态）；handleSessionEvent 内各事件 case 下沉为独立 handler 模块（参照已有 toolCallHandler.js 模式）。

### 5.3 P2/P3 发现明细

| ID | 优先级 | 问题 | 位置 | 建议 |
|----|--------|------|------|------|
| cq-05 | P2 | 15 个组件超 500 行（最大 DeepResearchView 1377 行），视图层普遍混入轮询/搜索/对话框等多职责 | frontend/langchain-vue/src/views/DeepResearchView.vue:1-1377<br>frontend/langchain-vue/src/views/WorkflowView.vue:1-1164<br>frontend/langchain-vue/src/components/chat/ToolSelector.vue:1-1114<br>frontend/langchain-vue/src/components/chat/ChatMessage.vue:1-1055 | 轮询抽 usePolling composable、对话框抽子组件；优先拆 DeepResearchView/WorkflowView 两个千行级视图<br>**[✅4 个千行级已修复 2026-08-20 · spec:c6-split-giant-components]** ToolSelector 1114→574+三 tab 子组件（契约不变）；DeepResearchView 1377→1175+两 composables（实例安全）+ContinueResearchDialog+死样式 135 行删除；WorkflowView 1303（实测）→1036+两 composables+api 死方法 6 个删除；**ChatMessage 变更锁死零改动**（1188 行中样式占 558 行/已委托 8 子组件/高频渲染路径，拆分无收益有风险）——"不为拆分而拆分"组件级落地；前端 188/188 三绿 |
| cq-06 | P2 | types/index.js 违规导出 7 个工具函数（规范禁止 types 导出工具），且与 zod 校验双轨并存存在漂移风险 | frontend/langchain-vue/src/types/index.js:169-219<br>frontend/langchain-vue/src/utils/validation.js:3-24 | 工厂函数迁到 utils/messageOperations.js（或 messageFactory.js）；校验统一收敛到 zod schema 一处<br>**[✅已修复 2026-08-20 · spec:converge-validation-zod]** 实测"7 个工具函数违规导出"已被前序批次（messageOperations 抽离）顺带解决——types/index.js 现为 187 行纯枚举+JSDoc typedef 零函数导出，本批以顶部注释固化"禁止导出工具函数"约束；剩余双轨收敛：validateMessage 重写为 MessageSchema.safeParse 包装（zod 单一出口，签名/返回语义不变，唯一消费点 ChatMessages.vue 零修改）+ ChatRequestSchema 内联 role 字面量改引用 MessageRole 枚举源（消除两处维护）；前端 188/188 + build + lint 三绿 |
| cq-07 | P2 | 双异常体系并存（LCAgentException 与 AgentHubError）无共同基类；classify_exception 含静默 ImportError 降级 | backend/Django_xm/Django_xm/apps/ai_engine/services/exceptions.py:31,191-192,240-241<br>backend/Django_xm/Django_xm/apps/agent_hub/exceptions.py:1-30 | 引入项目级公共异常基类（core/exceptions.py 的 BaseAppError）两体系继承；ImportError 分支加 logger.debug<br>**[✅已修复 2026-08-20 · spec:c1-lightweight-convergence]** 新建 core/exceptions.py BaseAppError 项目级基类，LCAgentException 与 AgentHubError 双体系继承；knowledge 等新异常体系同步继承；classify_exception 静默 ImportError 降级补 logger 警告日志 |
| cq-08 | P2 | SSE/WS 命名转换调用点散布 9+ 处且各点手动还原 type 字段，新增消费点漏还原风险持续存在 | frontend/langchain-vue/src/utils/sse.js（入口未内置转换）<br>frontend/langchain-vue/src/views/RagView.vue:606-608<br>frontend/langchain-vue/src/composables/useRealtimeSync.js:944-947<br>frontend/langchain-vue/src/views/DeepResearchView.vue:768-776 | 在 sse.js 的 readSSEStream 与 WS 入口封装 parseProtocolEvent(raw)（内置 toCamelCase + type 还原），消费点只拿解析后对象<br>**[✅已修复 2026-08-19 · spec:b1-low-risk-convergence（Task 3）· 2026-08-20 核验补标]** parseProtocolEvent 已建立于 sse.js:173（toCamelCase + type/subagent_thread_id 协议标识符还原 + Object.prototype.toString 严格判断 + 规范文档注释模板）；readSSEStream 主循环/尾部缓冲双点内置；DeepResearchView/WorkflowView/RagView/useResearchApprovalListener/useWorkflowExecution 消费点手动转换全删；useRealtimeSync WS 入口统一调用；8 个单测覆盖 |
| cq-09 | P2 | 项目文档缺失：根/后端/前端三级 README 均不存在，前端无 .env.example；现有 00_总览与导读.md 为微调学习手册内容与项目无关 | d:/programming/langchain/langchain_xm/（无 README.md）<br>backend/（无 README.md）<br>frontend/langchain-vue/（无 README.md、无 .env.example）<br>frontend/langchain-vue/00_总览与导读.md:1-30 | 补根 README（架构图+快速启动）、前后端各自 README（目录/环境变量/常用命令）；补前端 .env.example<br>**[✅已修复 2026-08-19 · spec:add-regression-test-infra · 2026-08-20 复审补标]** 根 README.md/backend README.md/.env.example 前后端均在位（磁盘确认） |
| cq-10 | P3 | axios 响应拦截器对每个响应做双重 JSON.stringify 调试比较（截断 200 字符），大响应全量序列化两次且生产也执行 | frontend/langchain-vue/src/api/axios.js:69-74 | 调试比较包在 import.meta.env.DEV 判断内，或删除<br>**[✅已修复 2026-08-19 · spec:b1-low-risk-convergence（Task 1）· 2026-08-20 核验补标]** 调试比较已包裹 import.meta.env.DEV（axios.js:69-79，注释标注 cq-10），生产分支仅 toCamelCase 无序列化开销 |
| cq-11 | P3 | .vue 组件内散落 81 处 catch（DeepResearchView 13、RagView 9、WorkflowView 9），提示文案与重试策略不统一 | frontend/langchain-vue/src/views/DeepResearchView.vue（13 处）<br>frontend/langchain-vue/src/views/RagView.vue（9 处）<br>frontend/langchain-vue/src/views/WorkflowView.vue（9 处） | 高频模式（加载+错误提示）抽 useApiTask 类 composable，统一 loading/error/ElMessage 生命周期<br>**[✅已修复 2026-08-19 · spec:b3-prod-config-cache]** 新建 composables/useApiTask.js（loading/error 托管、代际 token 防重复、unmount 清理、取消不弹 toast）+ 16 单测；三视图 31 处迁移完成、旧样板零残留；回归中追加修复 5 个 keep-alive 视图 onMounted/onActivated 双钩子首挂双发（统一收敛 onActivated + 首挂标志），并合并 DeepResearchView 3 个等价 useApiTask 重复定义 |
| cq-12 | P3 | 检索降级路径使用原生 SQL（to_tsvector/to_tsquery），偏离「用 ORM 不写原生 SQL」规则（已防注入有注释，技术上合理），且无测试覆盖 | backend/Django_xm/Django_xm/apps/knowledge/services/retrieval_service.py:51-80 | 保持实现但补集成测试验证防注入；或在规则中登记 PGVector 全文检索为 ORM 例外项 |
| cq-13 | P3 | 后端 except Exception 宽捕获 40+ 处（多数有 logger），瞬时错误与代码缺陷同路径，可能掩盖真实 bug | backend/Django_xm/Django_xm/apps/attachments/views.py:59-291<br>backend/Django_xm/Django_xm/apps/attachments/admin.py:41-73 | 预期错误捕获具体异常类型，仅顶层兜底保留 Exception；结合统一异常基类收窄 |

---

## 6. 维度四：功能设计合理性（7/10，15 条）

### 6.1 小结

整体为 Django MTV + DRF + Channels + Celery + LangGraph 的分层架构，app 依赖层次为：**core**（零依赖底层：认证/限流/日志/软删除基类）→ **ai_engine**（AI 基础设施层：LLM 工厂/缓存/子代理运行时，被 7 个 app 依赖）→ **agent_hub / tools / knowledge / approvals**（中间能力层）→ **chat**（最上层业务编排，依赖 9 个 app，其中 ai_engine 43 处、cache_manager 18 处、knowledge 15 处）。亮点突出：审批体系（状态机 + Redis/DB 双写 + ApprovalOutboxEntry 事务性发件箱 + 超时注入 ToolMessage 不终结任务）完整支撑了跨浏览器一致性；前端 sync store 以 seq 单调序 + orderedQueue + replay 分块回放解决了乱序/去重/断线恢复；流式管线（loop/context/finalizer/interrupt/fallback/strategy）职责拆分清晰，SSE/WS 双通道已收敛为 WS 单通道；安全默认值良好（全局 IsAuthenticated + 分级限流 + 参数化 SQL + 附件 magic bytes 校验 + CORS 白名单）。短板：**app 级双向依赖普遍**（chat↔research、ai_engine↔agent_hub、ai_engine↔knowledge、agent_hub↪research 等），大量靠函数内延迟导入掩盖循环；chat 有上帝 app 化倾向（chat_service.py 超 1200 行）；生产配置缺 Redis channel layer；跨 app 门面（cross_app.py）覆盖不全，深层 services 与私有函数被直接导入；JWT 经 URL query 传输与 shell 工具宿主执行属于已知但未根治的风险面。

### 6.2 P1 发现详述

#### ds-01【P1】生产配置缺少 CHANNEL_LAYERS，多进程部署下实时推送静默失效 **[✅已修复 2026-08-19 · spec:b3-prod-config-cache]** CHANNEL_LAYERS（channels_redis + symmetric_encryption_keys）上移至 base.py 单一权威，dev.py 删除同款配置改为继承，test.py 保持 InMemoryChannelLayer 覆盖；prod 继承 base 即得 Redis 层。grep 确认全仓仅 base.py 定义 + test.py 覆盖

**问题描述**：base.py 与 prod.py 均未定义 CHANNEL_LAYERS（仅 dev.py 有条件配置 Redis 版）。生产使用 Django Channels 默认的 InMemoryChannelLayer：单进程内可用，但 Celery worker / FastAPI 执行服务（8001）发出的 channel_layer.group_send 无法到达 Web/WS 进程，异常仅被 logger.exception 吞掉。事件虽已通过 Lua 脚本原子持久化到 Redis（有 replay 兜底），但审批 pending/processing、工具调用状态、流式内容更新的实时推送会丢失，浏览器只能等重连 replay 补偿——直接违背「工具审批多浏览器一致」「模块间实时同步」的核心设计目标。

**代码位置**：
- backend/Django_xm/Django_xm/settings/prod.py:1-119
- backend/Django_xm/Django_xm/settings/dev.py:38-59
- backend/Django_xm/Django_xm/common/realtime_events.py:213-221

**改进建议**：在 prod.py 显式配置 channels_redis.RedisChannelLayer（与 dev.py 相同结构，Redis 为生产必选依赖）；为 group_send 失败增加指标上报（Prometheus counter），将「实时推送降级为 replay 兜底」变为可观测事件而非仅日志。

**证据**：

```
dev.py:38 "BACKEND": "channels_redis.core.RedisChannelLayer" ... prod.py 全文无 CHANNEL_LAYERS；
realtime_events.py:219 except Exception: logger.exception(f"[RealtimeEvents] group_send 失败: session={session_id}")
```

#### ds-02【P1】app 级双向依赖普遍，靠延迟导入掩盖循环，且存在跨 app 私有成员导入

**问题描述**：跨 app import 统计显示多组双向依赖：chat↔research（7/6 处）、agent_hub↔ai_engine（24/3 处）、ai_engine↔knowledge（9/13 处）、ai_engine↔context_manager（3/11 处）、agent_hub↔research（4/4 处）、context_manager↔knowledge（4/2 处）、attachments↔tools（8/4 处）。其中相当一部分反向依赖以函数内 from ... import 形式延迟导入（如 ai_engine/subagent_runtime/adapters/langgraph_adapter.py:104 运行时导入 agent_hub），模块加载期不报错但运行时依赖环依然存在，重构任何一侧都可能触发 ImportError。更严重的是封装穿透：research/services/writeback.py 跨 app 导入 chat 的私有函数 _merge_tool_calls_incremental（3 处），chat/services/rag_chat_service.py 导入 knowledge 的私有函数 _keyword_overlap，research/views.py:709 直接导入 chat.models.ChatMessage 绕过 cross_app 门面。

**代码位置**：
- backend/Django_xm/Django_xm/apps/ai_engine/subagent_runtime/adapters/langgraph_adapter.py:104
- ~~backend/Django_xm/Django_xm/apps/ai_engine/services/model_provider.py:25~~（已删除，spec: unify-llm-fallback-single-chain）
- backend/Django_xm/Django_xm/apps/research/services/writeback.py:404
- backend/Django_xm/Django_xm/apps/research/views.py:709
- backend/Django_xm/Django_xm/apps/chat/services/rag_chat_service.py:105
- backend/Django_xm/Django_xm/apps/agent_hub/builders/deep_builder.py:36

**改进建议**：① 将 _merge_tool_calls_incremental、_keyword_overlap 等被跨 app 复用的函数去掉下划线前缀并提升到所属 app 的公共门面（或 common/ 模块）；② 明确依赖方向规则（如 agent_hub → ai_engine 单向，ai_engine 不得反向导入 agent_hub，langgraph_adapter 的反向调用改为回调注入）；③ 用 import-linter（contracts 配置）在 CI 中固化分层契约，禁止新增延迟导入式循环。

**证据**：

```
统计（跨 app import 计数）：chat->ai_engine 43, knowledge->ai_engine 13, ai_engine->knowledge 9,
agent_hub->ai_engine 24, ai_engine->agent_hub 3, chat->research 7, research->chat 6,
agent_hub->research 4, research->agent_hub 4
```

### 6.3 P2/P3 发现明细

| ID | 优先级 | 问题 | 位置 | 建议 |
|----|--------|------|------|------|
| ds-03 | P2 | chat 非流式路径在同步视图中阻塞执行最长 120 秒（run_async + future.result），少量并发即耗尽 ASGI 线程池；run_async 每次新建一次性线程池 | backend/Django_xm/Django_xm/apps/chat/views_chat.py:410<br>backend/Django_xm/Django_xm/async_utils.py:14-34 | 非流式入口改 async def 视图直接 await；或引导前端走流式；run_async 复用模块级线程池 |
| ds-04 | P2 | shell 工具以 shell=True 在宿主机运行，白名单+黑名单正则属缓解而非隔离；白名单内 curl/ping 仍可携带任意参数（SSRF/数据外带）；Docker 沙箱默认关闭 | backend/Django_xm/Django_xm/apps/tools/langchain/shell.py:479<br>backend/Django_xm/Django_xm/apps/tools/langchain/shell.py:90-126<br>backend/Django_xm/Django_xm/settings/base.py:382-390 | 生产强制 SANDBOX_ENABLED=true（prod.py 断言）；白名单改「命令+参数解析器」模式（curl 限域名、git 限子命令）；路径校验从 cwd 扩展到参数 |
| ds-05 | P2 | JWT 经 URL query 参数传输（QueryParamTokenAuthentication 全局默认 + fetchSSE injectTokenQuery），token 进入访问日志/浏览器历史/Referer | backend/Django_xm/Django_xm/apps/core/authentication.py:14-47<br>frontend/langchain-vue/src/utils/sse.js:31-35<br>frontend/langchain-vue/src/api/research.js:17<br>frontend/langchain-vue/src/api/workflow.js:75 | 移除 fetchSSE 的 injectTokenQuery（fetch 已带 Authorization header）；query 认证仅挂确实无法带 header 的下载端点；日志脱敏 |
| ds-06 | P2 | chat app 上帝化：chat_service.py 约 1300 行含多路径编排；rag_chat_service.py 直接 import knowledge 的 6 个深层 service 模块绕过 cross_app 门面 | backend/Django_xm/Django_xm/apps/chat/services/chat_service.py:23-1274<br>backend/Django_xm/Django_xm/apps/chat/services/rag_chat_service.py:17-266 | chat_service 按模式拆分（与 stream/ 目录对齐）；knowledge 对 chat 的检索能力在 cross_app.py 建立正式门面，chat 只允许 import 门面 |
| ds-07 | P2 | 子代理父 Graph 唤醒回调仅存于进程内存（SubAgentLifecycleManager 单例），执行服务重启后挂起协程永久失联，且锁死水平扩展 | backend/Django_xm/Django_xm/apps/ai_engine/subagent_runtime/lifecycle.py:25-79<br>backend/Django_xm/Django_xm/apps/chat/services/stream/loop.py:317-328 | Redis 登记挂起关系（parent_thread_id → subagent_thread_ids + 时间戳），启动时扫描孤儿挂起按超时策略恢复；wait_for_subagent 增加最大挂起时长 |
| ds-08 | P2 | 同步阻塞调用混入异步执行生态：web_fetch 用 requests.get、ollama_vl_embedding 用 requests.post（4 处）、resilient_invoker 等多处 time.sleep 重试 | backend/Django_xm/Django_xm/apps/tools/langchain/web_fetch.py:61<br>backend/Django_xm/Django_xm/apps/ai_engine/providers/ollama_vl_embedding.py:74<br>backend/Django_xm/Django_xm/apps/agent_hub/services/resilient_invoker.py:357<br>backend/Django_xm/Django_xm/apps/learning/services/resilience.py:146<br>backend/Django_xm/Django_xm/apps/approvals/services/approval_service.py:1260 | 改用 httpx.AsyncClient；resilient_invoker 异步路径改 asyncio.sleep；CI 加 flake8-bugbear B011/ASYNC 规则扫描 |
| ds-09 | P3 | dev 环境 channel layer 以 SECRET_KEY 作对称加密密钥，双用途扩大泄露影响面且 dev/prod 行为不一致 | backend/Django_xm/Django_xm/settings/dev.py:52 | 生成独立 CHANNEL_LAYER_ENCRYPTION_KEY 环境变量；或可信内网直接关闭加密保持环境一致 |
| ds-10 | P3 | SVG 在附件上传白名单中，可内嵌 script/onload，若经 media 直出或下载端点未强制安全响应头即构成存储型 XSS | backend/Django_xm/Django_xm/apps/attachments/services/attachment_validation.py:37<br>backend/Django_xm/Django_xm/settings/base.py:99-100 | 下载端点统一 Content-Disposition: attachment + CSP sandbox；或 svg 服务端净化；确认生产不经 static() 直出 media |
| ds-11 | P3 | cross_app 门面覆盖不全：chat→ai_engine 43 处几乎全部直达深层模块、chat→knowledge 15 处仅 1 处走门面，门面沦为「新增代码的规范」 | backend/Django_xm/Django_xm/apps/chat/services/cross_app.py:1-148<br>backend/Django_xm/Django_xm/apps/chat/serializers.py:248<br>backend/Django_xm/Django_xm/apps/chat/services/chat_service.py:194-769 | 明确门面契约范围（模型访问必须走门面）；import-linter 或自定义 lint 在 CI 强制；高频深层导入收敛为本地薄包装 |
| ds-12 | P3 | 前端事件一致性三套顺序基线（lastSeq/expectedSeq/seqDedup）经 _advanceEventBaseline 联动，机制正确但认知负担大，依赖注释性知识传承 | frontend/langchain-vue/src/stores/sync.js:109-130<br>frontend/langchain-vue/src/stores/sync/orderedQueue.js<br>frontend/langchain-vue/src/stores/sync/seqDedup.js<br>backend/Django_xm/Django_xm/apps/chat/consumers.py:37-107 | 收敛为单一序号状态机（或独立内部包 + 乱序/重复/间隙/replay 并发测试矩阵）；开发模式加基线不变式断言 |
| ds-13 | P3 | grading_node 逐题循环 update 写库（批量改分场景 N+1 写），位于流式学习工作流内与事件广播交织 | backend/Django_xm/Django_xm/apps/learning/nodes/grading_node.py:260-262 | 收集 (question_id, fields) 列表后 bulk_update；或 values_list 预取再统一 update |
| ds-14 | P3 | run_async 每次调用新建一次性线程池（max_workers=1），高频调用产生线程 churn；timeout=120 硬编码无法按调用方调节 | backend/Django_xm/Django_xm/async_utils.py:30-32 | 模块级共享单线程 executor（Lock 保护初始化）；timeout 参数化由调用方传入 |
| ds-15 | P3 | ai_engine 基础设施层顶层 import 上层 agent_hub 的 ResilientModel，分层倒置风险且无 CI 防护（resilient_invoker 增加任何 ai_engine 顶层依赖即触发循环） | backend/Django_xm/Django_xm/apps/ai_engine/services/model_provider.py:25<br>backend/Django_xm/Django_xm/apps/ai_engine/subagent_runtime/adapters/langgraph_adapter.py:104<br>backend/Django_xm/Django_xm/apps/ai_engine/subagent_runtime/adapters/langgraph_adapter.py:565 | ResilientModel（通用模型韧性包装器）下沉到 ai_engine 自身或 common/；agent_hub 仅保留编排职责 |

---

## 7. 维度五：代码冗余与质量问题（6.5/10，12 条）

### 7.1 小结

项目业务代码的复用健康度中上：跨 app 的 cross_app.py 与 status_provider.py 经核实是职责不同的接口化解耦设计而非重复；learning 复用 agent_hub 韧性原语、llm_cache 采用 langchain 官方 InMemoryCache、前端 composables 全部有真实引用且注释中可见多处历史去重（如 useSubagentExpansion 共用）。主要问题集中在三处：requirements.txt 把大量传递依赖与零引用库（spacy 全家桶约 17 个包、tenacity、eventlet、tornado 等）固化为直接依赖；context_manager 存在 4 个零引用死模块（约 321 行）；get_logger 与 MODEL_LIMITS 各有两份平行实现且各自被半数代码引用，属于迁移不彻底。前端依赖全部有使用、常量管理整体规范，散落硬编码为个别点。可安全删除的代码规模约 350 行 Python + 30 行 JS，依赖清单可削减 40+ 条目。

### 7.2 P1 发现详述

#### rd-01【P1】requirements.txt 固化大量零引用与传递依赖（spacy 全家桶、tenacity 等 40+ 条目）

**问题描述**：逐个 Grep 后端全部 .py 确认零 import 的直接依赖：spacy、en_core_web_sm、blis、cymem、murmurhash、preshed、srsly、thinc、wasabi、weasel、catalogue、confection、spacy-legacy、spacy-loggers、langdetect（spacy 生态 17 包，安装体积与构建耗时巨大）；tenacity>=9.1.4（项目自写重试框架而未用它）；eventlet（celery worker_pool 实为 solo）；tornado；httpx-sse；aiohttp；RapidFuzz；bleach；Markdown。另有开发工具混入生产清单：ipython 及其传递依赖（backcall、jedi、parso、pickleshare、stack-data、traitlets、matplotlib-inline、pure_eval、executing、asttokens、decorator）、pipreqs/yarg/docopt、numba/llvmlite（spacy 附带）。flower 仅在 celery.py 注释提及未实际部署。同时存在反向缺失：asgi.py/consumers.py import channels 但 requirements.txt 未声明 channels/daphne，说明清单非受控生成。

**代码位置**：
- backend/Django_xm/requirements.txt:1-216
- backend/Django_xm/Django_xm/celery.py:60
- backend/Django_xm/Django_xm/asgi.py

**改进建议**：用 pipreqs 或 pip-tools 重新生成直接依赖清单：仅保留真实 import 的库（Django、DRF、langchain 系列、celery、redis、unstructured 等），spacy 全家桶删除；tenacity 决定去留（见 rd-07）；补声明 channels/daphne；开发工具（ipython、pipreqs）移入 requirements-dev.txt。

**证据**：

```
Grep 'import spacy|from spacy' → 0 匹配；'import tenacity' → 0 匹配；
'import eventlet|from eventlet' → 0 匹配；celery.py:60 'app.conf.worker_pool = "solo"'
```

**[✅已修复 2026-08-20 · spec:c2-requirements-rewrite]** requirements.txt 由 219 行（freeze 混直接依赖）重写为 **56 条直接依赖**：按「模块顶层 import / 配置引用 / 命名空间隐蔽 import / 已装降级路径」四类证据逐一判定；清除约 162 条（spacy 全家桶 16、jupyter/ipython 全家桶 22、开发运维工具 pipreqs/flower/eventlet/simpleui/tenacity、langchain-anthropic 零 import、langchain-milvus 当前未启用、全部传递依赖固化不保留）；同时补此前缺漏声明 fastapi/uvicorn/python-docx/duckduckgo-search/groq/chromadb/langchain-ollama/langchain-unstructured。新环境（Python 3.11.15 干净 venv）安装回归：pip check 无冲突 + check 0 issues + **318 测试全绿** + fastapi/五向量后端注册冒烟通过；三处版本修正（langgraph-store-postgres 移除——3.11 无版本且命名空间由 checkpoint-postgres 提供；redis 上界 <8——redis-py 8 RESP3 与 Redis 5 不兼容；deepagents 下限校准 >=0.7.5——原 0.1.0 允许装到无 0.7.x API 的早期版本，用户复核）。逐条证据与清除映射见 .trae/specs/c2-requirements-rewrite/verification-report.md。

#### rd-02【P1】context_manager 4 个零引用死模块（确认可删，约 321 行）

**[🕐部分执行 2026-08-19 · spec:analyze-context-manager-dead-modules]** 3 模块已物理删除，anti_oscillation 保留待集成，hierarchical_memory 待确认。

**问题描述**：【确认可删】anti_oscillation.py（51 行，AntiOscillationGuard）、lazy_loader.py（82 行，LazyLoader）、rule_engine.py（108 行，ContextRuleEngine）、context_lifecycle.py（80 行，ContextLifecycleManager）四个模块在全后端零 import：services/__init__.py 未导出它们，manager.py/middleware.py/hierarchical_memory.py 均不引用，模块名在全仓库无任何出现。判断依据：类名与模块名双维度 Grep 均仅命中定义文件自身。

**代码位置**（2026-08-19 复核现状）：
- backend/Django_xm/Django_xm/apps/context_manager/services/anti_oscillation.py:10（**保留**：结论为值得集成，待统一注入通道稳定后另立 spec）
- backend/Django_xm/Django_xm/apps/context_manager/services/lazy_loader.py:6（文件已删除 2026-08-19）
- backend/Django_xm/Django_xm/apps/context_manager/services/rule_engine.py:23（文件已删除 2026-08-19）
- backend/Django_xm/Django_xm/apps/context_manager/services/context_lifecycle.py:8（文件已删除 2026-08-19）
- backend/Django_xm/Django_xm/apps/context_manager/services/hierarchical_memory.py（**保留**：处置待用户单独确认）

**改进建议**：直接删除四个模块。若为规划中的实验功能，至少从 services/ 移出并加 DEPRECATED 标记，避免读者误以为在生效链路上。

**证据**：

```
Grep 'anti_oscillation|lazy_loader|rule_engine|context_lifecycle' 全后端 0 匹配（模块名）；
Grep 类名仅命中定义处
```

### 7.3 P2/P3 发现明细

| ID | 优先级 | 问题 | 位置 | 建议 |
|----|--------|------|------|------|
| rd-03 | P2 | get_logger 双实现并存（core/config.py 旧版未删），61 文件用旧版、12 文件用新版、100 文件用原生 getLogger，三种方式分裂 | backend/Django_xm/Django_xm/apps/core/config.py:188<br>backend/Django_xm/Django_xm/apps/core/logging_utils.py:11<br>backend/Django_xm/Django_xm/apps/knowledge/services/splitters.py:42 | 保留 core.logging_utils 唯一实现，旧版删除或改 re-export；存量 61 处 import 一次性批量替换 |
| rd-04 | P2 | MODEL_LIMITS 模型上下文窗口映射双份定义（usage_tracker 与 compression.TokenEstimator），7 条目重合且各自维护独有条目，必然漂移 | backend/Django_xm/Django_xm/apps/ai_engine/services/usage_tracker.py:37-48<br>backend/Django_xm/Django_xm/apps/context_manager/services/compression.py:100-111 | 在 ai_engine 定义唯一 MODEL_LIMITS 常量导出；或挂到 AIProvider/AIModel 注册表模型 |
| rd-05 | P2 | 前端 types/realtimeEvents.js 三个零引用导出（EventSource 枚举、TOOL_CALL_EVENT_TYPES、APPROVAL_EVENT_TYPES），事件分发改用手写字符串判断，枚举沦为摆设 | frontend/langchain-vue/src/types/realtimeEvents.js:16<br>frontend/langchain-vue/src/types/realtimeEvents.js:106<br>frontend/langchain-vue/src/types/realtimeEvents.js:119<br>frontend/langchain-vue/src/stores/sync/handleSessionEvent.js:213 | 删除死导出；或反向收敛——stores/sync/ 事件分发改用两个集合判断，后者更符合设计初衷 |
| rd-06 | P2 | 前端错误响应解析双份：sse.js 内联实现（L133-178）与 apiErrorHandler.js 的 extractErrorMessage/extractSSEError 重叠，文案提取行为已现细微不一致 | frontend/langchain-vue/src/utils/sse.js:133-178<br>frontend/langchain-vue/src/utils/apiErrorHandler.js:39-103 | fetchSSE 非 2xx 分支复用 extractSSEError（已接受 Response 对象），删除 sse.js 内联解析段 |
| rd-07 | P2 | 自研三层韧性/重试框架（约 500 行 + 包装器），tenacity 已声明依赖却零使用；两个独立熔断器实现语义重叠（用途分离暂可接受） | backend/Django_xm/Django_xm/apps/agent_hub/services/agent_resilience.py:31-87<br>backend/Django_xm/Django_xm/apps/agent_hub/services/resilient_invoker.py:62<br>backend/Django_xm/Django_xm/apps/learning/services/resilience.py:82<br>backend/Django_xm/requirements.txt:188 | 保留 classify_and_decide/降级决策（业务核心）；纯重试循环替换为 tenacity（stop_after_attempt + wait_exponential）；learning 四入口收敛为装饰器 |
| rd-08 | P3 | 前端自写 debounce/throttle 三处（useRealtimeSync/TaskList/useThrottle.js 约 50 行），@vueuse/core 已安装且 ChatView 已在用 | frontend/langchain-vue/src/composables/useRealtimeSync.js:18<br>frontend/langchain-vue/src/composables/useThrottle.js:3<br>frontend/langchain-vue/src/components/chat/TaskList.vue:175<br>frontend/langchain-vue/src/views/ChatView.vue | 替换为 useDebounceFn/useThrottleFn；useThrottle 保留原 API 形状做薄封装，可删约 50 行 |
| rd-09 | P3 | providers 目录 5 文件重复 registry 配置读取样板（_get_registry_config/get_provider_config 各约 10 行 × 5 = 50 行） | backend/Django_xm/Django_xm/apps/ai_engine/providers/deepseek.py:11-18<br>backend/Django_xm/Django_xm/apps/ai_engine/providers/groq.py:11-18<br>backend/Django_xm/Django_xm/apps/ai_engine/providers/anthropic.py:5<br>backend/Django_xm/Django_xm/apps/ai_engine/providers/openai.py:5<br>backend/Django_xm/Django_xm/apps/ai_engine/providers/qianfan.py:5 | providers/__init__.py 提供 make_provider_config_reader(name) 工厂；或调用方直接从 registry_service 取值消除中间层 |
| rd-10 | P3 | 散落硬编码：chat_service.py timeout=10.0/recursion_limit=500 未走 ResilienceConfig；前端 useSnapshotSync 时间常量未进 config/settings.js | backend/Django_xm/Django_xm/apps/chat/services/chat_service.py:918<br>backend/Django_xm/Django_xm/apps/chat/services/chat_service.py:921<br>backend/Django_xm/Django_xm/apps/agent_hub/services/agent_resilience.py:446<br>frontend/langchain-vue/src/composables/useSnapshotSync.js:17-25 | 超时/递归上限提升为 ResilienceConfig 字段经 settings 可调；前端时间常量迁入 config/settings.js realtime 段 |
| rd-11 | P3 | useStreamFinalizer.js 自写 3 次重试循环（无退避仅 warn），与 sse.js 指数退避、后端韧性重试构成第三套重试实现 | frontend/langchain-vue/src/composables/useStreamFinalizer.js:93-101<br>frontend/langchain-vue/src/utils/sse.js:6-16 | 短期给 utils/async.js 补通用 withRetry(fn, {retries, delay})；长期 REST 请求在 axios 封装层按幂等性统一配置重试 |
| rd-12 | P3 | channels 依赖缺失声明（asgi.py/consumers.py/realtime_events.py 均 import channels，requirements.txt 无 channels/daphne/uvicorn/whitenoise），清单既冗余又缺漏非受控 | backend/Django_xm/Django_xm/asgi.py<br>backend/Django_xm/Django_xm/apps/chat/consumers.py<br>backend/Django_xm/requirements.txt:1-216 | 补充 channels 及所选 ASGI 服务器到 requirements.txt；与 rd-01 一并执行「以实际 import 为准重新生成清单」 |

---

## 8. 改进路线图

### 8.1 P0 与 P1 行动清单（17 条，逐条列出）

| 序号 | 优先级 | 行动项 | 涉及位置 | 预期收益 | 状态（2026-08-19） |
|------|--------|--------|----------|----------|--------------------|
| 1 | P0 | shell_exec 白名单从前缀匹配收紧为「命令 + 子命令枚举」：manage.py 仅允许只读子命令并显式拒绝 shell/test/dbshell；find/curl/ping 移出白名单或参数二次校验；BLOCKED_PATTERNS 补 find -delete/-exec、curl -o；引入 shlex 逐 token 校验 | backend/Django_xm/Django_xm/apps/tools/langchain/shell.py:42,90,252,275 | 关闭唯一 P0 攻击路径（任意代码执行/文件删除自动放行），恢复白名单「安全命令免审批」语义 | ⬜ 待办 |
| 2 | P1 | SubAgentBuilder 注入 ApprovalMiddleware：_build_internal 对齐 base_builder 调用 ensure_approval_middleware，或将审批注入下沉到 AgentFactory.create 统一入口 | backend/Django_xm/Django_xm/apps/agent_hub/builders/subagent_builder.py:26,32（修复后） | 消除子代理工具调用完全绕过审批的缺口 | ✅ 已完成（spec:unify-context-injection，含勘误：原缺口系死路径误报，审批注入已下沉 build_middleware 收敛点保底） |
| 3 | P1 | 审批策略反转为 fail-closed：未知工具（含 MCP 动态工具，按 metadata.is_mcp_tool 识别）默认 CONTROLLED 进审批批次，只读工具显式注册 SAFE | backend/Django_xm/Django_xm/apps/agent_hub/approval/middleware.py:69,742,776 | 所有 MCP 工具与新增内置工具纳入审批门控，安全边界默认受控 | ⏸ 用户决定不解决（2026-08-19） |
| 4 | P1 | CacheClearView 权限收紧为 IsAdminUser；用户自清理仅保留 cache_type='query' 分支（user_id 前缀隔离）；pattern 增加前缀白名单校验 | backend/Django_xm/Django_xm/apps/cache_manager/views.py:180,191,199 | 消除任意登录用户清空全局缓存/删任意 key 的越权风险 | ⬜ 待办 |
| 5 | P1 | requirements.txt 补充 channels>=4.x、channels-redis>=4.x、daphne>=4.x；改用 pip-compile/uv lock 生成锁定文件 | backend/Django_xm/requirements.txt:1；backend/Django_xm/Django_xm/settings/base.py:53 | 新环境可按清单复原部署，依赖清单恢复真实性 | ⬜ 待办 |
| 6 | P1 | 错误响应去 str(e)：5xx 只返回通用文案 + error_id，异常细节仅入 logger.exception | backend/Django_xm/Django_xm/apps/chat/views_chat.py:446；backend/Django_xm/Django_xm/apps/knowledge/views_kb.py:77,106；backend/Django_xm/Django_xm/apps/cache_manager/views.py:83；backend/Django_xm/Django_xm/apps/tools/views_mcp.py:253 | 阻断表结构/文件路径/上游报文等敏感信息泄漏 | ⬜ 待办 |
| 7 | P1 | ChatMessageSerializer 批量预取 ResearchTask：视图中 task_id__in 一次查询构建映射经 context 传入，两个 method field 复用 | backend/Django_xm/Django_xm/apps/chat/serializers.py:198,301,314（修复后） | 100 条消息会话详情从 +200 次查询降为 +1 次 | ✅ 已完成（spec:normalize-tools-rest-fix-chat-api） |
| 8 | P1 | 修复缓存分支翻页失效：缓存命中路径从 query_params 解析真实 page，切片使用真实页码，补带 page 参数的单测 | backend/Django_xm/Django_xm/apps/chat/views_chat.py:869,874（修复后） | 消除缓存有效期内前端翻页显示错误数据的正确性 bug | ✅ 已完成（spec:normalize-tools-rest-fix-chat-api） |
| 9 | P1 | tools app RESTful 资源化改造：mcp/servers/{name}/ 承载 PATCH/DELETE，toggle 映射 PATCH + status；存量路径保留兼容期标 deprecated | backend/Django_xm/Django_xm/apps/tools/urls.py:7-36；backend/Django_xm/Django_xm/apps/tools/views_mcp.py:294,319（修复后） | API 语义对齐团队 RESTful 规范，消除全项目一致性最差模块 | ✅ 已完成（spec:normalize-tools-rest-fix-chat-api；**BREAKING 无兼容层，前端 api/tools.js 同步切换，未保留 deprecated 兼容期**） |
| 10 | P1 | prod.py 显式配置 channels_redis.RedisChannelLayer；group_send 失败增加 Prometheus counter 指标 | backend/Django_xm/Django_xm/settings/prod.py:1-119；backend/Django_xm/Django_xm/common/realtime_events.py:213-221 | 恢复生产环境审批/工具/流式事件的实时推送，兑现跨浏览器一致性设计目标 | ⬜ 待办 |
| 11 | P1 | 双向依赖治理：跨 app 复用私有函数去下划线提升为公共门面；明确依赖方向规则；import-linter contracts 接入 CI 固化分层契约 | backend/Django_xm/Django_xm/apps/research/services/writeback.py:404；backend/Django_xm/Django_xm/apps/chat/services/rag_chat_service.py:105；backend/Django_xm/Django_xm/apps/research/views.py:709 等 | 消除运行时依赖环与封装穿透，重构不再触发 ImportError，分层可执行 | ✅ 已完成（spec:c3b-dependency-debt-cleanup：writeback 引用的 merge_tool_calls_incremental 已公共化；common→apps / apps→services / ai_engine→agent_hub 三契约零豁免固化） |
| 12 | P1 | 前端接入 vitest：安装依赖、添加 vitest.config.js 与 test 脚本、接入 lint/CI | frontend/langchain-vue/package.json:6-14；frontend/langchain-vue/src/utils/__tests__/ | 8 个既有测试文件进入验证链路，停止僵尸化 | ⬜ 待办 |
| 13 | P1 | 后端补测试：approval 状态机全流转参数化、stream_persistence 链路、context_manager 压缩/断路器单测、retrieval_service 降级链路；建 conftest.py 公共 fixture | backend/Django_xm/Django_xm/apps/approvals/tests/；backend/Django_xm/Django_xm/apps/context_manager/services/ 等 | 核心安全链路与 AI 服务路径获得回归保障 | ⬜ 待办 |
| 14 | P1 | 后端长函数拆分：chat_service 按模式分派拆策略类，_persist_to_db_sync 按字段组拆分（先补测试再重构） | backend/Django_xm/Django_xm/apps/chat/services/chat_service.py:193,404,612,729；backend/Django_xm/Django_xm/apps/chat/services/stream_persistence.py:268,431 | 最高变更频率核心路径的回归风险显著下降 | ⬜ 待办 |
| 15 | P1 | 前端超大文件拆分：session.js 按领域拆 store，handleSessionEvent 各事件 case 下沉独立 handler 模块 | frontend/langchain-vue/src/stores/session.js:1-1731；frontend/langchain-vue/src/stores/sync/handleSessionEvent.js:49-712 | 事件类型增长时维护成本从线性上升降为平缓 | ⬜ 待办 |
| 16 | P1 | 依赖清单重新生成：以实际 import 为准重写 requirements.txt，删 spacy 全家桶/tenacity/eventlet 等零引用 40+ 条目，开发工具移 requirements-dev.txt | backend/Django_xm/requirements.txt:1-216 | 安装体积与构建耗时显著下降，清单可审计 | ⬜ 待办 |
| 17 | P1 | 删除 context_manager 4 个零引用死模块（约 321 行，已双维度 Grep 确认可删） | backend/Django_xm/Django_xm/apps/context_manager/services/anti_oscillation.py:10（保留）等 4 处 | 消除读者对生效链路的误判，缩减自研记忆子系统维护面 | 🕐 部分完成（spec:analyze-context-manager-dead-modules，3 删 2 留：lazy_loader/rule_engine/context_lifecycle 已删；anti_oscillation 保留待集成、hierarchical_memory 保留待确认） |

### 8.2 P2/P3 主题行动项（归并 12 条）

| 序号 | 优先级 | 行动项（主题归并） | 涉及发现 | 预期收益 |
|------|--------|--------------------|----------|----------|
| 18 | P2/P3 | **安全防护纵深加固**：生产强制 SANDBOX_ENABLED；白名单命令参数解析器化；移除 fetchSSE injectTokenQuery、query 认证仅限下载端点；channel layer 独立加密密钥；SVG 下载端点强制安全响应头；adelete_thread 同步清 Redis checkpoint 缓存 | ds-04、ds-05、ds-09、ds-10、lc-11 | shell/附件/JWT/checkpoint 四个已知风险面整体收敛 |
| 19 | P2/P3 | **依赖与配置治理**：get_logger/MODEL_LIMITS 双实现收敛为单一权威来源；langchain-redis 替换 community 语义缓存；跟踪 langchain-classic 与 groq 补丁上游迁移 | rd-03、rd-04、lc-13、lc-14、lc-16（另含 rd-12 与 8.1#5/#16 联动） | 消除双实现漂移风险，依赖面向官方稳定路径收敛 |
| 20 | P2/P3 | **审批与子代理运行时健壮性**：审批注入失败 fail-fast；同一工具累计审批超时上限；父 Graph 唤醒关系 Redis 持久化 + 孤儿挂起恢复 | lc-04、lc-18、ds-07 | 审批体系从「可用」升级为「故障可恢复」，挂起会话不再永久失联 |
| 21 | P2/P3 | **分层依赖与门面收敛**：agent_hub↔research 循环解耦（适配器协议下沉）；ResilientModel 下沉 ai_engine/common；跨 app 私有函数提升公开 API；cross_app 门面契约 CI 强制 | lc-09、ds-11、ds-15、lc-15（与 8.1#11 联动） | 依赖图单向化，基础设施层不再反向依赖编排层 |
| 22 | P2/P3 | **死代码与重复实现清理**：删除 mcp_integration 死函数、strict_rag_chain 四份重复实现抽公共函数、realtimeEvents 死导出（或反向收敛事件分发）、sse.js 错误解析复用 apiErrorHandler、providers 样板工厂化、前端重试统一 withRetry（**rd-02 部分完成 2026-08-19：lazy_loader/rule_engine/context_lifecycle 已删，anti_oscillation 保留待集成、hierarchical_memory 待确认，处置详见 rd-02 标记**） | lc-05、lc-06、rd-05、rd-06、rd-09、rd-11（rd-02 部分完成） | 预计削减 500+ 行重复代码，单一逻辑单点维护 |
| 23 | P2/P3 | **复杂度与性能治理**：15 个超 500 行组件拆分（优先两个千行视图）；chat_service/rag_chat_service 走 knowledge 门面；grading_node 改 bulk_update | cq-05、ds-06、ds-13 | 前后端热点文件可维护性恢复，写侧 N+1 消除 |
| 24 | P2/P3 | **API 语义与路由规范化**：知识库双轨路由收敛；知识库视图引入 Serializer；get_absolute_url 参数对齐；列表端点统一 ProjectPagination；research/chat 路由命名清理（**dj-06 tools RESTful 资源化已于 2026-08-19 完成，见 8.1#9**） | dj-07、dj-10、dj-14、dj-15、dj-16（dj-06 已完成） | 分页响应结构统一，路由体系单一可信 |
| 25 | P2/P3 | **数据模型与迁移治理**：冗余索引清理；DocumentIndex/IndexMetadata 合并方案；JSONField 去 null=True；ContextRule 作用域解绑；squashmigrations；choices 统一 TextChoices | dj-12、dj-13、dj-18、dj-19、dj-21、dj-22 | 写入性能提升，模型语义一致，部署链路缩短 |
| 26 | P2/P3 | **缓存一致性与配置收敛**：SystemConfig 缓存迁 Redis + 失效广播；会话缓存失效粒度降为单条目；dev.py 去 settings 导入期 IO；韧性参数进 ResilienceConfig/settings | dj-08、dj-09、dj-20、rd-10 | 缓存命中率回升，多 worker 配置一致，无导入副作用 |
| 27 | P2/P3 | **提示词与 LangChain 模式统一**：工具指令迁 prompts.yaml 并合并重复章节；format 改 PromptTemplate；自研记忆与官方能力映射审计（双实现开关）；plan_execute reflect 节点结构化输出；子代理提示词中文化；纯重试迁 tenacity（**lc-10 与 rd-02 保留项联动：anti_oscillation 集成、hierarchical_memory 处置应在自研记忆能力映射审计中一并决策；lc-02 附带的 deep_dynamic_context_enabled 开关默认 False，learning/plan_execute 暂不接入**） | lc-07、lc-08、lc-10、lc-12、lc-17、rd-07 | 提示词版本可审计可热更，LangChain 用法全局一致 |
| 28 | P2/P3 | **前端工程化收敛与文档补齐**：types 工具函数迁出；sse.js 封装 parseProtocolEvent 统一转换入口；三级 README + 前端 .env.example；axios 调试序列化限定 DEV；useApiTask 统一视图错误处理；debounce/throttle 换 @vueuse；sync 三基线不变式断言 | cq-06、cq-08、cq-09、cq-10、cq-11、rd-08、ds-12 | 命名边界单一入口，新成员可自助上手，错误处理一致 |
| 29 | P2/P3 | **异常体系与异步运行时治理**：公共异常基类统一双体系；非流式聊天改 async 视图；requests/time.sleep 迁 httpx/asyncio.sleep；contextvars 替换 threading.local；core 信号注册统一 ready() 模式；宽捕获分层收窄；原生 SQL 补测试或登记例外 | cq-07、cq-12、cq-13、ds-03、ds-08、ds-14、dj-11、dj-17 | 事件循环免遭同步阻塞，异常处理单一路径，审计归因准确 |

---

## 9. 剩余项行动分析（2026-08-19 复核）

本章由两份 2026-08-19 复核文档合并而成：**9.1 剩余项安全分级**（依据第 2 章摘要 + 第 8 章路线图共 80 条发现，逐条 Read/Grep 核实当前代码现状，判断本周改动是否已顺带解决）与 **9.2 死代码利用价值分析**（8 项 + 顺带扫描，处置原则「利用优先于删除」）。口径说明：dj-04 / dj-05 / dj-06 / lc-02 已完成、lc-03 用户决定不解决（均已排除）；rd-02 部分完成，剩余部分由 9.2 承接。

分级标准：

| 级别 | 定义 | 处置方式 |
|------|------|----------|
| **A 类·纯增益零破坏** | 新增性质，不改变任何现有运行路径行为（测试基建、依赖补声明、文档、新配置项默认关闭等） | 可立即批量执行，建议一个 spec 打包 |
| **B 类·行为修正小改动** | 单点小改，属修复语义，但有可观察行为变化（权限收紧、错误文案、配置启用）——每项必须列行为变化点/影响面/回退难度 | 逐项等用户点头，按风险从低到高排序 |
| **C 类·需专项设计** | 大重构、拆分、安全策略、依赖清单重写等 | 必须另立 spec |

### 9.1 剩余项安全分级

#### A 类表（纯增益零破坏，可立即批量，4 项覆盖 5 个发现 ID）

> **执行状态：4 项已全部完成（2026-08-19，spec: `.trae/specs/add-regression-test-infra/`），lc-05 死文件已顺带删除。**

| 发现 ID | 标题 | 当前位置（复核后） | 核实结论（现状） | 改动面 | 推荐批次 |
|---------|------|--------------------|------------------|--------|----------|
| cq-01 | 前端接入 vitest | ~~frontend/langchain-vue/package.json:6-14；src/utils/__tests__/（7 个 .test.js + 2 个 .mjs 辅助文件在位）~~ | **✅ 已完成（2026-08-19）**：vitest 4.1.11 安装为 devDependency，vitest.config.js 就位（@ 别名 + node 环境，不加载生产插件），scripts 增 test / test:watch；6 个 .test.js 全部改写为 vitest 标准 API（describe/it/expect，97 用例逐文件等价迁移 M=N），删除 vite-alias-loader.mjs 与 logger.stub.mjs 两个 Node 直跑辅助文件；npm run test 97/97 全绿，build/lint 通过 | ~~新增 devDependency + vitest.config + 1 条 test 脚本；零运行代码改动~~ | 第 1 批（A 类打包 spec） |
| cq-02（含 cq-12） | 后端补测试：approval 状态机全流转 / context_manager 压缩断路器 / retrieval 降级链路 | ~~apps/approvals/tests/（仅 batch_resume 1 个）；apps/context_manager/services/（仍无 tests 目录）；apps/knowledge/services/retrieval_service.py:51-80（原生 SQL 保持不动，仅补测试）~~ | **✅ 已完成（2026-08-19）**：新增 approval 状态机测试 22 用例（含 timeout 双分支/终态幂等/查询过滤）、context_manager 压缩 16 + 断路器 12 用例、retrieval 降级 7 用例（DB 层 mock，聚焦链路逻辑与 degraded 标记）；全量 192/192 OK。**测试数据库按用户决策切换为 PostgreSQL**（settings/test.py 继承 base.py PG 配置，Django 自动创建 test_<DB_NAME> 临时库并销毁，开发库零接触；SQLite 属禁止项），同步清理 IS_TEST_ENV 死配置与 conftest/pytest 时代陈旧注释 | ~~新增测试文件 + conftest.py 公共 fixture；零生产代码改动~~ | 第 1 批（A 类打包 spec） |
| dj-02 | requirements.txt 补 channels / channels-redis / daphne 声明 | ~~backend/Django_xm/requirements.txt（216 行，无任何 channels 系条目）；settings/base.py:53 INSTALLED_APPS 含 daphne+channels~~ | **✅ 已完成（2026-08-19）**：按当前环境实际版本固化追加 channels>=4.3.2 / channels-redis>=4.3.0 / daphne>=4.2.2 三条（紧邻 Django 区域，字母序），其余条目未动 | ~~requirements.txt 加 3 行；零代码改动~~ | 第 1 批（A 类打包 spec） |
| cq-09 | 三级 README + 前端 .env.example | ~~项目根 / backend/ / frontend/langchain-vue/（均无 README.md）；前端仅 .env 与 .env.development，无 .env.example~~ | **✅ 已完成（2026-08-19）**：根 README（项目定位/技术栈/11 app 职责/启动/测试总表）、backend README（conda+docker PG 前置/.env 大类/settings 环境说明/启动测试命令）、frontend README（Node v20.19.5/命令表/环境变量表/代理联调）、.env.example（4 个 VITE_ 变量带注释）全部就位，命令均经真实核对可直接复制执行 | ~~新增 3 个 README + 1 个 .env.example；零代码改动~~ | 第 1 批（A 类打包 spec） |

小结：~~4 项共同特征是只新增文件/依赖声明/脚本，现有运行路径零改动。注意两点：cq-01 的 vitest 属 devDependencies 不进生产构建；dj-02 只补 3 条声明，不触碰 rd-01 的全清单重写（那是 C 类）。~~ **A 类打包已执行完毕（含 lc-05 顺带删除），后端测试基建 135→192 用例、前端 97 用例可一键运行，C4/C5 两大拆分重构的回归保障网已就位。**

#### B 类表（行为修正小改动，逐项等用户点头，按风险从低到高，15 项）

| # | 发现 ID | 标题 | 当前位置（复核后） | 行为变化点 / 影响面 / 回退难度 | 改动面 | 推荐批次 |
|---|---------|------|--------------------|-------------------------------|--------|----------|
| B1 | cq-10 | axios 响应拦截器调试序列化限定 DEV | frontend/langchain-vue/src/api/axios.js:69-74（双重 JSON.stringify 截断 200 比较仍在，无 import.meta.env.DEV 包裹，生产也执行） | 生产环境不再输出 console.debug 转换日志（纯调试输出，无功能语义）/ 仅控制台日志 / 删一个 if 判断 | 1 处 if 包裹 | **[✅已修复 2026-08-19 · spec:b1-low-risk-convergence]** |
| B2 | rd-03 | get_logger 双实现收敛（旧版改 re-export） | apps/core/config.py:188（旧版仍在）与 apps/core/logging_utils.py:11（新版）；61 文件用旧版、12 文件用新版、100 文件原生 getLogger 三分裂现状不变 | 旧版内部实现替换为 re-export，对外签名与行为完全一致 / 零（61 个 import 点不动）/ 恢复函数体 | config.py 内 1 处函数体改 import | **[✅已修复 2026-08-19 · spec:b1-low-risk-convergence]** |
| B3 | cq-06 | types/index.js 工具函数迁出（迁 utils + re-export 兼容） | frontend/langchain-vue/src/types/index.js:169-219（createMessage 等 7 个工具函数仍在此导出） | 函数实体移至 utils、types/index.js 保留 re-export，所有既有 import 零感知 / 零运行行为变化 / 移回即可 | 移动约 50 行 + 1 行 re-export | **[✅已修复 2026-08-19 · spec:remove-dead-code-batch]**（实际处置：7 个死工具函数直接删除 + validateMessage 迁 utils/validation.js，按红线不 re-export） |
| B4 | cq-08 | sse.js 封装 parseProtocolEvent 统一转换入口 | frontend/langchain-vue/src/utils/sse.js（无 parseProtocolEvent/toCamelCase；转换调用点仍散布 9+ 处各点手动还原 type） | 新增统一入口函数、各消费点改调用，单点转换行为与现手动逻辑等价（需对齐各点差异）/ SSE/WS 事件解析路径，转换错误会导致前端事件路由失败 / 恢复各点手动转换 | sse.js 新增 1 函数 + 消费点逐个切换（可分批） | **[✅已修复 2026-08-19 · spec:b1-low-risk-convergence]** |
| B5 | rd-08 | debounce/throttle 换 @vueuse 薄封装 | composables/useThrottle.js:3；useRealtimeSync.js:18、TaskList.vue:175 自写实现仍在（三处共约 50 行；@vueuse/core ^14.2.1 已安装） | 底层实现换 useDebounceFn/useThrottleFn、保留原 API 形状薄封装；leading/trailing 边界语义可能有细微差异 / 输入节流与实时同步触发频率 / 换回自写实现 | 3 处替换 + 薄封装保留 | **[✅已修复 2026-08-19 · spec:b1-low-risk-convergence]**（@vueuse 替换 + 删 useThrottle.js） |
| B6 | rd-06 | sse.js 错误解析复用 apiErrorHandler | src/utils/sse.js:133-178 内联解析仍在；src/utils/apiErrorHandler.js:39-103（双份解析并存，文案提取行为细微不一致） | SSE 非 2xx 错误文案改由 extractSSEError 统一提取，个别错误场景用户看到的文案会变化 / 仅错误提示文案 / 恢复内联段 | sse.js 删内联段改调用 | **[✅已修复 2026-08-19 · spec:b1-low-risk-convergence]** |
| B7 | rd-05 | realtimeEvents 零引用导出处置 | src/types/realtimeEvents.js:16,106,119（三个零引用导出仍在）；stores/sync/handleSessionEvent.js:213 仍手写字符串判断 | 方向二选一：(a) 删除死导出，零行为变化；(b) 反向收敛（事件分发改用枚举集合判断），分发逻辑等价重写需回归测试 / a 零风险；b 触及事件分发核心 / 均容易 | a 删 3 段；b 改 1 处分发 + 删手写判断 | **[✅已修复 2026-08-19 · spec:b1-low-risk-convergence]**（a+b 均做：死导出清理 + Set 枚举接线） |
| B8 | dj-03 | 错误响应去 str(e)（15 处） | chat/views_chat.py:446；knowledge/views_kb.py:75,104；cache_manager/views.py:83,135,175,215,231；tools/views_mcp.py:202,253,340,442,482,613,713 | 5xx 响应 message/error 字段从异常细节变通用文案（异常路径本身已是失败态，不破坏流程）/ 仅异常分支响应体 / 逐点恢复 str(e) | 15 处逐点替换 message 为通用文案（logger.exception 已就位） | **[✅已修复 2026-08-19 · spec:b2-exception-hardening]**（全量 17 文件 53 处，含 SSE/WS 出口；业务语义异常保留原则） |
| B9 | dj-01 | CacheClearView 权限收紧 | apps/cache_manager/views.py:180-215（仍 IsAuthenticated；pattern 透传 :191；全量清除 :199-210）；前端 KnowledgeBaseView.vue:439 普通用户可点「清空全部缓存」（详见下方新发现①） | 非管理员调全量清除/pattern 分支返回 403；前端该按钮对普通用户失效（本属危险操作）/ 知识库页「清空缓存」按钮 + 任何直接调用该端点的脚本 / 改回权限类一行 | views.py 1 个类权限收紧 + pattern 白名单校验；前端按钮按需隐藏 | **[✅已修复 2026-08-19 · spec:b2-exception-hardening]**（键错位根因修复 + 动态权限分派 + 前端 isAdmin 联动） |
| B10 | dj-20 | dev.py 去 settings 导入期 Redis ping | settings/dev.py:17-27（模块导入期 ping 仍在；REDIS_AVAILABLE 三元表达式 :38-59 依赖其结果，Redis 不可达时 dev 启动固定延迟约 3 秒） | 探测移到惰性点后 REDIS_AVAILABLE 判定时机后移，CHANNEL_LAYERS 选择逻辑需同步重构（首次使用时探测或 REDIS_PROBE=0 跳过）/ 仅 dev 启动流程与 channel layer 回退时机 / 恢复模块级探测 | dev.py 1 处重构（探测与 CHANNEL_LAYERS 联动） | **[✅已修复 2026-08-19 · spec:b2-exception-hardening]**（删启动 ping + REDIS_AVAILABLE，固定 RedisChannelLayer 删 InMemory 降级分支） |
| B11 | ds-01 | prod.py 显式配置 CHANNEL_LAYERS | settings/prod.py（全文 119 行无 CHANNEL_LAYERS；对照 dev.py:38-59） | 生产 channel layer 从 InMemory 变 Redis，Celery/FastAPI 侧 group_send 开始真正送达 WS 进程（实时推送激活，属修复但生产行为可观察变化）/ 生产实时事件通道，需生产 Redis 可用性保障 + 部署验证 / 删除配置块即回默认 | prod.py 新增 1 段配置（与 dev.py 同构） | **[✅已修复 2026-08-19 · spec:b3-prod-config-cache]**（方案升级：上移 base.py 单一权威而非 prod.py 单独加，见 9.4） |
| B12 | dj-08（最小改法） | SystemConfig 异步未命中静默默认值提升为 warning | apps/ai_engine/models.py:73-78（进程内 _config_cache + 异步未命中 logger.debug 返回 default 原样） | 最小改法仅日志级别提升、无功能行为变化；若采用 sync_to_async 读真实值，异步任务配置读取从「用默认值」变「读真实值」（修复语义，异步链路耗时微增）/ 异步任务模型选择正确性 / 还原日志级别或移除 sync_to_async | models.py 1 处 | **[✅已修复 2026-08-19 · spec:b3-prod-config-cache]**（方案推翻最小改法：全量迁 Redis，见 9.4） |
| B13 | dj-09 | 会话缓存失效粒度降为单条目 | apps/chat/signals.py:29（post_save 仍 invalidate_all_user_sessions 全量失效） | 失效粒度从「用户全量」降为「单会话条目」，缓存命中率上升，但短时间内列表可能显示旧标题（时效性语义变化）/ 会话列表显示时效（秒级窗口）/ 恢复全量失效调用 | signals.py 1 处 + secure_session_cache 增单条目方法 | **[✅已修复 2026-08-19 · spec:b3-prod-config-cache]** |
| B14 | rd-04 | MODEL_LIMITS 双实现收敛 | apps/ai_engine/services/usage_tracker.py:37 与 apps/context_manager/services/compression.py:100（_MODEL_LIMITS 双份仍在，各有独有条目） | 两份映射合并为单一权威来源后，引用侧部分模型的上下文窗口限额数值会变化（取哪份为准需用户确认），影响 token 预算估算与裁剪触发点 / 上下文压缩触发时机 / 恢复双定义 | 定义合一 + 2 处 import 改造 | **[⛔已划掉 2026-08-19]**（复核全仓仅剩 compression.py 单一权威表，合并已完成） |
| B15 | cq-11（仅试点切片） | useApiTask 统一视图错误处理——先试点 1-2 个视图 | views/DeepResearchView.vue（13 处 catch）/ RagView.vue（9 处）/ WorkflowView.vue（9 处）；全量 81 处散落 catch 仍在 | 试点视图的 loading/error/ElMessage 生命周期改走统一路径，提示文案与重试策略统一（用户可见文案变化）/ 单视图 / 该视图恢复原 catch | 新增 1 composable + 改 1 视图 | **[✅已修复 2026-08-19 · spec:b3-prod-config-cache]**（方案推翻试点：三视图全量迁移，见 9.4） |

小结：~~15 项经复核全部未消解~~ **进度更新（2026-08-19）：B1-B15 已全部闭环**（B1/B2/B4-B7 随 spec:b1-low-risk-convergence，B8-B10 随 spec:b2-exception-hardening，B3 死工具函数迁移与 rd-03 统一随 spec:remove-dead-code-batch，B11/B12/B13/B15 随 spec:b3-prod-config-cache 完成——其中两处推翻本表原推荐：dj-08 弃最小改法改全量迁 Redis、cq-11 弃试点切片改三视图全接；B14 rd-04 复核后划掉）。终验：后端 284/284 + 前端 vitest 127/127 + build/lint 零告警。原排序逻辑保留备考：B1-B7 零/近零功能影响（日志、死代码、等价重构、文案微调）→ B8-B10 异常分支与 dev 配置（不影响主流程）→ B11-B14 生产配置与缓存语义 → B15。每项均可独立回退。

#### C 类表（需专项设计，另立 spec，按价值排序 8 组）

| # | 发现 ID | 标题 | 当前位置（复核后） | 归 C 理由 | 推荐批次 |
|---|---------|------|--------------------|-----------|----------|
| C1 | lc-01 | **P0** shell_exec 白名单前缀匹配绕过 | apps/tools/langchain/shell.py:35-86（白名单仍含 "python manage.py"、find、curl、ping）；:89-125（BLOCKED_PATTERNS 仍无 find -delete/-exec、curl -o）；:252-288（_is_command_whitelisted 前缀匹配逻辑原样） | 未消解，P0 攻击路径仍在（python manage.py shell -c、find -delete、curl -o 仍会自动放行）。虽为 P0，但属安全策略收紧：白名单内命令（manage.py/find/curl/ping 等）从「自动放行」变为「进审批」，agent 执行这些命令的交互行为显著变化（审批弹窗增多），必须用户确认设计方向（命令+子命令枚举粒度、find/curl 移除还是参数二次校验、shlex 逐 token 校验方案）后才能动手 | 置顶，需用户确认设计方向后立即立 spec |
| C2 | rd-01（联动 dj-02 补声明之外的 rd-12） | 依赖清单重写（删 40+ 零引用/传递依赖） | **✅ 已完成（2026-08-20，spec: c2-requirements-rewrite）**：requirements.txt 219 行 → 56 条直接依赖；清除约 162 条（spacy/jupyter 全家桶、开发工具、传递依赖固化、langchain-anthropic 零 import、langchain-milvus 当前未启用等）；补缺漏声明（fastapi/uvicorn/python-docx/duckduckgo-search/groq/chromadb 等）；新环境（Python 3.11.15 干净 venv）安装回归 318 测试全绿 + check 0 issues + fastapi/五向量后端冒烟通过；三处版本修正（langgraph-store-postgres 移除 / redis 上界 <8 / deepagents 下限校准 >=0.7.5）；验证报告已落盘 spec 目录 | 已收口 | 已完成 |
| C3 | ds-02 / lc-09 / ds-11 / ds-15 | 双向依赖治理 + import-linter 分层契约 | **✅ 已完成（2026-08-20，spec: c3b-dependency-debt-cleanup）**：①审批三件套（approval_lifecycle/approval_gateway/approval_batch）从 common 整体迁移 apps/approvals/services，approval_service 4 私有函数公共化；②realtime_events 幽灵会话过滤经 set_user_history_filter 注册点注入（依赖反转，common→apps 违规清零）；③persist_chat_tool_calls 归位 apps/chat/services/stream_persistence（apps→services 违规清零）；④_merge_tool_calls_incremental/_publish_stream_event/_group_name 跨模块私有成员公共化；⑤.importlinter 三契约豁免清零（原 7 条 ignore_imports 全部删除），lint-imports 3 kept 0 broken；回归 339/339 + check + makemigrations --check + 前端 vitest 127/127 全绿 | 已收口（ds-02 核心收口；lc-09/ds-11/ds-15 跨 app 循环依赖属长期演进，契约已固化防止恶化） | 已完成 |
| C4 | cq-03 | 后端长函数拆分（chat_service 4 个超百行函数、_persist_to_db_sync 163 行） | **✅ 已完成（2026-08-20，spec: c4-split-long-functions）**：6 个目标函数全部 ≤80 行——chat_service 四方法 535/204/130/113 行 → 38/34/12/11 行编排（新增 33 个职责单一私有方法 + 2 个 dataclass，rebuild_agent_fn 闭包经工厂显式收参）；stream_persistence 三函数 161/112/85 → 62/74/70 行（新增 10 个函数 + 39 个直接单测，此前零覆盖）；deep_builder._build_internal 143→30 行（+6 私有方法）；顺带删除不可达死分支 28 行 + 死赋值 2 处；行为保持（事件序列/DB 写入/异常语义零变化）；后端 378/378（339+39）+ 前端 127/127 + lint-imports 零豁免全绿 | 已收口 | 已完成 |
| C5 | cq-04 | 前端超大文件拆分（session.js / handleSessionEvent） | **✅ 已完成（2026-08-20，spec: c5-split-frontend-stores）**：session.js 1700 行拆为 stores/session/ 五切片领域组合（sessionList/sessionCrud/messageFlow/versionFlow/toolCallState，各自持有 state，runtimeDeps 解决跨切片循环调用，公共 API 零变化 41 消费点零修改）；handleSessionEvent.js 707→297 行纯路由（审批/子代理/推理事件下沉 3 个 handler 模块）；新增 61 个 handler 单测；按用户指令修正"为压缩而压缩"的硬拆（approvalHandler 主流程还原连续形态）；前端 188/188 + build + lint 三绿、后端 378/378 无波及 | 已收口 | 已完成 |
| C6 | cq-05 | 15 个超 500 行组件拆分 | **✅ 已完成（2026-08-20，spec: c6-split-giant-components，4 个千行级优先项收口）**：ToolSelector 1114→574 行容器 + 三 tab 子组件（LangchainToolList/McpServerList/SkillList，契约不变调用点零修改，跨 tab 共享样式入 popover 命名空间非 scoped 块）；DeepResearchView 1377→1175 行 + useDeepResearchPolling/useDeepResearchKnowledge（实例安全：零模块级可变状态 + onUnmounted 自动清理）+ ContinueResearchDialog 子组件 + 死样式删除 135 行；WorkflowView 1303（实测）→1036 行 + useWorkflowPolling/useWorkflowExecution（实例安全 + onUnmounted 清理/断开 SSE）+ api/workflow.js 死方法删除 6 个；**ChatMessage 按决策变更锁死零改动**（1188 行中 style 段占 558 行，script 仅 ~300 行且已委托 8 个子组件，高频流式渲染路径拆分有性能回归风险）——"不为拆分而拆分"原则首次落地为组件级决策；前端 188/188 + build + lint 三绿、后端 378/378 无波及 | 4 个千行级已收口（其余 11 个超 500 行组件后续按机会推进，遵循同原则） | 已完成 |
| C7 | dj-12 / dj-13 / dj-18 / dj-19 / dj-21 / dj-22 | 数据模型与迁移治理（冗余索引清理、DocumentIndex/IndexMetadata 合并、JSONField 去 null、ContextRule 解绑、squashmigrations、choices 统一） | **✅ 第①批已完成（2026-08-20，spec: c2-model-migration-consistency）：dj-12 冗余索引、dj-21 squash（chat 20→1/approvals 13→1/learning 10→1）、dj-22 choices 全仓 TextChoices 化（含 approvals 175 处调用点）**；**✅ 第②批已完成（2026-08-20，spec: c7b2-pagination-route-normalization）：dj-15 分页统一 + dj-16 路由规范化（前端 100% 适配）**；**✅ 第③批已完成（2026-08-20，spec: c7b3-data-model-consolidation）：dj-18 ChatMessage JSONField 去 null（18 处双轨读点清理）+ dj-13 双索引模型合并（DocumentIndex 彻底删除、迁移拆三段修复 PG pending trigger events）；dj-19 ContextRule 已随死代码删除批次（spec: remove-dead-code-batch）以更彻底路径完成（僵尸表整体删除）——C7 至此全部收口** | 已收口 | 已完成 |
| C8 | rd-02（剩余部分） | context_manager 剩余死代码与 hierarchical_memory / 模型处置 / PromptCacheSerializer / 死配置 | ~~anti_oscillation.py 仍存在~~；其余按死代码分析结论处置（见 9.2） | **✅ 已全部完成（2026-08-20，spec: c1-lightweight-convergence）**：anti_oscillation.py 已删除，rd-02 至此全部收口 | 已收口 |

C 类归组（其余 P2/P3 未逐项展开项，均需专项设计）：

- **异步运行时治理**：ds-03（非流式阻塞 120s + run_async 线程池）、ds-08（requests/time.sleep 迁 httpx/asyncio.sleep）、ds-14（run_async 线程 churn）、dj-11（contextvars 替换 threading.local）——影响执行模型与事件循环，需整体设计。
- **安全纵深**：ds-04（沙箱强制 + 白名单参数解析器化，与 lc-01 同族设计）、ds-05（JWT query 传输移除，前后端认证面联动）、ds-10（SVG 安全响应头）、ds-09（channel layer 独立加密密钥）、lc-11（adelete_thread 清 Redis，涉及 key 扫描策略设计）。
- **LangChain 模式收敛**：~~lc-06（四份 RAG 查询重复实现抽公共，约 500 行）~~（**✅ 已完成 2026-08-19，spec: lc06-strict-rag-convergence**）、lc-07/lc-08/lc-17（提示词外置化 + PromptTemplate 替换 + 子代理提示词中文化）、lc-10（自研记忆子系统与官方能力映射审计）、lc-12（reflect 节点结构化输出）、lc-13/lc-14/lc-16（langchain-redis / langchain-classic / groq 补丁依赖面向官方收敛）、rd-07（纯重试迁 tenacity）。
- **API 语义规范化**：~~dj-07（知识库路由双轨收敛）~~（**✅ 已完成 2026-08-20，spec: c1-lightweight-convergence**）、~~dj-10（知识库视图 Serializer 标准化）~~（**✅ 已完成 2026-08-20，spec: c1-lightweight-convergence**）、~~dj-14（get_absolute_url 修复/删除）~~（**✅ 已完成 2026-08-20，spec: c1-lightweight-convergence**）、~~dj-15（列表分页统一，响应结构变化需前端适配）~~（**✅ 已完成 2026-08-20，spec: c7b2-pagination-route-normalization**）、~~dj-16（research 重复端点清理）~~（**✅ 已完成 2026-08-20，spec: c7b2-pagination-route-normalization**）、~~dj-17（core 信号注册统一）~~（**✅ 已完成 2026-08-20，spec: c2-model-migration-consistency**）。
- **健壮性与运行时状态**：lc-04（审批注入 fail-fast，失败场景从降级变报错，语义收紧需评估）、lc-18（审批超时累计限制）、ds-07（子代理唤醒 Redis 持久化）、ds-12（sync 三基线不变式断言，DEV 模式新增可并入 A 类边缘但建议随 cq-04 一起设计）、ds-13（grading_node bulk_update）、~~cq-07（双异常体系统一）~~（**✅ 已完成 2026-08-20，spec: c1-lightweight-convergence**）、cq-13（宽捕获收窄）、cq-11 全量推广（81 处 catch）。
- **杂项收敛**：~~lc-05（mcp_integration 死函数删除，零引用核实后近 A 类，建议并入 A 类 spec 顺带执行前再 Grep 复核一次）~~（**✅ 已于 2026-08-19 随 A 类 spec 删除，全仓零残留**）、lc-15（私有函数公开化）、rd-09（providers 样板工厂化）、rd-10（散落硬编码提升配置，若新配置默认值等于现硬编码值则降为 A 类边缘）、rd-11（withRetry 统一，替换调用点属 B 类边缘可随试点做）。

#### 本周复核新发现（需重点关注）

**本周改动未完全消解任何候选项**，但有 4 处部分消解或现状变化，其中前两条为风险恶化：

| 序号 | 类型 | 复核结论与证据 |
|------|------|----------------|
| ① | ~~dj-01 影响面扩大（风险恶化）~~ | **✅ 已消解（2026-08-19，spec:b2-exception-hardening）**：后端键错位根因修复（`type`→`scope`）+ get_permissions 按 scope 分派（query→IsAuthenticated，all/model/pattern→IsAdminUser）+ 前端按钮 `v-if="userStore.isAdmin"`（登录响应补 is_staff 字段）——界面上普通用户不再可见该按钮，构造请求也会被 403 拦截 |
| ② | ~~dj-03 反向扩散（风险恶化）~~ | **✅ 已消解（2026-08-19，spec:b2-exception-hardening）**：tools/views_mcp.py 全部 7 处 e!s/str(e) 已改为通用文案（"连接失败，详细信息请查看服务端日志"等）+ logger.exception 落日志 |
| ③ | cq-02 部分消解 | 后端测试文件 11 → 15 个（新增 tools/tests/test_views_rest.py、chat/tests/test_chat_message_serializer_research.py、chat/tests/test_chat_session_list_pagination.py、tools/tests/test_filesystem.py、research/tests/test_interrupt_resume_semantics.py）；但 approval 状态机全流转、context_manager 压缩/断路器、retrieval 降级链路仍零测试 |
| ④ | ~~dj-03 部分消解~~ | **✅ 已全部完成（2026-08-19，spec:b2-exception-hardening）**：knowledge/views_kb.py 全部 4 处 SERVER_ERROR 分支（List/Post/Detail get/patch）已改通用文案；残留的 str(e) 均为 ValueError/DoesNotExist 业务语义文案（设计决策：预期内校验错误保留原文） |

另：rd-02 已全部收口——lazy_loader.py / rule_engine.py / context_lifecycle.py / anti_oscillation.py 均已删除（最后一个于 2026-08-20 删除，spec: c1-lightweight-convergence）。

#### 推荐执行顺序

1. **A 类：可立即批量（建议一个 spec 打包）**——cq-01 + cq-02（含 cq-12）+ dj-02 + cq-09 四项一次立 spec 执行：全部是新增文件/依赖声明/测试基建，零运行路径改动，互不依赖可并行；其中 cq-01/cq-02 产出的测试基建正是 C4/C5 两大拆分重构的前置条件——先有回归保障网，后续 B/C 类的每一步都有兜底。lc-05 可在该 spec 内 Grep 复核零引用后顺带删除。
2. **B 类：逐项等用户点头（按风险从低到高三波）**——第一波（零/近零功能影响）：cq-10 → rd-03 → cq-06 → rd-08 → rd-06 → rd-05（先做删除方向 a）→ cq-08（封装后逐点切换），这一波单纯是收敛与清理，失败模式仅为「改回去」；第二波（异常分支与 dev 配置）：dj-03（15 处 str(e)，可按文件分批）→ dj-01（权限收紧，建议与前端 KnowledgeBaseView 按钮联动）→ dj-20（dev 启动探测惰性化）；第三波（生产配置与缓存语义，需用户明确点头）：ds-01（生产 channel layer，需部署验证）→ dj-08 最小改法 → dj-09 → rd-04（需先确认 MODEL_LIMITS 权威版本）→ cq-11 试点。
3. **C 类：按价值排序，P0 置顶但需先确认设计方向**——lc-01（唯一 P0，白名单内命令从自动放行变进审批，需用户确认子命令枚举粒度、find/curl 移除还是参数二次校验、是否引入 shlex 逐 token 校验；方向定了即可快速落地，单文件 shell.py + policies.py 联动）→ rd-01 依赖清单重写（独立价值高：新环境可复原）→ ds-02/lc-09 依赖治理 + import-linter（结构止血，防止恶化）→ cq-03 / cq-04 两大核心路径拆分（前置依赖 A 类测试基建）→ cq-05 组件拆分、dj 组模型迁移治理（长期按机会推进）→ rd-02 剩余部分跟随 9.2 结论。lc-03 已排除（用户决定不解决）。

### 9.2 死代码利用价值分析

#### 8 项结论总表（含顺带扫描）

> 路径口径：`apps/...` 相对 `backend/Django_xm/Django_xm/`，`src/...` 相对 `frontend/langchain-vue/`；行数统计含注释与空行。

| # | 项 | 位置 | 结论 |
|---|----|------|------|
| 1 | lc-05：mcp_integration.py 死函数 | ~~apps/agent_hub/mcp_integration.py（全文件 136 行）~~ | **✅ 已删除（2026-08-19，随 A 类 spec 执行）**：全文件删除 + langgraph_integration.py docstring 同步修正，全仓 Grep 零残留，manage.py check 通过。原判定（零运行时影响、双重腐烂、保留构成第二入口）已验证成立 |
| 2 | ~~lc-06：strict_rag_chain.py 四份重复~~ | ~~apps/knowledge/services/strict_rag_chain.py（759 行）~~ | **✅ 已完成（2026-08-19，spec: lc06-strict-rag-convergence）**：4 死符号 + 4 失效 import 删除（services/__init__.py 死 re-export 含 STRICT_RAG_QA_PROMPT 同步清理）；三在役入口步骤 0-2 抽 `_maybe_rewrite_query` / `_retrieve_with_fallback` / `_handle_empty_result` sync+async 双版本内核 + `_is_degraded_result` 判定；3 个重复字符串收敛为模块常量；759 → 613 行（净减 146，含新增内核 docstring）；新增 17 用例逐分支锁行为（sync 关键字传参 k vs async to_thread 位置传参、事件序列、HyDE 仅用于检索等），210/210 全绿，调用点零改动 |
| 3 | rd-05（注）：realtimeEvents.js 死导出 | ~~src/types/realtimeEvents.js（224 行）~~ | **✅ 全部完成（2026-08-19）**：EventSource 枚举已删除（零引用验证成立）；Set 反向收敛接线已随 B 类第一波 spec（b1-low-risk-convergence）完成——handleSessionEvent.js / handleTaskEvent.js 两处 13 个 tool_call_*/approval_* case 字符串全部替换为 `TOOL_CALL_EVENT_TYPES.has(event.type)` / `APPROVAL_EVENT_TYPES.has(event.type)` 分支路由 |
| 4 | PromptCacheSerializer | ~~apps/context_manager/serializers.py:76-96（21 行）~~ | **✅ 已删除（2026-08-19，随死代码 spec）**：用户拍板「僵尸表直接删除，要彻底，数据中的僵尸表也要处理」——ContextRule / AutoMemory / PromptCache 三僵尸模型（代码 + admin + hierarchical_memory 消费模块 399 行 + docker PostgreSQL 开发库三张表，经 0003 DeleteModel 迁移真实删表）已全部彻底删除，全仓代码零残留（仅历史迁移文件命中） |
| 5a | cross_session_max_context_length 死配置 | ~~apps/context_manager/config.py:46 + services/manager.py:61/:77（3 行）~~ | **✅ 已删除（2026-08-19）**：三处（config 字段 / manager 字段 / 装配传参）全删，全仓零残留 |
| 5b | learning 默认能力死配置 | ~~apps/ai_engine/config.py:94（1 行）~~ | **✅ 已删除（2026-08-19）**：learning 条目行已删，AGENT_CAPABILITIES_DEFAULT 字段与 base/deep_research 条目保留（get_default_capabilities 5 处调用不受影响） |
| 6 | rd-04：MODEL_LIMITS 双份 | ~~apps/ai_engine/services/usage_tracker.py:37-48 与 apps/context_manager/services/compression.py:100-111~~ | **✅ 已收敛（2026-08-19）**：usage_tracker 本地表已删，get_max_tokens 改调 TokenEstimator.get_model_limit（新增 default 参数，settings fallback 语义保留）；gemini×2 条目已并入 compression.py 唯一权威表（避免 gemini 用户 fallback 错变为 128K）；test_compression.py 新增 gemini 断言用例；全量 193 测试通过 |
| 7 | rd-03：get_logger 双实现 | ~~apps/core/config.py:188-199（61 文件消费）与 apps/core/logging_utils.py:11-23（12 文件消费）~~ | **✅ 已彻底统一（2026-08-19，弃用原转发代理方案）**：按用户「不要兼容旧代码」红线，未采用本报告原定的 re-export 转发，而是 core/config.py 旧实现彻底删除、logging_utils 版吸收 settings.log_level 默认值并删除 level 死参数、57 个消费文件 import 全部迁移（`core.config import get_logger` 零残留）；核心 __getattr__ 惰性机制保留指向 logging_utils |
| 8 | 顺带：前端 utils 死常量 + 自写 debounce/throttle | ~~src/utils/validation.js、src/utils/format.js~~；~~useRealtimeSync.js:18-30、TaskList.vue:175-183、useThrottle.js~~ | **✅ 全部完成（2026-08-19）**：validation.js 6 个死 schema 与 format.js mergeClasses/truncateMiddle 已删（ChatRequestSchema 与 getModeLabel 在役保留）；自写 debounce/throttle 已随 B 类第一波 spec（b1-low-risk-convergence）替换为 @vueuse/core 的 useThrottleFn/useDebounceFn（useThrottle.js 整文件删除，全仓零残留） |

> 注：死代码分析原文将 realtimeEvents 标注为 rd-09，与第 7 章 rd-09（providers 样板工厂化）冲突；按主报告编号体系 realtimeEvents 对应 rd-05（即 9.1 B7），本章统一为 rd-05。

#### 删除类汇总（原合计约 445 行；**✅ 已全部完成（2026-08-19，spec: remove-dead-code-batch，除 lc-06 死符号归其收敛 spec 外）**，另加僵尸表三模型 + hierarchical_memory 共约 470+ 行）

| 项 | 删除内容 | 行数 | 连带影响 |
|----|----------|------|----------|
| lc-05 | mcp_integration.py 整文件 | 136 | **✅ 已删除（2026-08-19，A 类 spec）**：零运行时验证成立；tools/mcp/langgraph_integration.py docstring 已同步修正，全仓零残留 |
| ~~lc-06（死符号部分）~~ | ~~create_strict_rag_chain + aquery_strict_rag + STRICT_RAG_SYSTEM_PROMPT~~ | 约 207 | **✅ 已删除（2026-08-19，spec: lc06-strict-rag-convergence）**：连带 `_get_chat_model` 与 4 个失效 import（ChatPromptTemplate / StrOutputParser / Runnable / RunnablePassthrough）、services/__init__.py 死 re-export 一并清理，全仓业务代码零残留（仅测试契约断言命中） |
| PromptCacheSerializer | serializers.py:76-96 | 21 | **✅ 已删除（2026-08-19）**：零影响（views.py import 列表本就不含） |
| 僵尸表三模型（用户拍板追加） | models.py 三模型 + admin 三注册 + hierarchical_memory.py 整文件（399 行） + 0003 DeleteModel 迁移 + docker PG 开发库真实删表（context_rule / auto_memory / prompt_cache） | 约 500+ | **✅ 已彻底删除（2026-08-19）**：运行时零消费验证成立（无 REST 路由/views/urls/services/tests 引用）；迁移 sqlmigrate 实证仅作用于三张僵尸表；introspection 验证三表消失；全量 193 测试通过（含 DeleteModel 迁移在测试库完整跑通） |
| 5a | cross_session_max_context_length 死配置 + 装配 | 3 | **✅ 已删除（2026-08-19）**：零影响（无消费逻辑） |
| 5b | ai_engine/config.py:94 learning 行 | 1 | **✅ 已删除（2026-08-19）**：零影响（无调用方传 "learning"） |
| rd-04（删除部分） | usage_tracker.py:37-48 本地表 | 12 | **✅ 已删除（2026-08-19）**：get_max_tokens 已同步改调 TokenEstimator.get_model_limit（default 参数承接 settings fallback；gemini×2 已并入权威表） |
| rd-03（统一部分） | core/config.py:188-199 实现体整体删除（弃用原转发方案） | 净减 14 | **✅ 已完成（2026-08-19）**：57 个调用文件 import 全部迁至 logging_utils（零残留）；logging_utils 版吸收 settings.log_level 默认值、删除 level 死参数；按用户「不要兼容旧代码」红线未留转发代理 |
| rd-05 | realtimeEvents.js EventSource 枚举 | 8 | **✅ 已删除（2026-08-19）**：零影响（零引用）；Set 收敛接线归 B 类第一波 |
| 顺带 | validation.js 6 个死 schema | 约 40 | **✅ 已删除（2026-08-19）**：仅 ChatRequestSchema + validateSchema 被 stores/chat.js:14 消费，保留 |
| 顺带 | format.js mergeClasses / truncateMiddle | 约 8 | **✅ 已删除（2026-08-19）**：getModeLabel 被 stores/session.js:9 消费，保留 |

打包执行建议：~~一个低风险 spec 一次 PR 完成~~ **已执行完毕**。验证结论：`manage.py check` 零问题 + 全量 193/193 测试通过 + 前端 97/97 测试与 build/lint 通过 + 残留符号全仓 grep 零命中（MODEL_LIMITS 仅剩 compression.py _MODEL_LIMITS 唯一权威源）。

#### 利用类汇总（各自立项）

| 项 | 接线点 | 规模 |
|----|--------|------|
| ~~strict_rag_chain 收敛（lc-06）~~ | ~~strict_rag_chain.py 内抽 `_retrieve_with_fallback` / `_handle_empty_result` 双版本，三入口改造~~ | **✅ 已完成（2026-08-19，spec: lc06-strict-rag-convergence）**：抽 4 组共享内核（`_maybe_rewrite_query` / `_retrieve_with_fallback` / `_handle_empty_result` 各 sync+async 双版本 + `_is_degraded_result`），三入口改造完成，759 → 613 行；行为等价性由新增 17 用例锁定（sync/async 降级传参差异、事件序列、HyDE 语义、空结果分支） |
| ~~realtimeEvents 事件 Set 反向收敛（rd-05）~~ | ~~handleSessionEvent.js:404-419、handleTaskEvent.js:89-109 的 case 组替换为 Set.has 分支~~ | **✅ 已完成（2026-08-19，spec: b1-low-risk-convergence）**：两文件 13 个 case 字符串全部替换为 Set.has 分支路由 |
| ~~MODEL_LIMITS 收敛（rd-04）~~ | ~~get_max_tokens 改调 TokenEstimator.get_model_limit；gemini×2 并入 compression.py 表~~ | **✅ 已完成（2026-08-19，随死代码 spec）** |

**~~最值得优先立项：strict_rag_chain 四份收敛（lc-06）~~**——**✅ 已完成（2026-08-19）**：三在役入口收敛至共享内核后单一逻辑单点维护，759 → 613 行（实际净减 146 行：原估 300 行重复中，sync/async 双版本内核是必要保留的结构性差异，且新增内核 docstring 与 17 个等价性测试用例占据部分行数）；未来「异步非流式版」仍可零成本复活（3 行薄包装调共享内核）。

#### 并入类汇总

| 项 | 并入目标 |
|----|----------|
| ~~PromptCache 模型 + admin + 迁移（连带 serializer 决策）~~ | **✅ 已完成（2026-08-19，随死代码 spec）**：用户拍板三僵尸表彻底删除（代码 + docker PG 开发库真实删表） |
| ~~get_logger 双实现收敛~~ | **✅ 已完成（2026-08-19，随死代码 spec）**：彻底统一（弃用转发代理），57 调用点全迁移 |
| ~~自写 debounce/throttle → @vueuse~~ | **✅ 已完成（2026-08-19，spec: b1-low-risk-convergence）**：App.vue / useRealtimeSync.js / TaskList.vue 三处直改 useThrottleFn/useDebounceFn，useThrottle.js 整文件删除 |
| 双能力默认表漂移风险（ai_engine 3 条目 vs agent_hub 7 条目） | rd-01 依赖治理记录（本次新发现） |

#### 既有结论引用（context_manager 死模块，不展开）

- **anti_oscillation**：~~值得集成（接线点 `ContextManagerMiddleware.before_model`，3 处修正）。⬜ 待集成决策。~~ → **✅ 已决策（2026-08-19）：直接删除**——全仓零引用死代码，无实际震荡问题报告驱动，集成属无痛点过度设计 → **✅ 已执行删除（2026-08-20，spec: c1-lightweight-convergence；至此 rd-02 全部收口）**。
- ~~**hierarchical_memory**：建议删除（399 行，待用户确认）~~ → **✅ 已删除（2026-08-19）**：用户拍板僵尸表彻底删除后连带删除（零外部引用 + 僵尸模型唯一消费方双重死属性）。

### 9.3 建议下一步（行动清单，供直接挑选）

> **进度锚点（2026-08-19 终次更新）**：第 1、2、3、4 条已完成（spec: `.trae/specs/add-regression-test-infra/`、`.trae/specs/remove-dead-code-batch/`、`.trae/specs/b1-low-risk-convergence/`、`.trae/specs/lc06-strict-rag-convergence/`，全部终验通过）；**B 类第二批已完成（第 5 条，spec: b2-exception-hardening，后端 220/220 + 前端 111/111 + build/lint 全绿）**；第 6 条 lc-01 用户最终确认保留不动（行动清单关闭）；**剩余全部待决策项（B类第三批 4 项 + C类执行方式 + 未入清单 P2 取舍 + 数据库重置口径）已逐项拍板，完整记录见 9.4**；当轮用户指示仅固化决策、不启动实施，执行时机由用户后续指示。

1. **~~A 类打包 spec~~（✅ 已完成 2026-08-19）**：cq-01 vitest 接入（6 个测试文件 97 用例 vitest 化改写，build/lint 全过）+ cq-02 后端补测（approval 状态机 22 / 压缩 16 / 断路器 12 / retrieval 降级 7，全量 192/192 OK，**测试库已按用户决策 PG 化：settings/test.py 继承 base.py，SQLite 禁用**）+ dj-02 requirements 补 channels>=4.3.2 / channels-redis>=4.3.0 / daphne>=4.2.2 + cq-09 三级 README + .env.example；lc-05 mcp_integration.py 已删除（全仓零残留）。回归保障网已就位，C4/C5 拆分重构的前置条件满足。
2. **~~死代码删除打包 spec~~（✅ 已完成 2026-08-19，spec: remove-dead-code-batch）**：PromptCacheSerializer / cross_session_max_context_length / learning 死配置 / rd-05 EventSource / 前端 6 死 schema + mergeClasses/truncateMiddle 全部删除；rd-04 MODEL_LIMITS 收敛单一权威源（gemini×2 并入）；**rd-03 get_logger 按用户红线弃用转发代理、彻底统一**（57 调用点全迁移，`core.config import get_logger` 零残留）；**僵尸表 ContextRule/AutoMemory/PromptCache 按用户拍板彻底删除**（代码 + admin + hierarchical_memory 399 行死模块 + 0003 DeleteModel 迁移 + docker PG 开发库真实删表，introspection 验证三表消失）。终验：后端 193/193、前端 97/97、check/build/lint 全过、残留符号全仓零命中。
3. **~~B 类第一波~~（✅ 已完成 2026-08-19，spec: b1-low-risk-convergence）**（零功能影响剩余 6 项：cq-10 / cq-06 / rd-08 / rd-06 / rd-05 Set 接线 / cq-08；~~rd-03~~ 已随死代码 spec 完成）：
   - **cq-10**：axios 响应拦截器双重 stringify 调试比较包进 `import.meta.env.DEV`，生产仅执行 toCamelCase 本体；
   - **cq-06**：types/index.js 7 个工具函数清零（6 死代码直接删，validateMessage 迁 utils/validation.js 并直改唯一消费方，零 re-export 兼容）；
   - **cq-08**：sse.js 新增 parseProtocolEvent 统一解析入口（toCamelCase + type/subagent_thread_id 协议标识符还原），readSSEStream 回调自此只发已转换对象，RagView / DeepResearchView / WorkflowView / useResearchApprovalListener / useRealtimeSync 五处手动转换段全部删除；
   - **rd-06**：SSE 错误解析统一到 apiErrorHandler.extractSSEError，并根因修复旧实现 body 二次读取缺陷（json() 消费后 text() 必空）——改为 text() 单次读取 + JSON/SSE data 行双路径解析；sse.js / WorkflowView×2 / RagView 四处内联段删除；
   - **rd-08**：自写 debounce/throttle 三处全部替换 @vueuse/core useThrottleFn/useDebounceFn（trailing=true 对齐原语义），useThrottle.js 整文件删除；
   - **rd-05 Set 接线**：handleSessionEvent.js / handleTaskEvent.js 13 个 tool_call_*/approval_* case 字符串替换为 Set.has 分支路由。
   - 终验：前端 111/111 测试（新增 sse.test.js 14 用例）、build、lint（修复 2 处实施引入的 lint 问题）、残留扫描四项全部通过。
4. **~~strict_rag_chain 收敛 spec~~（lc-06）（✅ 已完成 2026-08-19，spec: lc06-strict-rag-convergence）**：死符号删除（4 符号 + 4 失效 import + services/__init__.py 死 re-export）+ 三在役入口抽 sync/async 双版本共享内核（`_maybe_rewrite_query` / `_retrieve_with_fallback` / `_handle_empty_result` + `_is_degraded_result`），3 个重复字符串收敛为模块常量；行为零变更由新增 17 个等价性测试逐分支锁定（含 sync 关键字传参 k vs async to_thread 位置传参这一易被误统一的原样差异）；759 → 613 行，调用点（rag_chat_service.py / views_rag.py）零改动；终验：全量 210/210（PG 测试库）+ manage.py check 零问题 + 死符号全仓业务代码零残留。
5. **~~B 类第二批（异常分支、权限、dev 配置）~~（✅ 已完成 2026-08-19，spec: b2-exception-hardening）**：
   - **dj-03** 异常泄漏清理 17 文件 53 处（原则：业务语义异常保留原文——ValueError/FileNotFoundError 等校验文案，系统异常通用文案 + logger.exception；覆盖 HTTP error_response、SSE error 事件、WS payload 三种出口；views 层全量补漏 12 处）；
   - **dj-01** CacheClearView 越权根因修复（前后端 scope 键错位 → 后端统一读 scope + get_permissions 动态分派 query→IsAuthenticated / 其余→IsAdminUser + 登录响应补 is_staff + 前端 isAdmin 按钮联动；新增 10 用例权限矩阵测试）；
   - **dj-20** dev.py 删除启动 ping 探测（REDIS_AVAILABLE 零残留），CHANNEL_LAYERS 固定 RedisChannelLayer，**连 InMemoryChannelLayer 静默降级分支一并删除**（多进程 WS 互不可见的隐蔽故障源）；
   - 终验：后端 220/220（PG 测试库）+ check 零问题 + 前端 111/111 + build 成功 + lint 零告警。B2 勘察发现的两处 str(e)（tasks/*.py mark_failure 透出、健康检查诊断接口）经用户拍板**均保留不动**（发布阶段前再评估）。
6. **~~lc-01 P0 设计确认~~（✅ 已关闭 2026-08-19：用户最终确认保留不动）**：白名单内命令的审批提示已在，用户会自行确认；lc-01 不再纳入整改范围，本条从行动清单移除。

### 9.4 全量方案拍板记录（2026-08-19，用户逐项确认）

> 2026-08-19 决策会话完整记录：剩余全部待决策项（B类第三批 + C类执行方式 + 未入清单 P2 项取舍 + 数据库重置口径）经用户逐项拍板。**后续执行以本节为准**——B类表（B11-B15）与 C类表中对应条目的推荐方案如与本节冲突（dj-08、cq-11、anti_oscillation 三处已推翻原推荐），以本节为准。
> ~~当轮用户指示：「b3 你就不用做了……」~~ **【状态更新 2026-08-19】该指示随后已被用户撤销：B3 四项已按本节拍板方案全部执行完成（spec: b3-prod-config-cache），终验后端 284/284 + 前端 vitest 127/127 + build/lint 零告警。本节从决策存档转为决策+执行记录。**

#### B类第三批（4 项，✅ 已全部执行完成 2026-08-19 · spec: b3-prod-config-cache）

| 项 | 拍板方案 | 执行结果 |
|----|---------|---------|
| ds-01（B11） | CHANNEL_LAYERS **提升到 base.py 单一权威**：dev.py 删除同款配置（继承 base）；test.py 保持 InMemory 覆盖；prod 继承 base 即得 Redis。不采用 prod.py 单独加一份的方案（避免 dev/prod 配置漂移） | **[✅已修复]** CHANNEL_LAYERS 上移 base.py（channels_redis + symmetric_encryption_keys），dev.py 删除同款配置，test.py 保持 InMemory 覆盖；全仓 grep 确认仅 base.py 定义 + test.py 覆盖 |
| dj-08（B12） | **推翻「最小改法」推荐，全量迁 Redis**：SystemConfig get/set 走 Django cache（Redis backend）带 TTL，删进程内 `_config_cache` dict；多 worker 不一致 + 异步未命中静默用错模型两个问题一并根除；异步未命中日志 debug→warning | **[✅已修复]** get/set 全量迁 default cache（key `system_config:{key}`，TTL 3600s），删 `_config_cache` dict 与 `invalidate_system_config_cache`；异步未命中返回 default + warning；warmup 改遍历 DB 幂等写 Redis，四处调用点不变 |
| dj-09（B13） | 会话缓存失效**单条目 + 短 TTL**：post_save 只失效当前会话详情 key；列表 key 靠短 TTL 自然过期；重命名/标题变更端点显式失效列表。代价（用户已接受）：列表 updated_at/消息数短暂陈旧 | **[✅已修复]** post_save 仅失效当前会话单条 key，post_delete 失效单条 + 列表索引 remove，patch 重命名显式失效单条 + 列表；列表索引 TTL 24h→120s；**根因修复：chat/apps.py ready() 注册 signals（此前从未激活，失效逻辑为死代码）**；新增 11 测试 |
| cq-11（B15） | **推翻「试点切片」推荐，三视图全接 31 处**（DeepResearchView 13 + RagView 9 + WorkflowView 9）。执行规范按用户文档《确保一次性修改成功（useApiTask).md》：先 composable + vitest 单测全绿 → 再逐视图迁移（DeepResearchView → RagView+WorkflowView 分阶段）；旧样板迁移期间对照、验证通过后删除（最终态零旧代码）；止损信号（options 膨胀/行为难对齐）出现即停；人工回归按 启动.md 起服务 + MCP 浏览器 | **[✅已修复]** composables/useApiTask.js + 16 单测；三视图全量迁移（DeepResearchView 11 处 + RagView 7 处 + WorkflowView 5 处 useApiTask 实例化，SSE 调用点按规范维持原逻辑）、旧样板零残留；回归中追加修复 5 个 keep-alive 视图 onMounted/onActivated 双钩子首挂双发（收敛 onActivated 单一入口 + 首挂标志），合并 DeepResearchView 3 个等价 useApiTask 重复定义 |
| rd-04（B14） | **从清单划掉**：复核核实全仓仅剩 compression.py TokenEstimator._MODEL_LIMITS 单一权威表（usage_tracker 已改调它），合并已完成，无需再做 | **[⛔已划掉]** 核实无误，全仓单一权威表 |

#### C类执行方式（8 组全部定案）

| 项 | 拍板方案 |
|----|---------|
| rd-01（C2） | ~~以实际 import 为准重写 requirements.txt + **新环境（干净 venv）安装回归验证**，逐条确认传递依赖链（spacy 全家桶/eventlet/ipython 等 40+ 条目待清）~~ **[✅已完成 2026-08-20 · spec:c2-requirements-rewrite]** requirements.txt 219 行 → 56 条直接依赖（清 162 条：spacy/jupyter 全家桶、开发工具、传递依赖固化、langchain-anthropic 零 import、langchain-milvus 未启用；补 fastapi/uvicorn/python-docx 等缺漏）；新环境 venv 安装回归 318 测试全绿 + check 0 issues + fastapi/五向量后端冒烟；三处版本修正（langgraph-store-postgres 移除 / redis <8 / deepagents >=0.7.5）；含 dj-02/rd-12 联动；验证报告见 spec 目录 |
| ds-02（C3） | **三步全做**：① 跨 app 私有成员公共化（`_merge_tool_calls_incremental`/`_keyword_overlap` 等）② 依赖方向规则重构（langgraph_adapter 反向调用改回调注入等）③ import-linter CI 契约固化 |
| cq-03（C4） | ~~**逐链路拆**（chat_service 4 个超百行函数、_persist_to_db_sync 163 行；每步独立可回归，chat 为最高频路径）~~ **[✅已完成 2026-08-20 · spec:c4-split-long-functions]** 6 个超百行函数全部拆分达标（≤80 行）：chat_service 四方法 535/204/130/113 → 38/34/12/11 行编排（新增 33 个私有方法 + 2 个 dataclass、闭包捕获显式化）；stream_persistence 161/112/85 → 62/74/70 行（10 个新函数 + 39 个新单测）；deep_builder 143→30 行（+6 私有方法）；顺带删不可达死分支 28 行 + 死赋值 2 处；行为保持，后端 378/378 + 前端 127/127 + lint-imports 零豁免全绿 |
| cq-04（C5） | ~~session.js（1731 行）**按领域拆多 store** + handleSessionEvent.js（670 行）**case 下沉 handler 模块**~~ **[✅已完成 2026-08-20 · spec:c5-split-frontend-stores]** session.js 1700→五切片领域组合（公共 API/store id/import 路径零变化，41 消费点零修改，runtimeDeps 解决跨切片循环调用）；handleSessionEvent 707→297 行纯路由 + approvalHandler/subagentHandlers/streamStateHandlers 三模块下沉 + 61 个新单测；执行中修正一处"为压缩而压缩"硬拆（approvalHandler 主流程还原 162 行连续形态）；前端 188/188 三绿、后端 378/378 无波及 |
| cq-05（C6） | ~~**优先拆 4 个千行级**：DeepResearchView 1377 / WorkflowView 1164 / ToolSelector 1114 / ChatMessage 1055（轮询抽 usePolling、对话框抽子组件）~~ **[✅已完成 2026-08-20 · spec:c6-split-giant-components]** ToolSelector 拆三 tab 子组件（契约不变调用点零修改）；DeepResearchView/WorkflowView 抽 4 个实例安全 composables + 对话框子组件；**ChatMessage 按用户原则变更锁死不拆**（样式占 53% + 高频渲染路径，拆分无收益有风险）；顺带删死样式 135 行 + api 死方法 6 个；其余 11 个超 500 行组件按机会推进 |
| dj-12 系列（C7） | **按影响拆 2-3 个 spec**：~~① 无行为变化（冗余索引 dj-12 / 信号注册 dj-17 / TextChoices dj-22 / squash dj-21）~~ **[✅第①批已完成 2026-08-20 · spec:c2-model-migration-consistency]** ~~② API 变化需前端适配（分页 dj-15 / 路由命名 dj-16）~~ **[✅第②批已完成 2026-08-20 · spec:c7b2-pagination-route-normalization]**（dj-16 learning 同病延伸已收口 2026-08-20 · spec:learning-route-convergence：task/→tasks/ + 4 项防回归测试 + views_kb get_parsers 时序 bug 根因修复 + knowledge 死代码清理） ~~③ 数据迁移 + 大重构（JSONField null dj-18 / 双索引模型合并 dj-13）~~ **[✅第③批已完成 2026-08-20 · spec:c7b3-data-model-consolidation：C7 至此全部收口]** |
| rd-02 剩余（C8） | ~~anti_oscillation.py **直接删除**（推翻 9.2「值得集成」结论：零引用 + 无痛点驱动，接线属过度设计）；rd-02 至此全部收口~~ **[✅已执行 2026-08-20 · spec:c1-lightweight-convergence]** |
| dj-14 | ~~get_absolute_url **删除**（参数名错误 + 无消费方，不修复保留）~~ **[✅已执行 2026-08-20 · spec:c1-lightweight-convergence：6 处全删，全仓零残留]** |

#### 新增纳入（原未入 C类清单的 P2 项，用户勾选 3 项）

- ~~**dj-07** 知识库路由双轨收敛（/indices/ 与 /knowledge-bases/ 两套等价端点收敛单一资源体系，前端同步适配）~~ **[✅已修复 2026-08-20 · spec:c1-lightweight-convergence]**
- ~~**dj-10** 知识库校验规范化（标准 is_valid + 带码自定义异常，去掉以异常文案字符串做业务分支）~~ **[✅已修复 2026-08-20 · spec:c1-lightweight-convergence]**
- ~~**cq-07** 双异常体系统一（LCAgentException / AgentHubError 引入共同基类 + classify_exception 静默 ImportError 降级加日志）~~ **[✅已修复 2026-08-20 · spec:c1-lightweight-convergence]**
- cq-13（宽捕获收窄）未选，暂不做

#### 数据库重置口径（影响 dj-18 / dj-21 迁移写法）

开发阶段数据可丢，**整库可删除重做**：dj-18/dj-21 等迁移按「可重置」最简方案写。重建种子数据只需：用户 `daixing` / 密码 `daixing241212` + 大模型相关配置（厂商、模型、工具）；对话、任务等业务数据可抛弃。

#### 同日拍板的范围外保留项（dj-03 边界，已随修复标记记录）

- `tasks/*.py` mark_failure(error_message=str(e)) 透出异常原文：**保留不动**（开发阶段排障优先，发布阶段前再评估）
- db_monitor 两处（普通用户可见）+ mcp 一处（无调用方）健康检查 str(e)：**全部保留不动**（诊断价值优先）

---

## 10. 附录：ontology 图谱查询指南

### 10.1 数据位置与结构

本次评估的全部 80 条发现已作为 **Note 实体**登记至 ontology 图谱：

- **图谱文件**：`memory/ontology/graph.jsonl`（项目根相对路径）
- **实体类型**：Note（80 条，与本文报告一一对应）
- **属性结构**：
  - `content`：`[优先级][发现 ID] 标题 -- 问题描述 建议: 改进建议`
  - `tags`：`[维度, 优先级]` 二元数组，维度取值为 `django` / `langchain` / `code-quality` / `design` / `redundancy`，优先级为 `P0`-`P3`
  - `refs`：代码位置数组（相对路径:行号）
- **关联关系**：每条 Note 通过 `has_finding` 关系挂接 Project 实体（relation properties 含 dimension 与 priority）

### 10.2 查询命令

在**项目根目录**运行以下命令（PowerShell 环境下 JSON 参数内的双引号需以 `\"` 转义）：

**示例 1：查询 LangChain 维度的 P0 发现（当前唯一高危项）**

```powershell
python .trae/skills/ontology/scripts/ontology.py query --type Note --where '{\"tags\": [\"langchain\", \"P0\"]}'
```

**示例 2：查询 Django 维度的全部 P1 发现**

```powershell
python .trae/skills/ontology/scripts/ontology.py query --type Note --where '{\"tags\": [\"django\", \"P1\"]}'
```

**示例 3：列出全部 80 条 Note（自行按 tags/refs 过滤）**

```powershell
python .trae/skills/ontology/scripts/ontology.py list --type Note
```

### 10.3 使用注意

1. `--where` 匹配语义为**严格相等**（非包含匹配）：`tags` 必须提供完整二元数组 `[维度, 优先级]`，仅传 `{"tags":["P1"]}` 不会命中任何记录。
2. 常用维度-优先级组合速查：

| 维度 tag | P0 | P1 | P2 | P3 |
|----------|----|----|----|----|
| django | 0 | 6 | 9 | 7 |
| langchain | 1 | 2 | 9 | 6 |
| code-quality | 0 | 4 | 5 | 4 |
| design | 0 | 2 | 6 | 7 |
| redundancy | 0 | 2 | 5 | 5 |

3. 修复一条发现后，可通过 `update` 命令回写 Note 属性（如增加 `status: resolved` 标记）实现整改闭环，命令格式参见 `python .trae/skills/ontology/scripts/ontology.py --help`。

---

*报告完。生成于 2026-08-19，基于 5 份专项评估发现文件（dim2-django / dim3-langchain / dim4-quality / dim5-design / dim6-redundancy，共 80 条发现）汇总撰写。*

---

## 11. 重构后复审（2026-08-20 · spec:reassess-post-refactor-quality）

> 本章为整改主线（A/B/C 全类 12+ 批）完成后的**终局复审**，是当前项目状态的权威视图（前 10 章保留为历史评估记录）。方法：以重构前快照 `D:\programming\langchain\langchain_xm_\langchain_xm`（含前后端全量代码）为只读基线做功能完整性对比审计 + 全部 ✅ 标记真实性总核 + 五维度/Vue3 专项重评。基线：后端 378/378 + check + lint-imports 3 kept 0 broken；前端 188/188 + build + lint。

### 11.1 功能完整性审计结论

**总判定：后端零功能回归；前端 1 处真实功能回归（dj-01 残余 R1）；事件契约零漂移。**

#### 后端（对比重构前，逐 app 路由/契约/链路语义对照）

| 审计维度 | 结论 |
|----------|------|
| URL 路由 diff | 15 个 app 无增删；全部差异可追溯：dj-07（knowledge-bases 收敛，含上传端点等价迁移）、dj-16（task→tasks/result 删除/chat 删除路由合并）、dj-06（tools 资源化 25→17 条，逐视图验证等价）、dj-15（分页结构逐字段一致，越界页码行为差异为 spec 声明） |
| 事件契约 diff | **event_schema.py 新旧逐行一致**（36 个 EventType/41 项 ws_event_name 映射/必填字段）；realtime_sync/tool_call_lifecycle 逐行一致；发布点抽样（STREAM_*/APPROVAL_*/TOOL_CALL_*/MESSAGE_*）payload 逐字段一致——**零静默契约变化** |
| 核心链路语义 | 六链路抽样等价：chat 流式（编排顺序/model_fallback 事件序/persist 合并策略逐行等价；cb 跨 with 块传递经 `__exit__` 空操作证实等价）；审批状态机（仅函数公开化+State 常量路径）；子代理（spawn/wait 逐行一致）；深研回写（writeback payload 逐行一致）；learning（仅 1 行 logger 导入源差异）；RAG 降级链（逐行一致） |
| settings/迁移 | base.py 仅 2 处差异（CHANNEL_LAYERS 上移 ds-01/分页类 dj-15）；prod.py 零差异；squash replaces 完整性验证通过（chat 20/20、approvals 13/13、learning 10/10） |

行为变化提示（spec 有意设计，非回归）：① dev 环境 Redis 不可用时 WebSocket fail-fast（不再静默降级 InMemory，ds-01 设计）；② tools 写操作 HTTP 方法 POST→PUT/DELETE/PATCH（dj-06，前端已适配）。

#### 前端（对比重构前）

| 审计维度 | 结论 |
|----------|------|
| store API diff | 旧 session.js 68 项导出 → 新 50 项：4 个内部 state 收紧（零外部消费有 grep 证据）、8 项死代码清理（旧仓零消费）、11 个 setter 死代码、`_pendingResearchTaskIds` 恒空死链——**无真实丢失**；装配链 sync.js 新旧零差异 |
| 组件契约 diff | ToolSelector props/emits 逐字一致；ChatMessage 零变化（独立 diff 证实）；ContinueResearchDialog 表单/提交语义逐字段等价；消费点接线完整 |
| api 方法 diff | workflow.js 删 6 方法零引用复核通过；research/chat/knowledge/tools 路由适配调用点完整（25+ 处 URL 变化均有后端协同依据） |
| 路由/装配 | router/index.js 与 main.js 双侧 diff 为空 |
| composables | 旧视图函数全部找到新家（无"既没留视图也没进 composable"的丢失） |

#### 🔴 唯一真实回归：dj-01 残余（R1 + 组合绕过）

- **R1 前端 isAdmin 恒 false**：`stores/user.js:59` 读 `userData.is_staff`，但 axios 响应拦截器 toCamelCase（api/axios.js:66-82）已将后端 `is_staff` 键转为 `isStaff` → `userInfo.is_staff` 恒 undefined → `isAdmin`（user.js:23）恒 false → KnowledgeBaseView.vue:446「清空全部缓存」按钮**对所有用户（含管理员）不可见**（旧版无条件显示）。同时违反命名规范红线（前端 store 内 snake_case 键）。
- **后端 pattern 组合绕过**：`cache_manager/views.py:199` `if pattern:` 分支优先于 `:202 elif scope == "query"`，普通用户 POST `{scope:"query", pattern:"*"}` 时 get_permissions 仅返回 IsAuthenticated，post 内直接执行 `CacheService.delete_pattern`（无白名单）。
- 两处修复均 <10 行，**建议立即立项**（本复审批按红线未顺手修改业务代码）。

次要：sessionCrud.js:109 陈旧注释残留（对应已删除的乐观删除标记，无行为影响）。

### 11.2 原问题解决状态总核（80 项终局盘点）

| 终局状态 | 数量 | 占比 |
|---------|------|------|
| 已解决（磁盘核验/采信） | 47 | 59% |
| 部分解决 | 6（dj-01、lc-09、lc-15、cq-05、ds-11、rd-07） | 7.5% |
| 用户保留 | 3（lc-01、lc-03、cq-13） | 3.75% |
| 未解决 | 24 | 30% |

抽样深核 20+ 项：18 项与磁盘完全一致（含行号级吻合），**标记总体真实性高**。

**漂移修正**（本复审已执行/待办）：
- 负向：dj-01 由"✅已修复"降级"🔶部分解决"（**已修正**，残余见 11.1）；cq-07 位置勘误（BaseAppError 实际在 common/exceptions.py:31，非 core）；cq-08 行号勘误（sse.js:174）
- 正向：**ds-15 实际已解决**（ResilientModel 已下沉 ai_engine/services/resilient_invoker.py，model_provider.py:24 同包导入，报告原未标）；**cq-09 实际已解决**（A 类批次完成三级 README + .env.example，明细表漏标）；rd-07 的 tenacity 子问题已失效（requirements 已删）；lc-15 半失效（strict_rag 侧已收敛，approval/policies.py:157-158 残留）

### 11.3 五维度重评 + Vue3 规范专项

#### 新发现问题清单

| 优先级 | ID | 问题 | 位置 | 建议 |
|--------|-----|------|------|------|
| **P1** | dj-30 | 五个 app 零测试（users/attachments/core/analytics/realtime），含认证/限流/权限基类/文件校验等安全关键路径 | apps/{users,attachments,core,analytics,realtime} 无 tests/ | 优先补 users 登录注册 + core 认证类回归测试 |
| **P1** | dj-01R | dj-01 两处残余（见 11.1，前端 isAdmin 键名错位 + 后端 pattern 组合绕过） | user.js:23/59、cache_manager/views.py:199 | <10 行/处，立即立项 |
| P2 | cq-14 | 超长函数残留：adapter.py astream_research_with_interrupts **764 行**、approval/middleware.py aafter_model 440 行、stream_chunk_processors 312 行（全仓超百行函数 64 个，多数符合不硬拆原则，此三处为真问题） | research/services/adapter.py:224 等 | chunk 消费循环拆协程子函数；chat_session_id 提取独立 |
| P2 | lc-02 | Prompt 双轨制：prompts.yaml 模板化 vs 散落硬编码约 20 处 | guardrails/middleware.py:49、research/subagents.py、knowledge/retrieval_service.py 等 | 分批迁 prompts.yaml |
| P2 | dj-31 | MIDDLEWARE 在 dev/prod 完全重复定义（15 项逐行一致），base.py 漏共享 | settings/dev.py:17-33 vs prod.py:18-34 | 上移 base.py |
| P2 | vue-01 | 前端 console.log 残留 9 处绕过统一 logger | ChatView.vue:109 等 | 替换 logger |
| P2 | vue-02 | 超 500 行组件实为 **18 个**（原口径 11，多 7 个需复核）；ChatMessage 1188 行逼近 1200 | 详见 Task 5 清单 | 按机会推进，ChatMessage 下一个优先 |
| P2 | fd-01 | 循环依赖靠函数级延迟导入维持（跨 app import 403 行中 245 行延迟导入，ai_engine↔knowledge 真实环） | deep_builder.py:26-31 等 | 延续 cross_app 门面收敛 |
| P3 | dj-32 | 动词式路由残留约 10 处（sessions/create/、commands/execute/、cache invalidate/clear 等） | chat/urls.py:14 等 | 动作型端点可辩护，择优收敛 |
| P3 | cq-15 | 前端未引用导出约 16 个（toolAdapters/subagentStatus/researchTaskStatus 等状态映射与注册 API） | utils/ 多文件 | 确认后删除 |
| P3 | dj-33 | dev.py 冗余覆盖两处（CORS_EXPOSE_HEADERS/DEFAULT_THROTTLE_RATES 与 base 逐值相同） | settings/dev.py:51-63/74 | 删即可 |
| P3 | vue-03 | handleTaskEvent.js:169 `eventType || event_type` 防御性回退灰区（允许未转换 payload 进入分发） | stores/sync/handleTaskEvent.js:169 | 收紧为仅 eventType |

#### 评分对比

| 维度 | 原评分 | 复审分 | 依据 |
|------|--------|--------|------|
| Django 规范 | 7 | **7.5** | 视图/模型/信号/分页规范到位；扣 MIDDLEWARE 重复、5 app 零测试 |
| LangChain | 8 | **8** | 架构扎实；prompt 双轨与延迟导入持平未恶化 |
| 代码质量 | 7 | **7** | 拆分成效实证；764 行巨函数与测试分布头重脚轻 |
| 功能设计 | 7 | **7.5** | N+1 防护/异步/沙箱守卫实证到位；chat→ai_engine 46 次耦合扣分 |
| 冗余死代码 | 6.5 | **7** | print 零残留/console.log 仅 9 处/死导出可清单化 |
| Vue3 专项（新增） | — | **8** | script setup 100%/命名零违例/懒加载全覆盖/setup store 统一；扣 18 个超 500 行组件与组件层零测试 |

#### 实证健康项（抽检通过，无需处理）

`<script setup>` 覆盖率 100%（零 Options API）；组件 PascalCase/目录 kebab-case 零违例；路由懒加载 100%、keep-alive 机制一致；13 个 Pinia store 全 setup 风格（最大 911 行）；后端 print() 零残留；模型 choices 全部 TextChoices；Meta 索引合理；N+1 防护 38 处；sync_to_async 均伴随 close_old_connections；snake_case 键名违例除 R1 外为零（api/subagent.js 的 parent_thread_id 为 URL query 参数，规范允许）。

### 11.4 剩余行动项（终局优先级排序）

**立即（P1）**：dj-01R 两处残余小修（<10 行/处）；dj-30 补 users/core 安全路径测试。

**计划内（P2）**：ds-05（JWT query 传输）/ds-03（非流式同步阻塞 120s）/ds-08（同步库混入异步）/lc-04（审批注入 fail-open）/lc-11（checkpointer 删除不清 Redis）/ds-07（回调仅存进程内存）/dj-11（threading.local→contextvars）/ds-06（chat_service 1323 行）/rd-07 剩余（韧性框架收敛）/lc-07/08/10/12（LangChain 实践）/ds-11/lc-09（门面与环解耦）/cq-14/lc-02/dj-31/vue-01/vue-02/fd-01（本复审新增）。

**低优先（P3）**：ds-09/10/12/13/14、lc-13/14/15 剩余/16/17/18、rd-09/10/11、dj-32/33、cq-15、vue-03、cq-05 剩余 11+7 组件。

**用户保留**：lc-01（P0 白名单，需设计方向确认后可启动）、lc-03（fail-open）、cq-13（宽捕获）。

**部署前必做验证**（本审计静态完成、需环境演练）：squash 迁移 + knowledge 三段式在 staging 存量库演练 migrate；dj-09 signals 激活后线上缓存命中率观察。
