这套是 Claude Code、Trae IDE、Cursor 深度模块原生采用的标准交互方案，在基础方案C之上做完整能力升级，完美适配你「仅深度研究存在子Agent、仅展示工具调用」的场景。

## 整体渲染结构
```
【顶层：子代理分组卡片（以唯一 agentPath 划分独立单元）】
 ├─ 卡片头部聚合区
 │  子代理名称、agentPath 层级面包屑、总耗时、工具统计、整体运行状态、折叠控制按钮
 └─ 卡片内部容器：基于 parentToolCallId 递归缩进的工具调用树
     └─ 若工具内触发深层子Agent，直接在当前工具节点内嵌全新子代理分组卡片
```

## 核心升级特性
1. **原生支持多层递归子代理**
普通单层分组方案仅能承载一级子代理，遇到子代理内部再调用次级子代理时页面结构会混乱；本方案支持无限层级内嵌卡片，和 LangGraph 多层执行栈完全对齐，递归场景无结构错乱问题。

2. **分组头部完整聚合信息**
每个Agent卡片统一汇总全局概览信息：层级链路面包屑、运行总耗时、工具调用总量、成功/失败/进行中数量、整体执行状态，同时提供快捷操作入口（单独重试子代理、一键折叠），用户无需逐条查看工具就能掌握整体进度。

3. **智能自动折叠规则**
- 当前正在运行的子代理卡片默认展开，实时展示执行过程；
- 正常执行完毕的子代理默认折叠，减少页面冗余信息，降低视觉干扰；
- 异常、失败终止的子代理强制展开并高亮，优先暴露问题，便于调试。

4. 两层层级职责完全解耦
- Agent分组卡片：宏观任务单元，用来区分不同子代理、查看整体进度；
- 内部缩进树形：微观执行链路，清晰展示单代理内部工具的先后、嵌套调用关系。

## 业务场景专属落地规范
1. 普通聊天会话
保留原有单层工具缩进展示逻辑，不启用Agent分组卡片，和深度研究页面视觉做区分。

2. 深度研究页面渲染逻辑
- 根主代理自动生成一级外层分组卡片；
- 卡片内工具列表按照 parentToolCallId 递归缩进渲染；
- 任意工具触发新子Agent时，在该工具节点内部内嵌独立子代理卡片；
- 前后端共用同一套执行栈数据源，事件统一携带 depth、agent_path 字段，彻底解决页面刷新后子代理层级丢失、前后端状态不一致问题。

## 成熟产品配套高阶交互
1. 支持单独重启执行失败的子代理；
2. 鼠标悬浮展示完整 agentPath、工具入参、返回详情；
3. 视觉区分同步工具调用、嵌套异步子代理调用，降低阅读成本。
