# 项目质量整改总纲（持续目标 · 含进度状态）

> 本文件是整个整改主线的任务总纲。每完成一批，对应条目标 ✅ 并注明 spec 名。
> 上下文丢失时：先读 `上下文信息.md`（环境命令/红线约束/详细交付记录），再回到本文件继续。
> 最后更新：2026-08-20（learning 路由收敛 + knowledge 死代码清理完成；C 类已推进 6 批，剩余项见「八、下次任务提示词」）

## 一、分级概览（含完成状态）

1. **A类（4项）✅ 全部完成（spec:add-regression-test-infra，2026-08-19）**
- ✅ cq-01 前端接入 vitest（现有 111 用例）
- ✅ cq-02 后端补关键链路测试（现后端共 220 用例，PG 测试库）
- ✅ dj-02 requirements 补 channels / channels-redis / daphne
- ✅ cq-09 三级 README + .env.example
- ✅ 顺带删除 lc-05 死文件 mcp_integration.py（136 行，零引用核实）

2. **B类（15项，分三批）——已全部完成**
- ✅ **第一批（低风险等价收敛）完成**（spec:b1-low-risk-convergence）：生产调试日志关闭、双 get_logger 收敛（rd-03，57 处调用迁移）、前端死工具函数迁移、sse 统一解析（parseProtocolEvent）、节流防抖替换 @vueuse/core、sse 错误处理统一（extractSSEError 重写）、realtimeEvents 死导出清理 + Set 枚举接线（rd-05）。
- ✅ **第二批（异常分支、开发环境）完成**（spec:b2-exception-hardening）：
  - ✅ dj-03 str(e) 异常泄漏清理（17 文件 53 处，原则：业务语义保留/系统异常隐藏）
  - ✅ dj-01 CacheClearView 权限收紧 + 前后端参数键对齐（scope 键错位根因修复 + is_staff/isAdmin 联动）
  - ✅ dj-20 dev.py Redis 启动探测删除，固定 RedisChannelLayer（含删 InMemory 静默降级分支）
- ✅ **第三批（生产配置、缓存语义）完成**（spec:b3-prod-config-cache，2026-08-20）：
  - ✅ ds-01 CHANNEL_LAYERS 提升到 base.py 单一权威（dev 不再单独配）
  - ✅ dj-08 SystemConfig 缓存全量迁 Redis（删进程内 dict；多 worker 不一致 + 异步用错模型一并根除）
  - ✅ dj-09 会话缓存失效改单条目 + 短 TTL（重命名端点显式失效列表；根因修复 chat/apps.py ready() 注册 signals）
  - ✅ cq-11 useApiTask 三视图全接 31 处 + 5 个 keep-alive 视图 onActivated 首挂标志收敛 + 合并 3 个重复 useApiTask 定义
  - ✅ rd-04 划掉（核实已完成：全仓仅剩 TokenEstimator._MODEL_LIMITS 单一权威表）

3. **C类（单独 spec 专项设计）——已推进 5 批，剩余项见第八节**
- lc-01 shell_exec 白名单前缀绕过（P0）：**用户明确保留不动**（有审批提示，用户自行确认）
- ✅ **C类第一批**（spec:c1-lightweight-convergence，2026-08-20）：rd-02 收口（anti_oscillation.py 删除）+ dj-14（get_absolute_url 6 处删除）+ cq-07（BaseAppError 双异常体系统一）+ dj-10（知识库 Serializer 规范化 + 类型化异常）+ dj-07（知识库 /indices/→/knowledge-bases/ 路由收敛，前端 100% 适配）
- ✅ **C类第二批 / C7 第①批**（spec:c2-model-migration-consistency，2026-08-20）：dj-12 冗余索引清理 + dj-17 信号注册根因修复 + dj-21 squashmigrations（chat 20→1 / approvals 13→1 / learning 10→1）+ dj-22 choices 全仓 TextChoices 化（approvals 175 处调用点）
- ✅ **C7 第②批**（spec:c7b2-pagination-route-normalization，2026-08-20）：dj-15 ProjectPagination 统一分页（approvals 真分页删 [:100] + chat/knowledge 手写收敛 + 前端死函数删除）+ dj-16 路由规范化（research result 删 / task→tasks / chat 删除路由 RESTful 化，前端 4 调用点 100% 适配）
- ✅ **C2**（spec:c2-requirements-rewrite，2026-08-20）：rd-01 + dj-02 + rd-12 联动，requirements 219 行 → 56 条直接依赖，新环境 venv 安装回归 318 测试全绿
- ✅ **C7 第③批**（spec:c7b3-data-model-consolidation，2026-08-20）：dj-18 ChatMessage JSONField 去 null（+18 处双轨读点死防御清除 + NULL 归一迁移）+ dj-13 双索引模型合并（DocumentIndex 彻底删除、IndexMetadata 唯一实体、文件数派生查询、显示语义修正、迁移拆三段修复 PG pending trigger events、存量库 migrate 冒烟 + 数据完整性验证通过）——**C7 至此全部收口**
- ✅ **learning 路由收敛**（spec:learning-route-convergence，2026-08-20）：dj-16 learning 延伸（task/→tasks/ + name/类名对齐 + 前端 1 调用点 + 4 项防回归测试 + schema.yml 重生成）+ 顺带根因修复 views_kb.py get_parsers 时序 bug + knowledge 死代码清理（IndexInfoSerializer / views.py 垫片）
- **用户勾选新增（dj-07/dj-10/cq-07）**：✅ 已于 C1 完成（cq-13 暂不做）
- 数据库重置约定：开发阶段整库可删重做，种子只需 daixing 用户 + 大模型配置（厂商/模型/工具）

## 二、死代码与收敛（原 9.2 分析）✅ 基本完成

1. ✅ 直接删除集合（spec:remove-dead-code-batch + 前置分析）：mcp_integration.py、PromptCacheSerializer、僵尸表三模型（ContextRule/AutoMemory/PromptCache + 迁移 + hierarchical_memory.py 399 行）、前端死 schema/常量、多处无效配置、core/__init__ 转发层等，净删约 900+ 行。
2. ✅ 利用/收敛改造：
   - lc-06 strict_rag_chain 收敛（spec:lc06-strict-rag-convergence）：三入口收敛共享内核，净减 146 行 + 17 个等价性测试
   - rd-05 realtimeEvents：Set 枚举替换（随 b1 完成）
   - rd-04：本地表已删、get_max_tokens 已改调 TokenEstimator（两份配置完整合并的确认归 B类第三批）
   - rd-03 get_logger：统一到 logging_utils，未留转发（按「不兼容旧代码」红线）
3. ✅ 并入其他任务：debounce/throttle 已替换 vueuse、配置漂移已删。

## 三、执行路线进度（原推荐顺序）

| 步骤 | 任务 | 状态 |
|------|------|------|
| 1 | A类打包 spec（测试基建 + lc-05 顺带删除） | ✅ 完成 |
| 2 | 死代码删除 spec | ✅ 完成 |
| 3 | B类第一波低风险收敛 | ✅ 完成 |
| 4 | lc-06 strict_rag_chain 收敛 | ✅ 完成 |
| 5 | B类第二批（异常/权限/dev配置） | ✅ 完成 |
| 6 | B类第三批（生产配置/缓存语义） | ✅ 完成（spec:b3-prod-config-cache，2026-08-20） |
| 7 | C类专项（rd-01 → ds-02 → cq-03/04/05 → dj-12 系列 → rd-02 剩余） | 🔶 部分完成：已收口 5 批（C1 / C2模型迁移 / C7-2 / C2依赖 / C7-3），剩余见第八节 |

另：早期已完成 normalize-tools-rest-fix-chat-api（dj-04 N+1 / dj-05 缓存翻页 / dj-06 tools REST 资源化）与 unify-context-injection，均先于本路线执行。

## 四、不变约束（每次动手前核对）

1. 拒绝任何形式的补丁修复，严禁兜底策略，必须直面根因彻底解决；
2. 严格依据 `d:\programming\langchain\langchain_xm\.trae\rules\SpecificationRequirements.md` 开发，不合规代码直接修改，不要别名、映射；
3. 冗余、多余的死代码坚决删除（僵尸表、死模块一律彻底清除，数据库和代码都要处理）；
4. 允许大范围重构，必要时重写相关模块；前后端所有调用点 100% 全部适配（不兼容旧代码）；
5. 测试直接使用 PostgreSQL（贴合实际运行环境，禁 sqlite/内存库）；
6. 数据库在 Docker 容器中的 PostgreSQL；
7. 项目功能前端实测正常，修复必须认真仔细，不能引入回归；
8. 拿不稳的先讨论商量，不瞎猜不幻想；
9. 每个阶段新建 spec（`.trae/specs/<名称>/`），完成后提醒用户并说明做法；
10. 每批完成后跑全量回归（后端 220 + 前端 111 + build + lint），并更新《项目质量分析报告.md》对应条目为 `[✅已修复 日期 · spec:名称]`。

## 五、决策状态记录（B类第二批遗留项，均已拍板）

1. `tasks/*.py` mark_failure(error_message=str(e)) 透出异常原文：**用户已决策（2026-08-19）保留不动**——开发阶段排障优先，发布阶段前再评估。
2. db_monitor / mcp 健康检查类 str(e)：**用户已决策（2026-08-19）全部保留不动**——开发阶段排障优先，发布阶段前再统一收紧。
3. 两处均不属于 dj-03 views 层整改范围，后续 str(e) 扫描勿误改。

## 六、环境与基线速查（接手必读）

- **后端**：conda 环境 `D:\Anaconda_envs\envs\langchain_xm\python.exe`（Python 3.11.15），命令用 `conda run -n langchain_xm --no-capture-output python manage.py <cmd>`（新 shell 中 `conda activate` 不可用）；cwd 为 `backend/Django_xm`。
- **前端**：`frontend/langchain-vue`，Node v20.19.5（先 `nvm use v20.19.5`）。
- **数据库**：Docker PostgreSQL（开发库）；测试走 PG 测试库（settings/test.py 自动建 test_ 库，禁 sqlite）。
- **基线数字**：后端全量 **330/330**（PG 测试库；命令须含 `Django_xm.apps Django_xm.common Django_xm.services.fastapi_service` 三模块，否则漏计）+ `manage.py check` 0 issues + `makemigrations --check` No changes；前端 vitest **127/127** + `npm run build` + `npm run lint`（oxlint+eslint）零告警。任何改动后必须回到此基线。
- **注意**：`manage.py test --parallel` 在 Windows 下报 `cannot pickle '_contextvars.Context'`（Django 测试运行器限制），用串行全量。
- **改动未 commit**（用户同意 commit，提交前先问）。

## 七、红线约束（每次动手前默念，承接第四节）

1. 拒绝任何形式的补丁修复/兜底策略，必须根因彻底解决；2. 严格依据 `.trae/rules/SpecificationRequirements.md`（snake_case/camelCase 边界、sessionTransformers 唯一转换边界、协议标识符保护），不合规直接改不搞别名；3. 死代码坚决删除（僵尸表/死模块/死函数，代码+数据库都处理）；4. 允许大范围重构，**前后端所有调用点 100% 适配，漏一处就炸页面**，不兼容旧代码；5. 测试用 PostgreSQL；6. 拿不稳的先讨论不瞎猜；7. 每阶段新建 spec（spec.md + tasks.md + checklist.md），完成后更新质量报告 `[✅已修复 日期 · spec:名称]` + 上下文信息.md + 本文件；8. 实时同步是统一底层能力，禁止按模块打补丁；9. lc-01 P0（shell_exec 白名单）用户保留不动。

## 八、下次任务提示词（交接给接手者）

> 上一会话（2026-08-20）已完成：B类三批全完 + C1 / C2模型迁移 / C7-2 / C2依赖 / C7-3（数据模型收敛，C7 全部收口）/ **learning 路由收敛（dj-16 延伸 + knowledge 死代码清理）** 六批。接手后先读本文件全文 + `上下文信息.md` + `项目质量分析报告.md` 对应章节，再开工。

### 0. 开工前验证基线
跑 `conda run -n langchain_xm --no-capture-output python manage.py test Django_xm.apps Django_xm.common Django_xm.services.fastapi_service`（期望 330/330 OK，注意必须带全三模块否则漏计）→ `manage.py check` → 前端 `npx vitest run`（127/127）。确认全绿再动手；若有红，先修回基线再开始新任务。

### 1. 推荐下一批：C3（ds-02 跨 app 依赖治理 + import-linter）
- 方案已拍板（2026-08-19）：① `_merge_tool_calls_incremental` / `_keyword_overlap` 等跨 app 私有成员公共化；② 依赖方向规则重构（langgraph_adapter 反向调用改回调注入等）；③ import-linter CI 契约固化。结构性重构，跨 6+ 文件，**必须先立 spec 拆步骤**。
- 是 C4/C5 拆分的前置（依赖边界不清时拆函数/组件会更乱），建议优先。

### 2. 备选：C4（cq-03 后端长函数拆分）
- chat_service 4 个超百行函数 + `_persist_to_db_sync` 163 行，逐链路拆，每步独立可回归；chat 为最高频路径，先补测试再拆（cq-02 已就位）。

### 3. 备选：C5/C6（cq-04 session.js 拆多 store / cq-05 巨型组件拆分）
- 前端大重构，依赖 vitest 兜底（已就位）。cq-05 优先拆 4 个千行级：DeepResearchView 1377 / WorkflowView 1164 / ToolSelector 1114 / ChatMessage 1055。

### 4. 交接注意事项
- 质量报告当前「26 项关闭」（总分 7.1 未变，评分不随关闭项调整）；每完成一项更新对应标记 + 总分行计数。
- C2 依赖重写后 **requirements.txt 只有 56 条直接依赖**，新环境复原以该文件为准（传递依赖自动解析）；redis 上界 `<8`、deepagents `>=0.7.5` 两条约束不要动。
- 三个 app（chat/approvals/learning）迁移已 squash 为单 `0001_squashed.py`（replaces 机制），**新迁移请挂在 0001_squashed 之后**；knowledge 迁移现为 0001-0007 链（0005/0006/0007 为 dj-13 三段迁移：DDL/数据回填/FK 切换，**DDL 与 DML 必须分迁移，勿混入同事务**——PG deferred CREATE INDEX 与 DEFERRABLE FK 的 pending trigger events 冲突）。
- 路由变更后记得重新生成 schema.yml（`manage.py spectacular --file schema.yml`）——views_kb.py 的 get_parsers 时序 bug 已修（类级 parser_classes），生成命令可正常工作。
- **编辑器缓存异常警告**：本轮遭遇 Edit 工具报告成功但磁盘未变、Read 显示新内容但 Python 读旧代码的缓存脱节问题；关键编辑后用 `Select-String` 验证磁盘真实状态，异常时用 Python 脚本直接写盘。
- 若接手时遇到与实时同步/审批相关的功能问题，先看 `.trae/rules/` 与记忆中的架构约束（统一底层、单通道），勿按模块打补丁。
- 待决策项：按红线「拿不稳先讨论」向用户讲清问题/位置/影响/选项后再动手。
