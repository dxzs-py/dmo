# 项目质量整改总纲（持续目标 · 含进度状态）

> 本文件是整个整改主线的任务总纲。每完成一批，对应条目标 ✅ 并注明 spec 名。
> 上下文丢失时：先读 `上下文信息.md`（环境命令/红线约束/详细交付记录），再回到本文件继续。
> 最后更新：2026-08-19（B类第二批完成 + 剩余全量方案拍板）

## 一、分级概览（含完成状态）

1. **A类（4项）✅ 全部完成（spec:add-regression-test-infra，2026-08-19）**
- ✅ cq-01 前端接入 vitest（现有 111 用例）
- ✅ cq-02 后端补关键链路测试（现后端共 220 用例，PG 测试库）
- ✅ dj-02 requirements 补 channels / channels-redis / daphne
- ✅ cq-09 三级 README + .env.example
- ✅ 顺带删除 lc-05 死文件 mcp_integration.py（136 行，零引用核实）

2. **B类（15项，分三批）——已完成前两批**
- ✅ **第一批（低风险等价收敛）完成**（spec:b1-low-risk-convergence）：生产调试日志关闭、双 get_logger 收敛（rd-03，57 处调用迁移）、前端死工具函数迁移、sse 统一解析（parseProtocolEvent）、节流防抖替换 @vueuse/core、sse 错误处理统一（extractSSEError 重写）、realtimeEvents 死导出清理 + Set 枚举接线（rd-05）。
- ✅ **第二批（异常分支、开发环境）完成**（spec:b2-exception-hardening）：
  - ✅ dj-03 str(e) 异常泄漏清理（17 文件 53 处，原则：业务语义保留/系统异常隐藏）
  - ✅ dj-01 CacheClearView 权限收紧 + 前后端参数键对齐（scope 键错位根因修复 + is_staff/isAdmin 联动）
  - ✅ dj-20 dev.py Redis 启动探测删除，固定 RedisChannelLayer（含删 InMemory 静默降级分支）
- ⬜ **第三批（生产配置、缓存语义）——方案已全部拍板（2026-08-19）**：
  - ds-01 CHANNEL_LAYERS 提升到 base.py 单一权威（dev 不再单独配）
  - dj-08 SystemConfig 缓存全量迁 Redis（删进程内 dict；多 worker 不一致 + 异步用错模型一并根除）
  - dj-09 会话缓存失效改单条目 + 短 TTL（重命名端点显式失效列表）
  - cq-11 useApiTask 三视图全接 31 处（执行规范：`确保一次性修改成功（useApiTask).md`；先 composable+单测后视图；旧样板验证通过后删除）
  - rd-04 划掉（核实已完成：全仓仅剩 TokenEstimator._MODEL_LIMITS 单一权威表）

3. **C类（单独 spec 专项设计）——方案已全部拍板（2026-08-19），待执行**
- lc-01 shell_exec 白名单前缀绕过（P0）：**用户明确保留不动**（有审批提示，用户自行确认）
- rd-01 requirements 以实际 import 为准重写 + 新环境安装回归验证
- ds-02 跨 app 依赖治理三步全做：私有成员公共化 + 依赖方向重构（回调注入）+ import-linter CI
- cq-03 后端长函数逐链路拆（chat_service 4 个超百行 + _persist_to_db_sync 163 行）
- cq-04 session.js 按领域拆多 store + handleSessionEvent case 下沉 handler 模块
- cq-05 巨型组件优先拆 4 个千行级（DeepResearchView/WorkflowView/ToolSelector/ChatMessage）
- dj-12 系列按影响拆 2-3 个 spec（无行为变化 / API 变化前端适配 / 数据迁移+大重构）；dj-14 get_absolute_url 删除
- rd-02 剩余：anti_oscillation.py 直接删除
- **用户勾选新增**：dj-07 知识库路由双轨收敛、dj-10 校验规范化、cq-07 双异常体系统一（cq-13 暂不做）
- 数据库重置约定：开发阶段整库可删重做，种子只需 daixing 用户 + 大模型配置（厂商/模型/工具）

## 二、死代码与收敛（原 9.2 分析）✅ 基本完成

1. ✅ 直接删除集合（spec:remove-dead-code-batch + 前置分析）：mcp_integration.py、PromptCacheSerializer、僵尸表三模型（ContextRule/AutoMemory/PromptCache + 迁移 + hierarchical_memory.py 399 行）、前端死 schema/常量、多处无效配置、core/__init__ 转发层等，净删约 900+ 行。
2. ✅ 利用/收敛改造：
   - lc-06 strict_rag_chain 收敛（spec:lc06-strict-rag-convergence）：三入口收敛共享内核，净减 146 行 + 17 个等价性测试
   - rd-05 realtimeEvents：Set 枚举替换（随 b1 完成）
   - rd-04：本地表已删、get_max_tokens 已改调 TokenEstimator（两份配置完整合并的确认归 B类第三批）
   - rd-03 get_logger：统一到 logging_utils，未留转发（按「不兼容旧代码」红线）
3. ✅ 并入其他任务：debounce/throttle 已替换 vueuse、配置漂移已删。

## 三、执行路线进度（原推荐顺序）

| 步骤 | 任务 | 状态 |
|------|------|------|
| 1 | A类打包 spec（测试基建 + lc-05 顺带删除） | ✅ 完成 |
| 2 | 死代码删除 spec | ✅ 完成 |
| 3 | B类第一波低风险收敛 | ✅ 完成 |
| 4 | lc-06 strict_rag_chain 收敛 | ✅ 完成 |
| 5 | B类第二批（异常/权限/dev配置） | ✅ 完成 |
| 6 | B类第三批（生产配置/缓存语义，方案已拍板 2026-08-19） | ⬜ **下一步** |
| 7 | C类专项（rd-01 依赖清理 → ds-02 依赖治理 → cq-03/cq-04/cq-05 拆分 → dj-12 系列 → rd-02 剩余） | ⬜ 待启动 |

另：早期已完成 normalize-tools-rest-fix-chat-api（dj-04 N+1 / dj-05 缓存翻页 / dj-06 tools REST 资源化）与 unify-context-injection，均先于本路线执行。

## 四、不变约束（每次动手前核对）

1. 拒绝任何形式的补丁修复，严禁兜底策略，必须直面根因彻底解决；
2. 严格依据 `d:\programming\langchain\langchain_xm\.trae\rules\SpecificationRequirements.md` 开发，不合规代码直接修改，不要别名、映射；
3. 冗余、多余的死代码坚决删除（僵尸表、死模块一律彻底清除，数据库和代码都要处理）；
4. 允许大范围重构，必要时重写相关模块；前后端所有调用点 100% 全部适配（不兼容旧代码）；
5. 测试直接使用 PostgreSQL（贴合实际运行环境，禁 sqlite/内存库）；
6. 数据库在 Docker 容器中的 PostgreSQL；
7. 项目功能前端实测正常，修复必须认真仔细，不能引入回归；
8. 拿不稳的先讨论商量，不瞎猜不幻想；
9. 每个阶段新建 spec（`.trae/specs/<名称>/`），完成后提醒用户并说明做法；
10. 每批完成后跑全量回归（后端 220 + 前端 111 + build + lint），并更新《项目质量分析报告.md》对应条目为 `[✅已修复 日期 · spec:名称]`。

## 五、决策状态记录（B类第二批遗留项，均已拍板）

1. `tasks/*.py` mark_failure(error_message=str(e)) 透出异常原文：**用户已决策（2026-08-19）保留不动**——开发阶段排障优先，发布阶段前再评估。
2. db_monitor / mcp 健康检查类 str(e)：**用户已决策（2026-08-19）全部保留不动**——开发阶段排障优先，发布阶段前再统一收紧。
3. 两处均不属于 dj-03 views 层整改范围，后续 str(e) 扫描勿误改。
