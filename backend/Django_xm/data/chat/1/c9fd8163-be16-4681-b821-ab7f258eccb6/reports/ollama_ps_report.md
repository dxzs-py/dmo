# Ollama 运行模型进程报告

**报告时间**：2026-08-18 11:51:58
**执行命令**：`ollama ps`
**执行状态**：✅ 成功（无错误、无超时）

---

## 一、`ollama ps` 完整输出

```
NAME    ID    SIZE    PROCESSOR    CONTEXT    UNTIL
```

（仅显示表头，无数据行）

---

## 二、运行状态分析

**结论：当前没有任何模型正在运行/加载到内存中。**

| 状态 | 说明 |
|------|------|
| 命令执行 | ✅ 成功，退出正常 |
| 正在运行的模型 | **0 个** |
| 占用资源 | 无 CPU/GPU/显存占用 |
| 备注 | Ollama 守护进程已安装并响应，但当前无模型处于加载状态 |

---

## 三、字段说明（供参考）

`ollama ps` 输出各列含义：

| 列名 | 含义 |
|------|------|
| **NAME** | 模型名称（含 tag，如 `llama3.2:latest`） |
| **ID** | 模型的唯一标识（哈希值） |
| **SIZE** | 模型占用的内存/显存大小 |
| **PROCESSOR** | 处理器类型（100% CPU / 100% GPU / CPU+GPU 混合） |
| **CONTEXT** | 上下文窗口大小（单位 K） |
| **UNTIL** | 模型在内存中保留的截止时间（5m = 5 分钟后自动卸载） |

---

## 四、下一步建议

如需查看/启动模型，可尝试：

```bash
# 查看已安装的模型列表
ollama list

# 启动一个模型（例如 llama3.2），之后再运行 ollama ps 即可看到进程
ollama run llama3.2
```

*本报告由 LC-StudyLab 智能学习助手生成。*
