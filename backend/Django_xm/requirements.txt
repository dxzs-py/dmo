# =====================================================================
# 后端直接依赖清单（rd-01 重写）
#
# 生成依据：全仓 AST import 扫描（顶层/降级路径）+ INSTALLED_APPS /
# MIDDLEWARE / CHANNEL_LAYERS / CACHES / DATABASES / 启动链核对。
# 仅声明直接依赖，传递依赖由 pip 解析；类型存根见 requirements-dev.txt。
# 新环境（Python 3.11 干净 venv）安装回归验证记录见：
#   .trae/specs/c2-requirements-rewrite/verification-report.md
# 可选依赖说明（当前环境未安装、降级路径自动禁用，启用时再补声明）：
#   - Milvus 向量后端：langchain-milvus + pymilvus
#   - NLI 本地校验：torch + transformers
#   - PDF/DOCX/PPTX 备用解析：PyMuPDF(fitz) / python-pptx
# =====================================================================

# --- Web 框架与 ASGI ---
Django>=5.2.12
djangorestframework>=3.17.1
djangorestframework-simplejwt>=5.5.1
django-cors-headers>=4.9.0
django-filter>=25.2
channels>=4.3.2
channels-redis>=4.3.0
daphne>=4.2.2
drf-spectacular>=0.29.0

# --- 数据库与缓存 ---
psycopg2-binary>=2.9.10
django-redis>=6.0.0
# redis-py 8.x 默认 RESP3 握手（HELLO 命令），Redis 5.0 不支持 → 上界 <8 保持 RESP2 兼容
redis>=5.3.1,<8

# --- Celery ---
celery>=5.4.0

# --- LangChain 生态 ---
langchain>=1.2.13
langchain-core>=1.2.22
langchain-classic>=1.0.3
langchain-community>=0.4.1
langchain-openai>=1.0.2
langchain-groq>=1.1.0
langchain-ollama>=1.1.0
langchain-chroma>=0.2.0
langchain-postgres>=0.0.12
langchain-mcp-adapters>=0.1.0
langchain-tavily>=0.2.17
langchain-text-splitters>=1.1.1
langchain-unstructured>=1.0.1
langgraph>=1.1.3
langgraph-checkpoint-sqlite>=3.0.3
# 同时提供 langgraph.checkpoint.postgres 与 langgraph.store.postgres 两个命名空间
langgraph-checkpoint-postgres>=2.0.0
deepagents>=0.7.5

# --- AI provider SDK（直接 import）---
openai>=2.30.0
groq>=0.37.1
tiktoken>=0.12.0
duckduckgo-search>=8.1.1

# --- 向量库客户端（直接 import）---
chromadb>=1.5.9

# --- 文档解析 ---
pypdf>=6.9.2
python-docx>=1.2.0
python-magic>=0.4.27
# Windows 上 python-magic 依赖外部 libmagic.dll，python-magic-bin 提供内置 DLL，
# 消除 unstructured 加载时 "libmagic is unavailable" 警告（Linux/macOS 走 python-magic）
python-magic-bin>=0.4.14 ; platform_system == "Windows"
filetype>=1.2.0

# --- FastAPI 执行服务（run_fastapi.py）---
fastapi>=0.136.1
uvicorn>=0.46.0

# --- 通用工具 ---
httpx>=0.28.1
requests>=2.33.0
pydantic>=2.12.5
pydantic-settings>=2.13.1
python-dotenv>=1.0.1
PyYAML>=6.0.3
loguru>=0.7.3
aiosqlite>=0.22.1
numpy>=2.4.3
# pandas 必须 >=2.2.3 以兼容 numpy 2.x（pandas 2.0.x 按 numpy<2 编译，
# 会导致 UnstructuredLoader 导入 pandas 时抛 "numpy.dtype size changed" → 知识库上传 400）
pandas>=2.2.3
cryptography>=46.0.6
asgiref>=3.11.1
typing_extensions>=4.15.0
pillow>=12.1.1

# --- 依赖方向契约校验（.importlinter 三契约）---
import-linter>=2.0
