# RAG选型文档（含术语释义附录）
## 落地决策：面对真实场景，企业该如何选型？
根据项目所处阶段与业务严苛程度，进行科学的技术栈积木式组合

| 应用场景阶段 | 推荐工程组合 | 核心收益特点 |
| ---- | ---- | ---- |
| 新创项目/敏捷探索 | Simple RAG + Semantic Chunking + Rerank | 极低改造成本，迅速扫清80%常见检索漏洞 |
| 企业生产级知识库 | Hierarchical + Hybrid Search + Rerank + Compression | 高并发下响应迅捷，准确率极稳，严防关键漏检 |
| 高价值复杂推理 | Knowledge Graph + Self-RAG + CRAG | 不计构建成本，主攻长链路严密逻辑推导与动态兜底 |

---

# 附录：全部技术名词简明释义
## 一、基础模块
1. **Simple RAG（基础简易RAG）**
标准最简检索增强生成架构：文档切块→向量化入库→向量检索→拼接上下文交给大模型。优势：开发快、上手门槛低；短板：处理复杂问题、长文档容易丢失信息。适合PoC验证、敏捷原型。

2. **Semantic Chunking（语义分块）**
不再依靠固定字数切割文本，基于语义边界自动切分段落，保证每个片段语义完整。相比固定大小切块，大幅减少语义被强行拆分造成的检索失效。

3. **Rerank（重排序/重排器）**
向量检索粗筛出一批候选片段后，使用小模型二次打分，重新排序，把真正和用户问题相关的文本靠前。是性价比极高、普遍落地的精度优化手段。

## 二、生产级进阶模块
4. **Hierarchical RAG（分层RAG / 层级检索）**
常见为「摘要层+细节层」两级索引：顶层存储文档摘要，底层存储原文片段。先检索摘要定位文档，再读取精细片段。大幅减少检索噪声，适合海量知识库。

5. **Hybrid Search（混合检索）**
**关键词检索（BM25） + 向量检索**两路结果融合。向量擅长语义匹配，关键词擅长专有名词、编号、专业术语召回，互相弥补短板，生产知识库标配方案。

6. **Compression（上下文压缩 / 检索结果压缩）**
将检索回来的冗长段落精简，剔除无关文字，只保留和query相关的核心语句。减少送入大模型的token数量，降低成本、加快响应，同时缓解上下文窗口压力。

## 三、复杂高阶方案
7. **Knowledge Graph（知识图谱）**
把文档信息抽取为【实体-关系-实体】结构化网络。擅长处理多实体关联、因果推理、跨文档关联查询，弥补向量检索难以做逻辑关联的缺陷。缺点：构建、维护成本很高。

8. **Self-RAG（自反思RAG）**
大模型具备自主判断能力：自动判断「是否需要检索知识」「检索内容是否可信」「是否需要再次检索」，动态决定检索轮次，避免无意义检索与错误素材生成。

9. **CRAG（Corrective RAG / 修正式RAG）**
检索后校验素材质量，如果检索到的信息不足、冲突、无关，模型主动修正策略：再次检索、调整查询词，规避“拿到劣质文档直接编造答案”，实现动态兜底。

---

- 🚀 快速验证、初创试点：Simple RAG + 语义分块 + Rerank
- 🏢 企业正式知识库、客服/内部文档平台：分层RAG + 混合检索 + Rerank + 上下文压缩
- ⚖️ 金融、法务、医疗等高严谨推理场景：知识图谱 + Self-RAG + CRAG
