### 核心结论
如果追求彻底、合理、对齐行业成熟方案，最优解是**「单实例常驻执行 + 事件驱动唤醒」架构**——完全对齐 Claude Code、Trae IDE 等产品的 Agent 执行范式，从根源上消灭锁竞争、死锁、接力失败、恢复延迟等所有问题，同时为后续体验升级和能力扩展打下最干净的架构基础。

它和你现有方案的本质区别是：
- 现有方案（包括之前的1/2/3）：**多任务协作模型**——Agent 执行和审批恢复是多个独立 Celery 任务，靠锁/队列接力，问题源于“用并发任务去驱动天然串行的 Agent”。
- 新方案：**单任务全生命周期模型**——一个深度研究任务对应一个 Celery 长任务，从启动到结束全程只有一个执行流，审批等待是任务内部的暂停，靠事件直接唤醒，从根源上消除了并发协作的所有复杂度。

---

## 一、先对齐：成熟产品的底层设计范式
Claude Code、Trae IDE、Cursor 的深度研究模式，在 Agent 执行与中断审批上的核心设计高度一致，本质都是三点：
1.  **单一执行上下文**：一个会话/任务对应唯一的执行协程/线程，全程只有一个执行流。不存在“执行任务”“恢复任务”的拆分，自然没有锁、抢锁、接力的概念。
2.  **中断是原生暂停语义**：审批中断是执行循环内部的主动挂起，不是任务退出。Agent 执行到高危工具时，主动等待审批决策，而不是结束进程、等外部任务来重启。
3.  **事件驱动唤醒**：用户的审批操作通过实时消息通道直接送达执行中的 Agent，收到即恢复，零延迟。

这套范式的核心优势是：**架构简单、语义正确、没有并发副作用**。所有问题都出在“把一个本该串行的执行流拆成了多个并发任务”上，成熟产品从一开始就避免了这个错误。

---

## 二、基于 Celery 的彻底落地方案
不需要推翻你现有的 Celery 技术栈，Celery 依然负责任务调度、资源隔离、失败兜底，只是把“多短任务接力”改成“单长任务常驻 + 内部事件驱动”。

### 整体架构
```
用户审批 → HTTP接口 → 1. 持久化审批结果到DB
                     2. Redis Pub/Sub 发布审批事件
                                          ↓
Celery 长任务（单Agent全生命周期）
├─ 启动 → 加载LangGraph → 执行Agent
├─ 遇到interrupt → 打checkpoint持久化 → 订阅审批事件 → 挂起等待
└─ 收到审批事件 → 校验批次完整性 → 注入decisions → 恢复执行 → 继续循环
```

### 核心模块拆解
#### 1. 执行层：单 Celery 任务 = 全生命周期
一个深度研究任务，全程只对应一个 Celery 任务，不再拆分 `research_task`、`research_resume_task`。
- 任务内部用 `asyncio` 事件循环运行 LangGraph，天然支持异步等待。
- 执行到审批中断点时，任务不退出，也不释放任何资源，而是进入「等待决策」状态，阻塞在事件监听上。
- **核心价值**：全程单执行流，天然串行，彻底消灭 thread 锁、跨批次防护、死锁、接力失败等所有并发问题——因为问题产生的前提（多个任务抢同一个线程）已经不存在了。

#### 2. 唤醒层：Redis Pub/Sub 事件驱动，替代任务派发
用户点击审批/拒绝的接口，不再派发 Celery 恢复任务，改为两步操作：
1.  **持久化**：把审批结果写入数据库，作为最终状态源。
2.  **发信号**：向 `agent:approval:{thread_id}` 的 Redis 频道发布一条轻量的审批事件，包含批次ID和决策结果。

常驻的 Agent 任务内部维护一个事件监听器：
- 收到事件后，从 DB 拉取当前批次的全部审批结果；
- 若本批次所有工具都已决断，就把完整 decisions 注入 LangGraph，立刻恢复执行；
- 若未齐，继续等待下一个事件。

**优势**：
- 零延迟恢复：审批点击后毫秒级唤醒执行，工具结果实时推送，体验和 Claude Code 完全一致。
- 无调度开销：没有 Celery 任务排队、抢锁、重试的成本，资源利用率更高。
- 解耦：审批接口只负责存结果+发信号，不用关心执行任务的状态，逻辑极简。

#### 3. 兜底层：Checkpoint + 巡检机制，覆盖所有异常
针对 Worker 重启、部署发布、进程崩溃等极端场景，做两层兜底：
1.  **强制 Checkpoint**：每次进入审批等待前，强制写入 LangGraph 检查点到持久化存储（DB/Redis），保证状态不丢失。
2.  **低频巡检任务**：用 Celery Beat 跑一个 5 分钟级的巡检任务，扫描「处于待审批状态，但对应 Celery 任务已退出」的异常线程。发现后自动重新拉起长任务，从 Checkpoint 恢复执行，继续监听审批事件。

因为审批结果已经持久化在 DB，任务重启后会自动加载已有的审批结果，用户完全无感知，不需要重复操作。

#### 4. 多批次语义：天然串行，无死锁土壤
因为是单执行流，Agent 永远是“执行一段 → 遇到审批 → 停下等 → 审批完继续执行 → 再遇到下一批审批 → 再停下”，**不会出现多批次同时 pending 的情况**。
- 同一次 interrupt 的多个工具（同一批次）：依然保持“全部决断再恢复”，这是 LangGraph 的原生要求，不影响架构。
- 不同批次的审批：严格按执行顺序先后出现，前一批恢复执行后，才会产生后一批，天然没有互等的可能，死锁从逻辑上就不可能发生。

---

## 三、为什么这是最优解（对比之前的三个方案）
| 维度 | 方案1 死等 | 方案2 接力 | 方案3 轮询 | 本方案 事件驱动单实例 |
| :--- | :--- | :--- | :--- | :--- |
| 根因解决程度 | 补丁，没解决架构问题 | 优化并发协作，仍有多任务复杂度 | 消灭并发，但靠轮询驱动 | 消灭并发前提，架构层面根治 |
| 恢复响应速度 | 慢（轮询间隔） | 较快（接力触发） | 慢（轮询间隔） | 毫秒级（事件直达） |
| 架构复杂度 | 低（但逻辑脏） | 中（队列+接力） | 高（重写触发机制） | 低（单任务，逻辑线性） |
| 资源开销 | 高（多任务空转） | 中（一次一个任务） | 中（单任务常驻） | 低（无空转，无额外调度） |
| 长期扩展性 | 差 | 一般 | 好 | 极好 |
| 对齐成熟产品程度 | 完全不对齐 | 不对齐 | 部分对齐 | 完全对齐 |

简单说：
- 方案1、2是在错误的架构上修修补补，越补逻辑越重；
- 方案3方向对了，但用轮询驱动是倒退，体验和效率都不如事件驱动；
- 本方案直接用成熟产品验证过的正确范式，一次做对，后续没有技术债务。

---

## 四、额外收益：体验与能力全面升级
这套架构不止解决当前的 bug，还能直接补上你提到的其他问题，并且支持后续更多能力：
1.  **任务状态实时同步**：唯一执行任务可以在状态变更时主动推送 WebSocket 事件，前端状态实时更新，不再依赖 SSE 轮询。
2.  **子代理层级天然统一**：单执行流的事件顺序严格一致，`depth/agent_name/agent_path` 字段全程携带，SSE 推送和 DB 落库用同一套数据源，刷新后标识不会丢失，前端可以轻松实现按子代理分组折叠、树形展示。
3.  **支持中途干预**：可以很方便地扩展「终止任务」「追加用户指令」「修改参数」等能力，都通过事件总线发给常驻任务即可，这是现有架构很难实现的。
4.  **可观测性提升**：单任务全生命周期，日志、监控、排障都非常简单，不用跨多个任务排查链路。

---

## 五、落地路径建议（不在乎工作量，分阶段稳落地）
因为改动涉及核心链路，建议分三阶段落地，全程可灰度、可回滚：
### 阶段一：核心架构替换（解决核心问题）
- 实现单长任务的执行循环 + 内部中断等待；
- 接入 Redis Pub/Sub 事件唤醒；
- 实现 Checkpoint 持久化 + 基础巡检兜底；
- 灰度小流量验证，和旧架构并行运行，验证稳定后全量切换。
- 目标：彻底解决死锁、恢复失败、恢复延迟问题。

### 阶段二：体验与一致性补齐
- 接入 WebSocket 状态推送，替换 SSE 轮询；
- 统一事件字段规范，子代理标识全链路落库；
- 完善异常场景兜底、监控告警、全链路日志。
- 目标：解决状态不实时、子代理标识丢失问题，前端体验对齐成熟产品。

### 阶段三：高级能力扩展
- 支持中途终止、暂停、追加指令；
- 支持审批超时自动处理、优先级调度；
- 支持任务断点续跑、跨部署发布无缝衔接。
- 目标：能力上追上 Claude Code / Trae IDE 的交互体验。

---

## 补充：关于 Celery 长任务的常见顾虑
1.  **长任务占 Worker 怎么办？**
    深度研究本身就是长耗时任务，无论拆成多少个短任务，总占用 Worker 的时间是一样的。单长任务反而减少了任务调度、抢锁、重试的额外开销，实际资源利用率更高。可以单独划分一个 `research-long` Celery 队列，和普通业务队列隔离，互不影响。

2.  **发布重启 Worker 会不会丢任务？**
    配合优雅停机 + Checkpoint + 巡检机制，Worker 退出前会持久化状态，重启后巡检会自动拉起任务，用户无感知。成熟的长任务系统都是这么做的，Celery 本身也支持长任务的优雅终止。

3.  **为什么不用协程/单独的 Agent 服务？**
    如果未来规模极大，可以拆独立的 Agent 执行服务，但现阶段基于 Celery 实现成本最低，完全复用现有技术栈，运维和监控体系都不用改，效果是一样的。

---

### 最终总结
如果你的目标是“一次做对、长期好用、对齐行业最佳实践”，不用犹豫，直接上**单实例常驻 + 事件驱动唤醒**的方案。它不是在现有问题上做修复，而是直接换掉了产生问题的底层架构，是真正意义上的“根本修复”，同时能支撑后续很长时间的体验和功能迭代。